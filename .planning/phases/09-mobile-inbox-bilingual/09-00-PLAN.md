---
phase: 09-mobile-inbox-bilingual
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/package.json
  - backend/prisma/schema.prisma
  - backend/src/__tests__/agent/inboxRoutes.test.ts
  - backend/src/__tests__/agent/pushTokenRegister.test.ts
  - backend/src/__tests__/agent/tools/draftCourierMessage.bilingual.test.ts
  - backend/src/__tests__/services/outboundResolver.test.ts
  - backend/src/__tests__/services/notificationChannels.cloudWhatsapp.test.ts
  - backend/src/__tests__/services/notificationChannels.unifonic.test.ts
  - backend/src/__tests__/services/expoPushService.test.ts
  - backend/src/__tests__/services/translationService.test.ts
  - mobile/package.json
  - mobile/app.json
  - mobile/__tests__/setup.ts
  - mobile/__tests__/mocks/expo-notifications.ts
  - mobile/__tests__/mocks/expo-localization.ts
  - mobile/__tests__/inbox/inboxCache.test.ts
  - mobile/__tests__/inbox/pushService.tokenRegister.test.ts
  - mobile/__tests__/inbox/pushService.tokenRotation.test.ts
  - mobile/__tests__/inbox/pushService.notificationChannel.test.ts
  - mobile/__tests__/inbox/InboxRow.bilingual.test.tsx
  - mobile/__tests__/inbox/inboxScreen.deepLink.test.tsx
  - mobile/__tests__/inbox/i18n.languagePref.test.ts
  - .planning/phases/09-mobile-inbox-bilingual/deferred-items.md
autonomous: true
requirements:
  - REQ-mobile-agent-inbox
  - REQ-bilingual-courier-comms

must_haves:
  truths:
    - "Wave 0 RED test files exist for every Phase 9 surface (10 backend + 7 mobile)"
    - "Each failing test has a precise grep-able invariant that turns GREEN exactly when the corresponding Wave 1-3 implementation is correct"
    - "Prisma schema has additive Driver.preferredLanguage + Driver.outboundOptIn + Driver.smsOptOut + Driver.expoPushToken + Tenant.outboundChannels + Tenant.whatsappBusinessAccountId + Tenant.whatsappPhoneNumberId + Tenant.outboundDailyKdLimit columns (NOT YET migrated to DB — Wave 3 runs prisma migrate)"
    - "Language enum (EN | AR | AUTO) added to Prisma schema"
    - "backend/package.json lint:tenant glob includes the new Phase 9 backend files (services/outboundResolver.ts, services/expoPushService.ts, services/translationService.ts, services/notification/whatsappCloudApi.ts, services/notification/unifonicSms.ts, agent/templates/whatsappTemplates.ts, agent/tools/action/draftCourierMessage.ts already in scope)"
    - "Mobile jest-expo runner exits 0 with N failing tests (NOT a runtime crash)"
    - "Backend jest runner includes the 9 new Phase 9 RED files and they FAIL as expected"
    - "deferred-items.md scaffolded for Phase 9 (founder-gated items: WABA per-tenant onboarding + Unifonic NOC paperwork + Khaleeji string review with design partner)"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "Additive Phase 9 columns + Language enum"
      contains: "preferredLanguage"
    - path: "backend/src/__tests__/agent/inboxRoutes.test.ts"
      provides: "RED: GET /api/agent/inbox returns Notifications where metadata.driverId=device.driverId (Pitfall 9 fix); POST /api/agent/inbox/:id/read sets read+readAt; rejects cross-tenant inbox query"
      min_lines: 90
    - path: "backend/src/__tests__/agent/pushTokenRegister.test.ts"
      provides: "RED: POST /api/agent/push-token writes Driver.expoPushToken keyed by deviceId; idempotent re-register; rejects malformed token shape"
      min_lines: 50
    - path: "backend/src/__tests__/agent/tools/draftCourierMessage.bilingual.test.ts"
      provides: "RED: draftCourierMessage rejects when bodyArabic missing; writes both message + bodyAr; titleAr is set; calls outboundResolver.deliver post-approval"
      min_lines: 70
    - path: "backend/src/__tests__/services/outboundResolver.test.ts"
      provides: "RED: resolves AR body when Driver.preferredLanguage=AR; falls back to Tenant.defaultLanguage when AUTO; skips delivery on outboundOptIn=false; respects quiet hours unless severity=CRITICAL; channel order = push→whatsapp→sms; respects Tenant.outboundChannels feature flags"
      min_lines: 90
    - path: "backend/src/__tests__/services/notificationChannels.cloudWhatsapp.test.ts"
      provides: "RED: sendWhatsAppCloud posts to graph.facebook.com/v22.0/{pn-id}/messages with template name + language code + variables; returns ok:false on 4xx; sends nothing when WABA creds absent (returns stub)"
      min_lines: 60
    - path: "backend/src/__tests__/services/notificationChannels.unifonic.test.ts"
      provides: "RED: sendSmsUnifonic posts to api.unifonic.com when creds present; falls back to existing Twilio sendSms when Unifonic creds absent; respects per-tenant daily KD cost ceiling"
      min_lines: 60
    - path: "backend/src/__tests__/services/expoPushService.test.ts"
      provides: "RED: sendExpoPush posts to exp.host/--/api/v2/push/send; payload includes Android channelId=darb-inbox; retries on 429; nulls Device.expoPushToken on DeviceNotRegistered response"
      min_lines: 70
    - path: "backend/src/__tests__/services/translationService.test.ts"
      provides: "RED: translateToArabic returns Khaleeji-register string when ctx.register='khaleeji'; strips U+202D / U+202E override chars from output; Anthropic SDK mocked"
      min_lines: 50
    - path: "mobile/jest.config.js"
      provides: "Phase 9 mocks wired into setupFilesAfterEach"
      contains: "expo-notifications"
    - path: "mobile/__tests__/mocks/expo-notifications.ts"
      provides: "jest mock for getExpoPushTokenAsync + setNotificationChannelAsync + setNotificationHandler + addNotificationReceivedListener + addNotificationResponseReceivedListener"
      min_lines: 30
    - path: "mobile/__tests__/mocks/expo-localization.ts"
      provides: "jest mock for getLocales (default: en-US single entry)"
      min_lines: 10
    - path: "mobile/__tests__/inbox/inboxCache.test.ts"
      provides: "RED: getCachedInbox/setCachedInbox round-trip via expo-sqlite; LRU trims to 50 rows; survives db close+reopen"
      min_lines: 50
    - path: "mobile/__tests__/inbox/pushService.tokenRegister.test.ts"
      provides: "RED: registerPushToken calls getExpoPushTokenAsync({projectId}) then POSTs to /api/agent/push-token; persists last-token to SecureStore"
      min_lines: 40
    - path: "mobile/__tests__/inbox/pushService.tokenRotation.test.ts"
      provides: "RED: when getExpoPushTokenAsync returns a different value from SecureStore'd last-token, POSTs the new token; otherwise no-op"
      min_lines: 35
    - path: "mobile/__tests__/inbox/pushService.notificationChannel.test.ts"
      provides: "RED: bootNotificationHandler calls setNotificationChannelAsync('darb-inbox', ...) before registering; calls setNotificationHandler that returns {shouldShowBanner:true,...}"
      min_lines: 30
    - path: "mobile/__tests__/inbox/InboxRow.bilingual.test.tsx"
      provides: "RED: renders item.titleAr when lang='AR' and bodyAr present; falls back to title when bodyAr null; applies writingDirection:rtl for AR; LTR otherwise"
      min_lines: 50
    - path: "mobile/__tests__/inbox/inboxScreen.deepLink.test.tsx"
      provides: "RED: tapping row with metadata.orderId calls router.push('/(tabs)/orders', {orderId}); also calls markRead(id) before navigation"
      min_lines: 40
    - path: "mobile/__tests__/inbox/i18n.languagePref.test.ts"
      provides: "RED: useLanguagePref reads from SecureStore; defaults to expo-localization first locale (en→EN, ar→AR); fallback EN when locale absent"
      min_lines: 30
    - path: "backend/package.json"
      provides: "lint:tenant script extended with Phase 9 backend file globs"
      contains: "services/outboundResolver.ts"
    - path: ".planning/phases/09-mobile-inbox-bilingual/deferred-items.md"
      provides: "Founder-gated Phase 9 items inventory: WABA per-tenant onboarding, Unifonic NOC paperwork (15 working days), Khaleeji string review with design partner, Expo projectId obtainment"
      min_lines: 30
  key_links:
    - from: "mobile/jest.config.js"
      to: "mobile/__tests__/setup.ts"
      via: "setupFilesAfterEach extended with new mocks"
      pattern: "expo-notifications"
    - from: "backend/src/__tests__/services/outboundResolver.test.ts"
      to: "backend/src/services/outboundResolver.ts (Wave 1)"
      via: "import { deliver, resolveOutboundChannels } from ../../services/outboundResolver"
      pattern: "from.*services/outboundResolver"
    - from: "backend/src/__tests__/agent/inboxRoutes.test.ts"
      to: "backend/src/routes/agent.ts (Wave 1 extension — GET /inbox, POST /inbox/:id/read)"
      via: "supertest(app).get('/api/agent/inbox') and .post('/api/agent/inbox/:id/read')"
      pattern: "/api/agent/inbox"
    - from: "backend/package.json lint:tenant"
      to: "src/services/outboundResolver.ts, src/services/expoPushService.ts, src/services/translationService.ts, src/services/notification/whatsappCloudApi.ts, src/services/notification/unifonicSms.ts, src/agent/templates/whatsappTemplates.ts"
      via: "ESLint glob expansion"
      pattern: "lint:tenant.*outboundResolver"
---

<objective>
Wave 0 — RED test scaffolding + additive Prisma schema + Expo projectId provisioning gate.
Establish the safety net BEFORE any implementation lands. Every behavior shipped in Waves 1-3
has a corresponding failing test that turns GREEN exactly when the behavior is correct. This
is the Nyquist verification floor for Phase 9.

Purpose: Phase 9 wires the bilingual delivery path across three new external providers (Expo
Push, Meta WhatsApp Cloud API, Unifonic SMS) AND extends an existing tool (`draftCourierMessage`)
that already lives behind a propose-and-confirm gate. Without RED tests covering the channel
fan-out + language resolver + Pitfall 9 (`metadata.driverId` vs `userId`) the executor has no
way to know when each surface is correct. The additive schema lands NOW (not at the end) so
Wave 1+ implementation can write Prisma queries against the real columns; the migration itself
runs in Wave 3 once all three channels are wired and the Tenant.outboundChannels feature flags
are usable.

Output:
- 9 backend RED test files (inboxRoutes, pushTokenRegister, draftCourierMessage.bilingual,
  outboundResolver, notificationChannels.cloudWhatsapp, notificationChannels.unifonic,
  expoPushService, translationService) + supporting Prisma schema additions
- 7 mobile RED test files (inboxCache, pushService×3, InboxRow.bilingual, inboxScreen.deepLink,
  i18n.languagePref) + 2 new mocks (expo-notifications, expo-localization)
- Additive Prisma schema columns + Language enum (NOT migrated yet)
- backend/package.json lint:tenant glob extended for Phase 9 backend files
- mobile/app.json updated with extra.eas.projectId placeholder + expo-notifications projectId
  reference (real projectId obtainment is documented in deferred-items.md as user-gated)
- deferred-items.md scaffolded with founder-gated items list
- `cd backend && npm test` runs the new RED files (failures expected — they describe Wave 1-3
  behaviors that don't exist yet)
- `cd mobile && npm test` exits 0 with the new failing tests (NOT a runtime crash)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-mobile-inbox-bilingual/09-RESEARCH.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-mobile-gps-beacon/05-00-PLAN.md
@.planning/phases/05-mobile-gps-beacon/05-02-PLAN.md
@backend/prisma/schema.prisma
@backend/src/agent/tools/action/draftCourierMessage.ts
@backend/src/services/notificationChannels.ts
@backend/src/services/notificationService.ts
@backend/src/routes/agent.ts
@backend/src/queues/notificationQueue.ts
@backend/src/queues/notificationWorker.ts
@backend/package.json
@mobile/package.json
@mobile/app.json
@mobile/jest.config.js
@mobile/__tests__/setup.ts
@mobile/src/services/outbox.ts
@mobile/src/api/client.ts
@mobile/app/(tabs)/_layout.tsx

<interfaces>
<!-- Contracts the RED tests must assert. Implementation lands in Waves 1-3. -->

backend/src/services/outboundResolver.ts (Wave 1) MUST export:
  type ResolvedOutbound { lang: 'EN'|'AR'; body: string; channels: Array<'IN_APP_PUSH'|'WHATSAPP'|'SMS'>; quietHoursSuppressed: boolean }
  resolveOutboundChannels(args: { driverId: string; tenantId: string; severity: 'LOW'|'MEDIUM'|'HIGH'|'CRITICAL' }): Promise<ResolvedOutbound | null>
  deliver(args: { tenantId: string; notificationId: string; driverId: string; bodyEn: string; bodyAr: string; channelHint?: string; severity: string; intent?: string }): Promise<{ enqueued: string[]; suppressed: boolean }>

backend/src/services/expoPushService.ts (Wave 1) MUST export:
  sendExpoPush(messages: Array<{ to: string; title: string; body: string; data?: Record<string, unknown>; badge?: number; channelId?: string }>): Promise<Array<{ status: 'ok'|'error'; message?: string; details?: { error?: string } }>>

backend/src/services/translationService.ts (Wave 1) MUST export:
  type TranslationContext { audience: 'kuwait-courier' | 'kuwait-supervisor' | 'generic'; register: 'khaleeji' | 'msa'; preserveLatinTokens?: string[] }
  translateToArabic(en: string, ctx: TranslationContext): Promise<string>

backend/src/services/notification/whatsappCloudApi.ts (Wave 2) MUST export:
  sendWhatsAppCloud(args: { phone: string; templateName: string; templateLang: 'en'|'ar'; variables: string[]; phoneNumberId: string; accessToken: string }): Promise<{ ok: boolean; provider: 'whatsapp-cloud'; error?: string; metaMessageId?: string }>

backend/src/services/notification/unifonicSms.ts (Wave 3) MUST export:
  sendSmsUnifonic(args: { phone: string; message: string; senderId: string; appSid: string }): Promise<{ ok: boolean; provider: 'unifonic'|'twilio-fallback'; error?: string }>

backend/src/agent/templates/whatsappTemplates.ts (Wave 2) MUST export:
  type WhatsappTemplate { intent: string; nameEn: string; nameAr: string; variableCount: number; category: 'UTILITY' }
  TEMPLATES: Record<string, WhatsappTemplate>  // keyed by intent
  templateFor(intent: string, lang: 'en'|'ar'): WhatsappTemplate | null

backend/src/routes/agent.ts (Wave 1 extension) MUST add:
  POST /push-token  — body: { deviceId: string, expoPushToken: string }  → writes Driver.expoPushToken via Device.driverId join; 404 if device missing
  GET  /inbox       — query: ?limit=50&since=ISO  → Notification.findMany where tenantId=device.tenantId AND metadata.driverId=device.driverId; orderBy createdAt desc; returns { items, unreadCount }
  POST /inbox/:id/read   — sets read=true, readAt=now() on Notification by id where tenantId+metadata.driverId match; 404 otherwise
  POST /inbox/opt-out    — sets Driver.outboundOptIn=false on the device's driverId

Prisma schema additions (this Wave — additive only, NO migrate run):
  enum Language { EN AR AUTO }
  Driver:
    preferredLanguage  Language @default(AUTO)
    outboundOptIn      Boolean  @default(true)
    smsOptOut          Boolean  @default(false)
    expoPushToken      String?
  Tenant:
    defaultLanguage          Language @default(AR)
    outboundChannels         Json     @default("{\"inApp\":true,\"whatsapp\":false,\"sms\":false}")
    whatsappBusinessAccountId String?
    whatsappPhoneNumberId    String?
    outboundDailyKdLimit     Decimal? @db.Decimal(10, 3)  // null = unlimited; default seeded to 5.000 in Wave 3 migration

Notification reuse (NO schema change to Notification — Pitfall 9 fix is at the query layer, not the schema):
  - Continue using existing titleAr/bodyAr/category fields shipped by Phase 2
  - GET /api/agent/inbox query MUST filter on `metadata->>'driverId' = device.driverId` AND
    `tenantId = device.tenantId` — explicitly NOT on Notification.userId

Mobile contract (Wave 1):
  mobile/src/services/inboxCache.ts MUST export:
    getCachedInbox(): Promise<InboxNotification[]>
    setCachedInbox(items: InboxNotification[]): Promise<void>
    Schema: inbox_cache(id TEXT PRIMARY KEY, payload TEXT, createdAt INTEGER, read INTEGER)
    LRU: trims to 50 newest rows
    Same darb-outbox.db file Phase 5 uses (separate table).

  mobile/src/services/pushService.ts MUST export:
    registerPushToken(): Promise<{ token: string | null; changed: boolean }>
    bootNotificationHandler(): Promise<void>  // sets channel + handler + listeners

  mobile/src/services/i18n.ts MUST export:
    useLanguagePref(): 'AR' | 'EN'  // reads SecureStore key 'lang_pref'; falls back to expo-localization getLocales()[0].languageCode
    setLanguagePref(lang: 'AR'|'EN'|'AUTO'): Promise<void>
    t(key: string): string  // i18n-js wrapper

  mobile/src/api/inboxApi.ts MUST export:
    listInbox(args?: { limit?: number; since?: string }): Promise<{ items: InboxNotification[]; unreadCount: number }>
    markRead(notificationId: string): Promise<void>
    archiveInbox(notificationId: string): Promise<void>  // sets metadata.archived=true
    registerPushToken(token: string): Promise<void>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive Prisma schema (Language enum + Driver/Tenant columns) + backend lint:tenant scope</name>
  <files>backend/prisma/schema.prisma, backend/package.json</files>

  <read_first>
    - backend/prisma/schema.prisma model Driver (line 490) — see existing Phase 5 additions (no batteryLevel/expoPushToken on Driver yet)
    - backend/prisma/schema.prisma model Tenant (line 346) — see existing Phase 2 additions (designPartner, monthlyOverrideKd, trialEndsAt, settings)
    - backend/prisma/schema.prisma Notification (line 1659) — titleAr/bodyAr/category already present
    - backend/package.json line 17 — current lint:tenant glob list
  </read_first>

  <action>
    Step 1 — Add the Language enum near the top of `backend/prisma/schema.prisma` (after the existing `Platform` / `VehicleType` enums; group with other small enums). Search for `enum Platform` to locate; insert below the platform enum block:

      enum Language {
        EN
        AR
        AUTO
      }

    Step 2 — Extend `model Tenant` (line 346). Find the existing settings/createdAt block and insert ABOVE `settings`:

      // Phase 9 Wave 0 — additive bilingual outbound + channel-config fields.
      // Migration runs in Wave 3 alongside Unifonic adapter ship. designPartner-1
      // tenants get outboundChannels seeded {inApp:true, whatsapp:false, sms:false}
      // until WABA verification and Unifonic NOC clear (founder-gated).
      defaultLanguage          Language @default(AR)
      outboundChannels         Json     @default("{\"inApp\":true,\"whatsapp\":false,\"sms\":false}")
      whatsappBusinessAccountId String?
      whatsappPhoneNumberId    String?
      outboundDailyKdLimit     Decimal? @db.Decimal(10, 3)

    Step 3 — Extend `model Driver` (line 490). Find the existing photoUrl/supervisorId block and insert ABOVE `monthlySalary`:

      // Phase 9 Wave 0 — bilingual outbound preferences + push token.
      preferredLanguage  Language @default(AUTO)
      outboundOptIn      Boolean  @default(true)
      smsOptOut          Boolean  @default(false)
      expoPushToken      String?  // ExponentPushToken[...] — rotated per Pitfall 4

    NOTE: Driver already has a one-to-one with Device (line 548). The Wave 0 RESEARCH section "Architectural Component Responsibilities" says push tokens live on `Device.expoPushToken` (per device, not per driver). I am putting it on `Driver.expoPushToken` instead because (a) Device has @@unique on driverId so it's effectively 1:1 anyway, (b) the outboundResolver query is per-driver and reading Driver.expoPushToken avoids a join in the hot path, (c) if a driver changes phones the next register call updates the same Driver row. The Device row continues to own deviceId/imei/lastSeen/batteryLevel — token is the only Phase 9 add. Document this choice in the Wave 1 SUMMARY.

    Step 4 — Run `prisma format` to verify syntax:
      cd backend && npx prisma format

    Expected: no errors. The schema parses; new columns are visible.

    Step 5 — Do NOT run `prisma migrate dev` yet. Wave 3 is the migration wave (per Phase 5's pattern of deferring the migration until all wires are in place). The schema parses + Prisma client regenerates so Wave 1-2 TypeScript can compile against the new fields — but the DB columns don't exist until Wave 3.

    Step 5b — Regenerate the Prisma client so Wave 1 TypeScript can reference the new fields:
      cd backend && npx prisma generate

    Expected: regenerated client; new types `Language` (enum), `Driver.preferredLanguage`, `Tenant.outboundChannels`, etc. exported from @prisma/client.

    Step 6 — Extend `backend/package.json` lint:tenant glob. The current line 17 looks like:

      "lint:tenant": "eslint src/agent/ src/__tests__/agent/ ... src/services/ingest/ --ext .ts --no-eslintrc ..."

    Add these targets BEFORE `src/services/ingest/`:

      src/services/outboundResolver.ts
      src/services/expoPushService.ts
      src/services/translationService.ts
      src/services/notification/
      src/agent/templates/whatsappTemplates.ts
      src/__tests__/services/outboundResolver.test.ts
      src/__tests__/services/expoPushService.test.ts
      src/__tests__/services/translationService.test.ts
      src/__tests__/services/notificationChannels.cloudWhatsapp.test.ts
      src/__tests__/services/notificationChannels.unifonic.test.ts
      src/__tests__/agent/inboxRoutes.test.ts
      src/__tests__/agent/pushTokenRegister.test.ts
      src/__tests__/agent/tools/draftCourierMessage.bilingual.test.ts

    Append them space-separated, preserving the existing flag tail (`--ext .ts --no-eslintrc --resolve-plugins-relative-to . --rulesdir eslint-rules --parser @typescript-eslint/parser --parser-options=ecmaVersion:2022,sourceType:module --rule \"{\\\"no-prisma-without-tenant\\\":\\\"error\\\"}\" --no-error-on-unmatched-pattern`).

    NOTE: per Phase 5 deferred-items.md, `backend/src/routes/agent.ts` is NOT in lint:tenant scope yet because of 5 pre-existing violations in /selfie /commands /register. Wave 1 of Phase 9 extends agent.ts; we follow the same pattern (don't add agent.ts to scope this phase). The Wave 1 new code uses the existing `device→driver→tenantId` pattern so the cross-tenant boundary is enforced by query shape regardless of lint coverage. Document this in deferred-items.md Task 4.

    Step 7 — Verify lint:tenant still passes with the new (empty) target files:
      cd backend && npm run lint:tenant
    Expect: exits 0 (no files match the new globs yet — `--no-error-on-unmatched-pattern` flag handles it).

    Self-check:
    - `npx prisma format` exits 0
    - `npx prisma generate` exits 0; `node -e "console.log(Object.keys(require('./src/generated/prisma')))"` includes `Language`
    - `npm run lint:tenant` exits 0
  </action>

  <verify>
    <automated>cd /Users/mac/Documents/Darb/backend && npx prisma format 2>&1 | tail -3 && npx prisma generate 2>&1 | grep -E "Generated|generated" | head -2 && npm run lint:tenant 2>&1 | tail -3</automated>
  </verify>

  <done>
    - schema.prisma has Language enum + 5 new Tenant columns + 4 new Driver columns
    - `npx prisma format` exits 0
    - `npx prisma generate` regenerates the client with the new fields
    - backend/package.json lint:tenant scope extended; lint:tenant exits 0
    - NO migration run yet (deferred to Wave 3)
  </done>
</task>

<task type="auto">
  <name>Task 2: Backend RED test scaffolding (9 files, all failing)</name>
  <files>backend/src/__tests__/agent/inboxRoutes.test.ts, backend/src/__tests__/agent/pushTokenRegister.test.ts, backend/src/__tests__/agent/tools/draftCourierMessage.bilingual.test.ts, backend/src/__tests__/services/outboundResolver.test.ts, backend/src/__tests__/services/notificationChannels.cloudWhatsapp.test.ts, backend/src/__tests__/services/notificationChannels.unifonic.test.ts, backend/src/__tests__/services/expoPushService.test.ts, backend/src/__tests__/services/translationService.test.ts</files>

  <read_first>
    - backend/src/__tests__/agent/locationIngest.test.ts (Phase 5 example — supertest + prisma mock shape)
    - backend/src/__tests__/services/activePlatformAttribution.test.ts (Phase 5 example — service unit-test shape)
    - backend/src/__tests__/mocks/config.ts (extend with new prisma delegates if needed: driver.update for push-token; notification.findMany for inbox)
    - backend/src/agent/tools/action/draftCourierMessage.ts (existing Phase 2 shape — extension contract)
  </read_first>

  <action>
    Step 1 — Verify backend/src/__tests__/mocks/config.ts exposes the prisma delegates these tests need. The mock file already exposes most prisma models (Phases 1-6 added them). Confirm presence of:
      prisma.notification.findMany / .update / .findFirst
      prisma.driver.update / .findFirst
      prisma.device.findUnique
      prisma.tenant.findUnique

    If any is missing, add as `jest.fn()` — but do NOT add new method names that don't already exist on the schema. The Wave 0 plan only declares mock shape; the implementations supply the real values in Waves 1-3.

    Step 2 — Create backend/src/__tests__/agent/inboxRoutes.test.ts (RED). The file describes 5 behaviors:

      describe("GET /api/agent/inbox", () => {
        it("returns Notifications where metadata.driverId equals device.driverId (Pitfall 9 fix)", ...);
        it("rejects cross-tenant — Notifications from tenant B never appear for tenant A's device", ...);
        it("returns unreadCount in response body", ...);
        it("filters category to OPS_TODO/COURIER_MESSAGE — IMPORTANT-only notifications stay on the owner inbox", ...);
        it("supports ?since=ISO query — only Notifications createdAt>=since", ...);
      });
      describe("POST /api/agent/inbox/:id/read", () => {
        it("sets read=true + readAt=now() on the notification when device.driverId matches metadata.driverId", ...);
        it("rejects 404 when the notification belongs to a different driver", ...);
        it("rejects 404 when the notification belongs to a different tenant", ...);
        it("is idempotent — second call leaves readAt unchanged", ...);
      });

    Each `it` block contains a supertest call against the app (imported from backend/src/server.ts or backend/src/app.ts — check Phase 5 test imports for the canonical pattern). Use the existing mock prisma client. Tests will fail because routes don't exist yet (404 on every path).

    Wave 0 deliverable: file exists with all 9 `it` blocks, each calling supertest and asserting expected response shape. They MUST fail — the Wave 1 implementation makes them pass.

    Step 3 — Create backend/src/__tests__/agent/pushTokenRegister.test.ts (RED):

      describe("POST /api/agent/push-token", () => {
        it("writes Driver.expoPushToken via Device.driverId join", ...);
        it("rejects 400 on malformed token (must start with 'ExponentPushToken[')", ...);
        it("returns 404 when deviceId doesn't exist", ...);
        it("returns 404 when device has no driver assigned", ...);
        it("is idempotent — re-registering same token returns 200 with unchanged DB state", ...);
      });

    Step 4 — Create backend/src/__tests__/agent/tools/draftCourierMessage.bilingual.test.ts (RED). This extends the EXISTING draftCourierMessage shape — the current Phase 2 tool only requires bodyEnglish; Wave 1 adds bodyArabic as required.

      describe("draftCourierMessage Phase 9 extension", () => {
        it("rejects via Zod when bodyArabic is missing", ...);
        it("rejects when bodyArabic is shorter than 20 chars", ...);
        it("rejects when bodyArabic is longer than 500 chars", ...);
        it("writes both message (English) AND bodyAr (Arabic) on Notification.create", ...);
        it("sets titleAr to '\\u0631\\u0633\\u0627\\u0644\\u0629 \\u0625\\u0644\\u0649 ' + driver.name", ...);
        it("calls outboundResolver.deliver after Notification.create when ctx.userId is set (post-approval path)", ...);
        it("editableParams includes both 'bodyEnglish' AND 'bodyArabic' (Pitfall 10 fix)", ...);
        it("DOES NOT call outboundResolver.deliver when driver.outboundOptIn=false — opt-out short-circuits", ...);
      });

    Mock outboundResolver via jest.mock("../../../services/outboundResolver") returning a `deliver: jest.fn()`.

    Step 5 — Create backend/src/__tests__/services/outboundResolver.test.ts (RED):

      describe("outboundResolver.resolveOutboundChannels", () => {
        it("returns AR body + lang='AR' when Driver.preferredLanguage='AR'", ...);
        it("returns EN body + lang='EN' when Driver.preferredLanguage='EN'", ...);
        it("falls back to Tenant.defaultLanguage when Driver.preferredLanguage='AUTO'", ...);
        it("returns null + suppressed=true when Driver.outboundOptIn=false", ...);
        it("returns channel order [IN_APP_PUSH, WHATSAPP, SMS] when all three enabled in Tenant.outboundChannels", ...);
        it("omits WHATSAPP when Tenant.whatsappPhoneNumberId is null", ...);
        it("omits SMS when Driver.smsOptOut=true", ...);
        it("omits IN_APP_PUSH when Driver.expoPushToken is null", ...);
        it("sets quietHoursSuppressed=true between 22:00-06:00 Kuwait time", ...);
        it("ignores quiet hours when severity='CRITICAL'", ...);
      });
      describe("outboundResolver.deliver", () => {
        it("enqueues IN_APP_PUSH only for severity=MEDIUM when push token present (Stage-1 default)", ...);
        it("fans out to WHATSAPP and SMS in addition to push when severity=HIGH or CRITICAL", ...);
        it("returns suppressed=true when driver opted out, enqueued=[]", ...);
        it("does NOT exceed Tenant.outboundDailyKdLimit — circuit breaker at 100% (count NotificationDelivery rows for today, sum estimated cost)", ...);
      });

    Step 6 — Create backend/src/__tests__/services/notificationChannels.cloudWhatsapp.test.ts (RED). Mock global.fetch.

      describe("sendWhatsAppCloud", () => {
        it("posts to https://graph.facebook.com/v22.0/{phoneNumberId}/messages with correct shape", ...);
        it("body includes messaging_product='whatsapp', type='template', template.name + language.code + components[0].parameters", ...);
        it("phone is normalised — leading '+' stripped before send", ...);
        it("returns ok:true with metaMessageId on 200", ...);
        it("returns ok:false with error message on 400", ...);
        it("returns ok:false with provider='whatsapp-cloud' when accessToken missing", ...);
        it("encodes Arabic variables as UTF-8 in JSON body (no \\u escapes leak)", ...);
      });

    Step 7 — Create backend/src/__tests__/services/notificationChannels.unifonic.test.ts (RED):

      describe("sendSmsUnifonic", () => {
        it("posts to api.unifonic.com/rest/SMS/messages with appSid + senderId + recipient + body", ...);
        it("returns ok:true on Unifonic 200 with status='Success'", ...);
        it("falls back to existing Twilio sendSms when UNIFONIC_APP_SID env var absent", ...);
        it("returns ok:false on Unifonic 4xx", ...);
        it("encodes Arabic body as UTF-8 (Unifonic uses application/x-www-form-urlencoded)", ...);
      });

    Step 8 — Create backend/src/__tests__/services/expoPushService.test.ts (RED). Mock global.fetch.

      describe("sendExpoPush", () => {
        it("posts to https://exp.host/--/api/v2/push/send", ...);
        it("payload is an array of message objects (Expo accepts batches up to 100)", ...);
        it("includes Android channelId='darb-inbox' in every message (Pitfall 1)", ...);
        it("returns the .data array from Expo response", ...);
        it("retries on HTTP 429 with exponential backoff (max 3 attempts)", ...);
        it("nulls Driver.expoPushToken when Expo returns status='error' with details.error='DeviceNotRegistered' (Pitfall 4)", ...);
        it("includes EXPO_ACCESS_TOKEN as Authorization Bearer when env var present", ...);
      });

    Step 9 — Create backend/src/__tests__/services/translationService.test.ts (RED). Mock the @anthropic-ai/sdk default export.

      describe("translateToArabic", () => {
        it("returns a non-empty string when Anthropic returns a content block", ...);
        it("strips U+202D (LEFT-TO-RIGHT OVERRIDE) and U+202E (RIGHT-TO-LEFT OVERRIDE) from output (security)", ...);
        it("preserves Latin tokens listed in ctx.preserveLatinTokens (driver names, numbers, 'Darb')", ...);
        it("passes register='khaleeji' into the Anthropic prompt as a system-message constraint", ...);
        it("throws clear error when ANTHROPIC_API_KEY missing", ...);
      });

    Step 10 — Run all 9 new RED files to confirm they FAIL with the expected reasons (missing implementations, not crashes):
      cd backend && npx jest src/__tests__/agent/inboxRoutes.test.ts src/__tests__/agent/pushTokenRegister.test.ts src/__tests__/agent/tools/draftCourierMessage.bilingual.test.ts src/__tests__/services/outboundResolver.test.ts src/__tests__/services/notificationChannels.cloudWhatsapp.test.ts src/__tests__/services/notificationChannels.unifonic.test.ts src/__tests__/services/expoPushService.test.ts src/__tests__/services/translationService.test.ts 2>&1 | tail -20

    Expected: all 9 files run; most `it` blocks fail with `Cannot find module '../../services/outboundResolver'` or similar import errors — that IS the RED signal. A few may pass trivially if the implementation file gets stubbed for module resolution; that's fine.

    Step 11 — Run the full backend test suite to confirm NO regression on Phases 1-6:
      cd backend && npm test 2>&1 | tail -10

    Expected: pre-existing test count unchanged; only new Phase 9 RED tests fail.

    Step 12 — Run lint:tenant on the new test files:
      cd backend && npm run lint:tenant 2>&1 | tail -3

    Expected: exits 0. Test files using `prisma.X.findMany({ where: { tenantId } })` etc. pass the lint rule (every where clause includes tenantId, OR the call is on the mock and lint skips fixtures-only files per existing patterns).

    Self-check:
    - 9 new files exist under backend/src/__tests__/
    - Each file has >=5 `it` blocks
    - npm test from backend reports an INCREASED failure count (the new RED files) but no error in pre-existing tests
  </action>

  <verify>
    <automated>cd /Users/mac/Documents/Darb/backend && npx jest src/__tests__/agent/inboxRoutes.test.ts src/__tests__/agent/pushTokenRegister.test.ts src/__tests__/agent/tools/draftCourierMessage.bilingual.test.ts src/__tests__/services/outboundResolver.test.ts src/__tests__/services/expoPushService.test.ts src/__tests__/services/translationService.test.ts src/__tests__/services/notificationChannels.cloudWhatsapp.test.ts src/__tests__/services/notificationChannels.unifonic.test.ts --passWithNoTests 2>&1 | grep -E "Tests:.*failed|Tests:.*passed|Test Suites" | head -3</automated>
  </verify>

  <done>
    - 9 backend RED test files exist
    - npx jest on the 9 files runs (no module-resolution crash that prevents Jest from even loading)
    - Tests fail with import-resolution or assertion errors describing the expected Wave 1-3 behaviors
    - Pre-existing test count unchanged in `cd backend && npm test`
    - lint:tenant still exits 0
  </done>
</task>

<task type="auto">
  <name>Task 3: Mobile RED test scaffolding + new mocks + app.json projectId placeholder + deferred-items.md</name>
  <files>mobile/__tests__/setup.ts, mobile/__tests__/mocks/expo-notifications.ts, mobile/__tests__/mocks/expo-localization.ts, mobile/__tests__/inbox/inboxCache.test.ts, mobile/__tests__/inbox/pushService.tokenRegister.test.ts, mobile/__tests__/inbox/pushService.tokenRotation.test.ts, mobile/__tests__/inbox/pushService.notificationChannel.test.ts, mobile/__tests__/inbox/InboxRow.bilingual.test.tsx, mobile/__tests__/inbox/inboxScreen.deepLink.test.tsx, mobile/__tests__/inbox/i18n.languagePref.test.ts, mobile/app.json, .planning/phases/09-mobile-inbox-bilingual/deferred-items.md</files>

  <read_first>
    - mobile/__tests__/setup.ts (Phase 5 existing — see what jest.mock blocks are registered)
    - mobile/__tests__/mocks/expo-sqlite.ts (Phase 5 mock pattern to mirror for expo-notifications)
    - mobile/__tests__/outbox.idempotency.test.ts (Phase 5 test shape)
    - mobile/app.json (top-level expo config to extend)
    - .planning/phases/05-mobile-gps-beacon/deferred-items.md (style template)
  </read_first>

  <action>
    Step 1 — Create mobile/__tests__/mocks/expo-notifications.ts. Mirror the Phase 5 expo-sqlite mock style:

      // Phase 9 Wave 0 — expo-notifications jest mock.
      // Provides in-memory stand-ins for: getExpoPushTokenAsync, setNotificationChannelAsync,
      // setNotificationHandler, addNotificationReceivedListener, addNotificationResponseReceivedListener,
      // requestPermissionsAsync, getPermissionsAsync, AndroidImportance enum.

      const __state = {
        token: "ExponentPushToken[mock-token-abc123]",
        channels: new Map<string, any>(),
        handler: null as any,
        listeners: [] as Array<{ kind: 'received' | 'response'; cb: any }>,
        permission: 'granted' as 'granted' | 'denied' | 'undetermined',
      };

      export async function getExpoPushTokenAsync(opts: { projectId?: string }) {
        if (!opts?.projectId) throw new Error("Expo: projectId required");
        return { data: __state.token, type: 'expo' as const };
      }

      export async function setNotificationChannelAsync(channelId: string, config: any) {
        __state.channels.set(channelId, config);
      }

      export function setNotificationHandler(h: any) {
        __state.handler = h;
      }

      export function addNotificationReceivedListener(cb: any) {
        const sub = { remove: () => {
          __state.listeners = __state.listeners.filter(l => l.cb !== cb);
        }};
        __state.listeners.push({ kind: 'received', cb });
        return sub;
      }

      export function addNotificationResponseReceivedListener(cb: any) {
        const sub = { remove: () => {
          __state.listeners = __state.listeners.filter(l => l.cb !== cb);
        }};
        __state.listeners.push({ kind: 'response', cb });
        return sub;
      }

      export async function requestPermissionsAsync() {
        return { status: __state.permission, granted: __state.permission === 'granted' };
      }

      export async function getPermissionsAsync() {
        return { status: __state.permission, granted: __state.permission === 'granted' };
      }

      export const AndroidImportance = {
        UNSPECIFIED: 0, NONE: 1, MIN: 2, LOW: 3, DEFAULT: 4, HIGH: 5, MAX: 6,
      };

      // Test seam — Phase 9 tests use these to reset/inspect state.
      export const __testHelpers = {
        reset: () => {
          __state.token = "ExponentPushToken[mock-token-abc123]";
          __state.channels.clear();
          __state.handler = null;
          __state.listeners = [];
          __state.permission = 'granted';
        },
        setToken: (t: string) => { __state.token = t; },
        setPermission: (p: 'granted' | 'denied' | 'undetermined') => { __state.permission = p; },
        getChannels: () => __state.channels,
        getHandler: () => __state.handler,
        fireReceived: (n: any) => {
          __state.listeners.filter(l => l.kind === 'received').forEach(l => l.cb(n));
        },
        fireResponse: (n: any) => {
          __state.listeners.filter(l => l.kind === 'response').forEach(l => l.cb(n));
        },
      };

      export default {
        getExpoPushTokenAsync, setNotificationChannelAsync, setNotificationHandler,
        addNotificationReceivedListener, addNotificationResponseReceivedListener,
        requestPermissionsAsync, getPermissionsAsync, AndroidImportance, __testHelpers,
      };

    Step 2 — Create mobile/__tests__/mocks/expo-localization.ts:

      const __state = { locales: [{ languageCode: 'en', regionCode: 'US' }] };
      export function getLocales() { return __state.locales; }
      export const __testHelpers = {
        setLocales: (l: Array<{ languageCode: string; regionCode?: string }>) => { __state.locales = l; },
        reset: () => { __state.locales = [{ languageCode: 'en', regionCode: 'US' }]; },
      };
      export default { getLocales, __testHelpers };

    Step 3 — Extend mobile/__tests__/setup.ts. Append after the Phase 5 jest.mock blocks:

      // Phase 9 — expo-notifications + expo-localization mocks.
      jest.mock("expo-notifications", () => require("./mocks/expo-notifications"));
      jest.mock("expo-localization", () => require("./mocks/expo-localization"));

      // Phase 9 — i18n-js: simple identity translator for tests.
      jest.mock("i18n-js", () => {
        return {
          I18n: class {
            translations: any = {};
            locale: string = 'en';
            t(key: string) { return this.translations?.[this.locale]?.[key] ?? key; }
          },
        };
      });

      // Phase 9 — @expo-google-fonts/tajawal: tests don't load fonts; treat as ready.
      jest.mock("@expo-google-fonts/tajawal", () => ({
        useFonts: () => [true, null],
        Tajawal_400Regular: 'Tajawal_400Regular',
        Tajawal_700Bold: 'Tajawal_700Bold',
        Tajawal_500Medium: 'Tajawal_500Medium',
      }));

      // Reset Phase 9 mock state between tests.
      afterEach(() => {
        try { require("./mocks/expo-notifications").__testHelpers.reset(); } catch {}
        try { require("./mocks/expo-localization").__testHelpers.reset(); } catch {}
      });

    Step 4 — Create mobile/__tests__/inbox/inboxCache.test.ts (RED):

      import * as SQLite from "expo-sqlite";
      import { getCachedInbox, setCachedInbox } from "../../src/services/inboxCache";

      describe("inboxCache", () => {
        beforeEach(() => { /* reset mock SQLite db state */ });
        it("setCachedInbox writes rows; getCachedInbox returns them in createdAt-desc order", async () => { ... });
        it("LRU trims to 50 rows — 60-row batch leaves only the 50 newest", async () => { ... });
        it("survives db close+reopen (uses the persistent darb-outbox.db, not in-memory)", async () => { ... });
        it("returns [] when table is empty (does not throw)", async () => { ... });
        it("stores Notification.metadata as a JSON-stringified payload — round-trips intact", async () => { ... });
      });

    Step 5 — Create mobile/__tests__/inbox/pushService.tokenRegister.test.ts (RED):

      jest.mock("../../src/api/inboxApi");
      import * as SecureStore from "expo-secure-store";
      import * as Notifications from "expo-notifications";
      import { registerPushToken } from "../../src/services/pushService";

      describe("registerPushToken", () => {
        it("calls getExpoPushTokenAsync with the projectId from app.json extra.eas.projectId", async () => { ... });
        it("POSTs the token to /api/agent/push-token via inboxApi.registerPushToken", async () => { ... });
        it("persists last-token to SecureStore under key 'last_expo_push_token'", async () => { ... });
        it("returns { token, changed:true } on first registration", async () => { ... });
        it("returns { token, changed:false } when token matches SecureStore'd last-token (no POST)", async () => { ... });
        it("returns { token:null, changed:false } when permission is denied", async () => { ... });
      });

    Step 6 — Create mobile/__tests__/inbox/pushService.tokenRotation.test.ts (RED):

      describe("registerPushToken rotation (Pitfall 4)", () => {
        it("when getExpoPushTokenAsync returns a DIFFERENT token from SecureStore's last-token, POSTs the new value", ...);
        it("when expo returns the SAME token, does NOT POST", ...);
        it("clears SecureStore'd token when Expo throws", ...);
      });

    Step 7 — Create mobile/__tests__/inbox/pushService.notificationChannel.test.ts (RED):

      import { bootNotificationHandler } from "../../src/services/pushService";
      import * as Notifications from "expo-notifications";

      describe("bootNotificationHandler", () => {
        it("calls setNotificationChannelAsync('darb-inbox', { importance: HIGH }) before any push registration (Pitfall 1)", ...);
        it("calls setNotificationHandler with handler returning { shouldShowBanner:true, shouldShowList:true, shouldPlaySound:true, shouldSetBadge:true } (Pitfall 8)", ...);
        it("registers an addNotificationReceivedListener callback", ...);
        it("returns within 3 seconds (otherwise expo-notifications discards the notification per Pitfall 8)", ...);
      });

    Step 8 — Create mobile/__tests__/inbox/InboxRow.bilingual.test.tsx (RED):

      import { render } from "@testing-library/react-native";
      import { InboxRow } from "../../src/components/inbox/InboxRow";

      describe("InboxRow", () => {
        it("renders item.title when lang='EN'", ...);
        it("renders item.titleAr when lang='AR' AND bodyAr present", ...);
        it("falls back to title when lang='AR' but bodyAr is null", ...);
        it("applies writingDirection='rtl' on Text components when lang='AR'", ...);
        it("applies writingDirection='ltr' when lang='EN'", ...);
        it("shows unread dot when item.read=false", ...);
        it("renders platform colour stripe on borderLeft when item.metadata.platform set", ...);
      });

    Step 9 — Create mobile/__tests__/inbox/inboxScreen.deepLink.test.tsx (RED):

      jest.mock("expo-router", () => ({ useRouter: () => ({ push: jest.fn() }), Stack: () => null }));
      jest.mock("../../src/api/inboxApi");

      describe("InboxScreen", () => {
        it("tapping a row with metadata.orderId calls router.push('/(tabs)/orders', { orderId })", ...);
        it("tapping a row calls markRead(item.id) BEFORE navigating", ...);
        it("does NOT call markRead when item.read is already true", ...);
        it("does NOT navigate when metadata.orderId is absent — opens detail panel instead", ...);
        it("pull-to-refresh re-fetches via listInbox()", ...);
        it("falls back to cached inbox when network fails (offline path)", ...);
      });

    Step 10 — Create mobile/__tests__/inbox/i18n.languagePref.test.ts (RED):

      import * as Loc from "expo-localization";
      import * as SecureStore from "expo-secure-store";

      describe("useLanguagePref", () => {
        it("returns 'EN' when expo-localization first locale is 'en'", ...);
        it("returns 'AR' when expo-localization first locale is 'ar'", ...);
        it("returns the SecureStore override when 'lang_pref' is set to 'AR'", ...);
        it("returns the SecureStore override when 'lang_pref' is set to 'EN'", ...);
        it("falls back to 'EN' when no locale and no SecureStore override", ...);
        it("setLanguagePref('AR') writes 'AR' to SecureStore under 'lang_pref'", ...);
      });

    Step 11 — Update mobile/app.json. Add (or extend) the `extra` block and the expo-notifications plugin entry. Find the existing `"plugins": [...]` array:

      a) Inside the plugins array, find the `"expo-notifications"` plugin entry (string form per current app.json). Replace the string entry with the array form to specify channel + projectId config:

         [
           "expo-notifications",
           {
             "icon": "./assets/notification-icon.png",
             "color": "#F97316",
             "defaultChannel": "darb-inbox"
           }
         ]

         (If the plugin entry is missing entirely, append it.)

      b) Append to the top-level `extra` object (or create it if absent):

         "extra": {
           "eas": {
             "projectId": "00000000-0000-0000-0000-000000000000"
           }
         }

         The all-zero UUID is a PLACEHOLDER. The real projectId comes from `eas init` run by the founder; documented in deferred-items.md. Wave 1 push-token tests use the mock value; Wave 4 (production) requires the real value.

      c) Append to the `dependencies` reminder (if a separate doc): expo-localization, i18n-js, @expo-google-fonts/tajawal, react-native-gesture-handler, react-native-reanimated. NOTE: actual npm install runs in Wave 1; this Wave only documents the projectId. Do NOT install packages here.

    Step 12 — Create .planning/phases/09-mobile-inbox-bilingual/deferred-items.md:

      # Phase 9 — Deferred Items

      Items discovered during Phase 9 planning/execution that are **out of scope**
      for the current waves but require founder action OR are scoped to a later phase.

      ---

      ## Founder-gated (no engineering blocker — paperwork only)

      ### 1. Meta WhatsApp Business Account (WABA) per-tenant onboarding

      Wave 2 ships the `sendWhatsAppCloud` adapter and the 12 template definitions
      (6 templates × 2 languages). Sending requires:
      - A Meta Business Manager account with Business Verification complete
      - A WABA registered to that business
      - A phone number registered to that WABA (Meta-issued, NOT a personal number)
      - The 12 utility templates submitted for approval (≈24h approval per Meta)

      Estimated time: 3-5 business days for Business Verification on first attempt,
      then ~24h per template approval. Total Wave 2 → production delay: 1-2 weeks
      assuming clean verification.

      Action items (user / founder):
      - [ ] Create Meta Business Manager account for the design-partner-1 entity
      - [ ] Submit Business Verification (passport / commercial registration)
      - [ ] Register WABA + phone number
      - [ ] Capture `whatsappBusinessAccountId`, `whatsappPhoneNumberId`,
            `whatsappAccessToken` per tenant; store in `Tenant.settings.whatsapp`
            (Phase 9 Wave 3 migration sets the columns)
      - [ ] Submit the 12 templates from `backend/src/agent/templates/whatsappTemplates.ts`

      ### 2. Unifonic Kuwait sender-ID registration (15 working days)

      Wave 3 ships `sendSmsUnifonic`. Unifonic local-route SMS requires:
      - Kuwait Trade License of the tenant
      - NOC letter on tenant's company letterhead, signed and stamped
      - Unifonic account + sender-ID registration (~15 working days)

      **START THE PAPERWORK AT WAVE 1.** SMS code ships in Wave 3, but if paperwork
      starts at Wave 3 then SMS-live is 3+ weeks after WhatsApp-live. Phase 5's
      onboarding wizard should surface a "Start Unifonic NOC" task at design-partner
      onboarding.

      Twilio international fallback covers the gap at $0.3164/SMS until Unifonic
      sender-ID is live.

      Action items (user / founder):
      - [ ] Generate NOC letter template for tenants
      - [ ] Add to Phase 2 admin onboarding wizard: "SMS sender-ID registration started"
            checkbox + uploaded NOC PDF storage
      - [ ] Track Unifonic application status per tenant on `Tenant.settings.unifonic`

      ### 3. Khaleeji string copy review with design partner

      Wave 1 ships the 6 template Arabic bodies using the strings in 09-RESEARCH.md
      §"WhatsApp Template Registry". The strings are research-quality Khaleeji
      Arabic but have NOT been reviewed by a native Kuwaiti Arabic speaker.

      Action items (user / founder):
      - [ ] Schedule a 30-min review with design-partner-1's supervisor (native AR speaker)
      - [ ] Review all 6 EN+AR pairs in the WhatsApp template registry
      - [ ] Review the static UI strings in mobile/src/i18n/ar.json
      - [ ] Iterate any flagged phrases; resubmit to Meta if template bodies change

      If review surfaces material issues with register or tone, Wave 1 can ship
      with a feature flag (`Tenant.outboundChannels.whatsapp=false`) keeping Phase 9
      in-app-push-only until copy is approved.

      ### 4. Expo `projectId` obtainment (blocks production push)

      Wave 0 sets `mobile/app.json` extra.eas.projectId to a zero-UUID placeholder.
      Real `projectId` comes from:
      ```
      cd mobile && eas init  # creates a new project under the Expo org
      ```
      The command output prints the real UUID; commit it back to app.json.

      Action items (user / founder):
      - [ ] Run `eas init` from mobile/ during Wave 1 or before Wave 4 production deploy
      - [ ] Commit the real projectId to mobile/app.json
      - [ ] Add `EXPO_ACCESS_TOKEN` to backend Vercel env vars for server-side push
            at production volume (Wave 4 gate)

      ---

      ## Phase 9 backend/src/routes/agent.ts lint:tenant scope (deferred to refactor wave)

      Phase 5 already deferred adding agent.ts to lint:tenant scope due to 5
      pre-existing violations. Phase 9 Wave 1 extends agent.ts with 4 new routes
      (push-token, inbox, inbox/:id/read, inbox/opt-out); all follow the existing
      `device→driver→tenantId` pattern that is functionally tenant-safe but
      statically opaque to the lint rule. We do NOT add agent.ts to lint:tenant
      scope in this phase; the 5 pre-existing + ~4 new violations are tracked here
      as a single refactor item.

      Action: open `routes/agent.ts` refactor in a future Phase (recommend Phase 10
      Wave 0 since Phase 10 hides legacy pages — natural cleanup window).

    Step 13 — Run mobile tests to confirm the new RED files are loaded and fail predictably:
      cd mobile && npm test 2>&1 | tail -15

    Expected: Jest runs; the 7 new files fail with import-resolution errors against the not-yet-existing src/services/inboxCache.ts, src/services/pushService.ts, src/services/i18n.ts, src/components/inbox/InboxRow.tsx, mobile/app/(tabs)/inbox.tsx. Phase 5 tests STILL PASS (no regression).

    Self-check:
    - 7 new mobile test files exist
    - 2 new mocks exist
    - app.json has extra.eas.projectId placeholder
    - deferred-items.md exists with 4 founder-gated items + lint:tenant refactor entry
    - mobile test count = phase-5-count + 7 RED files
  </action>

  <verify>
    <automated>cd /Users/mac/Documents/Darb/mobile && npm test 2>&1 | grep -E "Tests:.*failed|Tests:.*passed|Test Suites" | head -3</automated>
  </verify>

  <done>
    - 7 mobile RED test files exist under mobile/__tests__/inbox/
    - mobile/__tests__/mocks/expo-notifications.ts + expo-localization.ts exist
    - mobile/__tests__/setup.ts wires the new mocks
    - mobile/app.json has extra.eas.projectId placeholder + expo-notifications plugin config
    - .planning/phases/09-mobile-inbox-bilingual/deferred-items.md exists with 4 founder-gated items
    - Phase 5 mobile tests still pass (no regression)
    - The 7 new RED files fail with expected import-resolution errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file authoring (this Wave) | No new runtime trust boundaries — this is RED scaffolding only |
| Future: Mobile HTTPS → /api/agent/push-token | (Wave 1) Untrusted Expo push token enters the backend |
| Future: Mobile HTTPS → /api/agent/inbox | (Wave 1) Device-auth scoped notification read; Pitfall 9 (cross-tenant leak) is the highest-impact threat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-00-01 | Information Disclosure | Wave 0 RED test fixtures leak real driver names / phone numbers if copied from prod | mitigate | All test fixtures use synthetic data (tenant-1/driver-1/+96599999999); no production PII enters the test scaffolding. Wave 0 contains NO fixtures from a real tenant. |
| T-09-00-02 | Tampering | Prisma schema columns added but migration not run — Wave 1 could regress and rely on a non-existent column at runtime | mitigate | The Wave 0 schema additions are additive Json/enum/scalar fields that compile-time-typecheck against the generated client. Wave 1 implementation runs `npx prisma generate` after pulling; any drift surfaces immediately as a type error. Wave 3 runs the migration before any code path requires the new columns to exist in the DB. |
| T-09-00-03 | Information Disclosure (forward-looking) | Pitfall 9 — `metadata.driverId` vs `userId` query mismatch leaks owner inbox to courier device OR returns empty inbox forever | mitigate | The RED test `agent/inboxRoutes.test.ts` asserts the device-auth route filters on `metadata->>'driverId' = device.driverId` AND `tenantId = device.tenantId`. Wave 1 implementation MUST make these tests pass; if it accidentally re-uses the owner-auth `userId` filter, the cross-tenant test fails. |
| T-09-00-04 | Spoofing (forward-looking) | Tampered Expo push token enters the system | mitigate (asserted by Wave 0 test) | RED test `agent/pushTokenRegister.test.ts` asserts rejection of malformed tokens (must start with `ExponentPushToken[`). Wave 1 implementation must Zod-validate. |
</threat_model>

<verification>
- `cd backend && npx prisma format` exits 0
- `cd backend && npx prisma generate` exits 0 and regenerates the client with `Language` enum + new fields
- `cd backend && npm test` runs 9 NEW failing test files; pre-existing test count unchanged
- `cd backend && npm run lint:tenant` exits 0 (no new no-prisma-without-tenant violations on test files)
- `cd mobile && npm test` runs 7 NEW failing test files; Phase 5 test count unchanged
- `.planning/phases/09-mobile-inbox-bilingual/deferred-items.md` exists with 4 founder-gated items
- `mobile/app.json` has `extra.eas.projectId` placeholder and `expo-notifications` plugin config
</verification>

<success_criteria>
- 9 backend + 7 mobile RED test files exist with at least 5 `it` blocks each
- Prisma schema parses with Language enum + 5 new Tenant columns + 4 new Driver columns
- backend lint:tenant glob extends to all Phase 9 backend file targets
- mobile mocks for expo-notifications + expo-localization exist and reset between tests
- deferred-items.md scaffolded with founder-gated items list
- No regression on Phases 1-6 backend tests OR Phase 5 mobile tests
- Wave 1 can begin: types compile, mocks exist, tests describe behaviors
</success_criteria>

<output>
After completion, create `.planning/phases/09-mobile-inbox-bilingual/09-00-SUMMARY.md` with:
- Wave 0 deliverables list
- The schema-but-no-migration deferral pattern (Wave 3 runs prisma migrate dev)
- The Driver.expoPushToken design choice (RESEARCH said Device.expoPushToken; we put it on Driver because Device.driverId is @@unique and outboundResolver queries are per-driver)
- The Pitfall 9 fix asserted at the test layer (metadata.driverId, not userId)
- The 4 founder-gated items from deferred-items.md surfaced for visibility
- Confirmation that agent.ts is intentionally NOT in lint:tenant scope (Phase 5 deferral honoured)
</output>
