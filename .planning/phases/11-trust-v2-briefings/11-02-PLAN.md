---
phase: 11-trust-v2-briefings
plan: 02
type: execute
wave: 2
depends_on: [00]
files_modified:
  - backend/src/agent/trustGraduation.ts
  - backend/src/agent/registry.ts
  - backend/src/agent/ledger.ts
  - backend/src/agent/memoryCompactor.ts
  - backend/src/queues/memoryPruneWorker.ts
  - backend/src/routes/admin/trust.ts
  - backend/src/server.ts
  - backend/src/__tests__/agent/trustGraduation.test.ts
  - backend/src/__tests__/agent/memoryCompactor.test.ts
  - backend/src/__tests__/queues/memoryPruneWorker.test.ts
  - backend/prisma/seed.ts
autonomous: true
requirements:
  - REQ-agent-trust-graduation
  - REQ-agent-long-term-memory

must_haves:
  truths:
    - "Tools allow-listed for auto-approve (draftCourierMessage with intent=WARN_GPS_STALE/WARN_LATE_CLOCKIN + flagForReview) execute without owner click when tenant.trustLevel >= 2 AND confidence > 0.8 AND rollbacks30d = 0 AND withinRateCap"
    - "Tools NOT allow-listed (applyPenalty + suspendDriver remain in v1 mode forever) ALWAYS fall through to PendingAgentAction even at trustLevel=3"
    - "Per-tool 60-day demotion window: a rollback on tool X demotes ONLY tool X for that tenant (Pitfall 6)"
    - "Auto-approved actions write AgentAction.source='auto' + approverId=SYSTEM_AUTO_APPROVE_USER_ID; visible in /decisions inbox with 'auto' badge pre-marked APPROVED (D-02)"
    - "Trust gate fast-path sub-1ms branch: when tool.allowAutoApprove is false, evaluateTrustGate returns autoApprove:false without any DB call"
    - "Memory pruner runs daily 03:00 Asia/Kuwait, hard-deletes AgentMemory rows older than 90d EXCEPT source IN (founder_pinned, user_correction) AND key NOT LIKE 'dismissed:%' AND value.protected != true"
    - "memoryCompactor compresses remaining old observations into source='observation_compressed' synthetic rows (NOT deletion — preserves append-only contract)"
    - "Admin endpoint POST /api/admin/tenants/:id/trust allows founder to set trustLevel + autoApproveCapDaily; gated by superAdmin middleware"
    - "System user system:auto-approve seeded in prisma/seed.ts for AgentAction.approverId FK"
  artifacts:
    - path: "backend/src/agent/trustGraduation.ts"
      provides: "ToolTrustConfig registry + evaluateTrustGate + SYSTEM_AUTO_APPROVE_USER_ID"
      min_lines: 150
    - path: "backend/src/agent/registry.ts"
      provides: "Modified invoke() with trust gate insertion (registry.ts:148-167)"
      contains: "evaluateTrustGate"
    - path: "backend/src/agent/ledger.ts"
      provides: "writeAutoApprovedAction wrapper"
      contains: "writeAutoApprovedAction"
    - path: "backend/src/agent/memoryCompactor.ts"
      provides: "compactMemoriesForTenant — compress old observation rows"
      min_lines: 80
    - path: "backend/src/queues/memoryPruneWorker.ts"
      provides: "BullMQ daily worker — delete >90d + call compactor + MetricEvent"
      min_lines: 100
    - path: "backend/src/routes/admin/trust.ts"
      provides: "POST /api/admin/tenants/:id/trust + GET /api/admin/tenants/:id/trust"
      min_lines: 60
    - path: "backend/prisma/seed.ts"
      provides: "Seed system:auto-approve User row"
      contains: "system:auto-approve"
  key_links:
    - from: "agent/registry.ts invoke()"
      to: "agent/trustGraduation.ts evaluateTrustGate"
      via: "approval-gate fall-through insertion"
      pattern: "evaluateTrustGate"
    - from: "agent/registry.ts trust-passed branch"
      to: "agent/ledger.ts writeAutoApprovedAction"
      via: "synthetic ctx.userId = SYSTEM_AUTO_APPROVE_USER_ID"
      pattern: "writeAutoApprovedAction"
    - from: "queues/memoryPruneWorker.ts processPruneTick"
      to: "agent/memoryCompactor.ts compactMemoriesForTenant"
      via: "post-delete compaction call"
      pattern: "compactMemoriesForTenant"
    - from: "routes/admin/trust.ts POST handler"
      to: "Tenant.trustLevel column write"
      via: "Prisma update + requireSuperAdmin middleware"
      pattern: "prisma\\.tenant\\.update.*trustLevel"
---

<objective>
Wave 2 — Trust graduation engine + memory compaction + admin trust route.

Purpose: Insert the v2 trust gate into agent/registry.ts:148-167 so allow-listed tools execute without owner click when tenant trust + confidence + rollback history + rate-cap conditions are met. Ship the per-tool ToolTrustConfig registry (draftCourierMessage + flagForReview opt-in; applyPenalty + suspendDriver remain manual forever). Ship the AgentMemory daily pruner + compactor so the long-term store stays useful instead of bloated. Ship the founder-only POST /api/admin/tenants/:id/trust endpoint for manual trust-level bumps (design partners → trustLevel=2 immediately per D-01).

Output: trustGraduation.ts (config registry + evaluation function + system user constant), modified registry.ts invoke() with trust-check insertion, ledger.ts writeAutoApprovedAction wrapper, memoryCompactor.ts (compaction helpers), memoryPruneWorker.ts (BullMQ daily worker), routes/admin/trust.ts (super-admin only), server.ts route mount + worker boot, seed.ts system user.

Per RESEARCH Pattern 3 (trust gate insertion at registry.ts:148-167) + Pattern 4 (Redis sliding-window-log rate cap) + Pattern 5 (append-only compaction) + Pitfall 2 (confidence drift) + Pitfall 3 (founder-pinned exemption) + Pitfall 6 (per-tool 60-day demotion) + Pitfall 8 (audit row after execute) + D-01 (default trustLevel=0) + D-02 (auto badge in /decisions inbox).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-trust-v2-briefings/11-RESEARCH.md
@.planning/phases/11-trust-v2-briefings/11-00-SUMMARY.md
@backend/src/agent/registry.ts
@backend/src/agent/ledger.ts
@backend/src/agent/memory.ts
@backend/src/middleware/superAdmin.ts
@backend/src/queues/scheduledBriefingsWorker.ts

<interfaces>
Existing registry.ts ToolDefinition (from imports):
  interface ToolDefinition {
    name: string;
    description: string;
    inputValidator: z.ZodSchema;
    requiredRole: string[];
    allowedAgents: string[];
    requiresApproval: boolean;
    execute: (ctx: ToolContext, input: unknown) => Promise of unknown;
  }
Phase 11 extends ToolDefinition with 3 optional fields:
  allowAutoApprove?: boolean;                  // default false (opt-in)
  minTrustLevel?: number;                      // default 2 (only allow at trustLevel >= 2)
  autoApproveBelowConfidence?: number;         // default 0.85 (DENY auto-approve if confidence < this; D-02 conservative)

Existing registry.ts invoke() flow (lines 120-200):
  1. lookup tool, RBAC, allowedAgents, validate input
  2. Approval gate (line 148): if (tool.requiresApproval && !ctx.userId) → PendingAgentAction.create + return pending
  3. Else: execute + AgentToolCall.create

Phase 11 inserts between (2) and the existing "stage PendingAgentAction" body:
  evaluateTrustGate({ tenantId, toolName, confidence, subjectType, subjectId, intent? }) → { autoApprove: bool, reasoning: string }
  if (autoApprove) → set ctx.userId = SYSTEM_AUTO_APPROVE_USER_ID, fall through to execute, write AgentAction.source='auto'
  if (!autoApprove) → existing PendingAgentAction.create path

Existing ledger.ts writeAgentAction signature (verified):
  writeAgentAction(row: AuditRow) → { id }
  AuditRow already supports source?: "decisions" | "chat" | "briefing" | "auto"
  AuditRow.approverUserId is REQUIRED (FK to User)

Phase 11 needs SYSTEM_AUTO_APPROVE_USER_ID = "system:auto-approve" seeded as a real User row (FK constraint).

Existing AgentMemory schema (verified):
  model AgentMemory { id, tenantId, key, value Json, confidence Float, source String?, agentRunId String?, createdAt DateTime }
  index: [tenantId, key, createdAt(sort: Desc)]

Existing superAdmin middleware (backend/src/middleware/superAdmin.ts):
  requireSuperAdmin(req, res, next) — checks DB User.isSuperAdmin (Phase 2 Wave 1)

BullMQ Worker boot pattern (mirror scheduledBriefingsWorker.ts):
  - getConnection() returns IORedis or null
  - getMemoryPruneQueue() returns Queue or null
  - startMemoryPruneWorker() bound from server.ts side-effect import
  - upsertJobScheduler with cron "0 3 * * *" tz "Asia/Kuwait"

Existing Anthropic graceful-degrade pattern: every new worker no-ops without error when REDIS_URL is unset (matches Phase 4+5+6).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 2.1: trustGraduation.ts — ToolTrustConfig registry + evaluateTrustGate + system user seed</name>
  <files>
    backend/src/agent/trustGraduation.ts,
    backend/src/agent/ledger.ts,
    backend/prisma/seed.ts,
    backend/src/__tests__/agent/trustGraduation.test.ts
  </files>
  <behavior>
    - trustGraduation.ts exports:
      * SYSTEM_AUTO_APPROVE_USER_ID = "system:auto-approve"
      * SYSTEM_AUTO_APPROVE_EMAIL = "system+auto-approve@darb.internal"
      * TOOL_TRUST_CONFIG: Record<string, ToolTrustConfig> — per-tool opt-in config
      * evaluateTrustGate(opts): Promise<{ autoApprove: boolean, reasoning: string, decisionMetric: object }>
    - ToolTrustConfig type: { allowAutoApprove: boolean; minTrustLevel: number; autoApproveBelowConfidence: number; intents?: string[]; demotionWindowDays?: number }
    - TOOL_TRUST_CONFIG seeds:
      * "draftCourierMessage": { allowAutoApprove: true, minTrustLevel: 2, autoApproveBelowConfidence: 0.85, intents: ["WARN_GPS_STALE", "WARN_LATE_CLOCKIN"], demotionWindowDays: 60 }
      * "flagForReview": { allowAutoApprove: true, minTrustLevel: 2, autoApproveBelowConfidence: 0.85, demotionWindowDays: 60 }
      * "applyPenalty": { allowAutoApprove: false, ... }       // explicit deny — financial impact
      * "suspendDriver": { allowAutoApprove: false, ... }      // explicit deny — HR impact
      * all other tools default to allowAutoApprove: false when unlisted
    - evaluateTrustGate logic:
      1. Fast-path: lookup TOOL_TRUST_CONFIG[toolName]. If missing or allowAutoApprove=false, return { autoApprove: false, reasoning: "tool not allow-listed", decisionMetric: {} } in sub-1ms (no DB call).
      2. Intent filter: if config.intents is set AND opts.intent not in config.intents → autoApprove:false reasoning "intent not allow-listed"
      3. DB lookups (Promise.all for latency): tenant.trustLevel + tenant.autoApproveCapDaily (default 50 if column missing — Wave 4 might add this column or we use 50 hardcoded for now) + rollbacks30d count via prisma.agentAction.count where tenantId+toolName+rolledBackAt not null + gte (now-30d) + per-tool 60-day demotion: count rollbacks for THIS tool in last config.demotionWindowDays
      4. Checks (all must pass):
         - tenant.trustLevel >= config.minTrustLevel
         - opts.confidence >= config.autoApproveBelowConfidence (per Pitfall 2: this is a soft gate; observability via decisionMetric)
         - rollbacks30d === 0 (general)
         - perToolRollbacks60d === 0 (Pitfall 6 — per-tool demotion)
         - withinRateCap(tenantId, cap=50): Redis sorted-set sliding-window-log (Pattern 4) — ZADD now + EXPIRE 90_000 + ZCARD must be ≤ cap. Use commit-then-verify (Security V2).
      5. Return { autoApprove, reasoning, decisionMetric: { trustLevel, confidence, rollbacks30d, perToolRollbacks60d, rateCount, cap } }
      6. Record MetricEvent (event: "trust_gate_decision", properties: { toolName, autoApprove, ...decisionMetric }) — async, don't await (fire-and-forget) so the hot path stays fast.
    - ledger.ts gets a writeAutoApprovedAction wrapper:
      writeAutoApprovedAction(opts) → calls writeAgentAction with approverUserId: SYSTEM_AUTO_APPROVE_USER_ID + source: "auto"
    - seed.ts adds idempotent upsert of system:auto-approve User row (so the FK in AgentAction.approverId resolves).
    - All Wave 0 trustGraduation.test.ts cases turn GREEN. Add a 5th test: fast-path returns in <5ms for allowAutoApprove=false tool (perf marker — not a real benchmark but a sanity check).
  </behavior>
  <action>
    1. Create backend/src/agent/trustGraduation.ts:
       - Imports: prisma from ../config, getConnection from ../queues/scheduledBriefingsWorker (to reuse IORedis singleton OR create new), recordMetricEvent from ./metricEvent, crypto from node.
       - Export constants SYSTEM_AUTO_APPROVE_USER_ID + SYSTEM_AUTO_APPROVE_EMAIL + TOOL_TRUST_CONFIG (typed).
       - Export interface ToolTrustConfig + EvaluateTrustGateOpts + EvaluateTrustGateResult.
       - Implement evaluateTrustGate as documented in behavior. The Redis call MUST tolerate REDIS_URL absent (return rateCount=0 + skipRateCap=true; log a warning so the test env doesn't need Redis).
       - Inline doc comment at top references RESEARCH Pattern 3 + Pitfalls 2/6/8.
       - File ≥150 lines including type defs + jsdoc.

    2. Edit backend/src/agent/ledger.ts — append a writeAutoApprovedAction export (~15 lines):
       ```typescript
       import { SYSTEM_AUTO_APPROVE_USER_ID } from "./trustGraduation";
       export async function writeAutoApprovedAction(opts: Omit<AuditRow, "approverUserId" | "source">): Promise<{ id: string }> {
         return writeAgentAction({ ...opts, approverUserId: SYSTEM_AUTO_APPROVE_USER_ID, source: "auto" });
       }
       ```

    3. Edit backend/prisma/seed.ts — add idempotent system user seed BEFORE any FK-dependent inserts:
       ```typescript
       await prisma.user.upsert({
         where: { id: "system:auto-approve" },
         update: {},
         create: {
           id: "system:auto-approve",
           email: "system+auto-approve@darb.internal",
           passwordHash: "<unhashable-placeholder>",  // never used for login
           role: "ADMIN",
           tenantId: <first-tenant-or-system-tenant-id>,  // adjust based on existing seed.ts shape
           isActive: false,  // can't log in; just FK target
         },
       });
       ```
       Read the existing seed.ts first to determine the right tenant FK + which User fields are required. Wrap in try/catch with logging.

    4. Update backend/src/__tests__/agent/trustGraduation.test.ts (from Wave 0):
       - Replace all `expect(true).toBe(true)` placeholders with real assertions.
       - Tighten "happy path" test: seed tenant.trustLevel=2 via raw SQL fixture, call evaluateTrustGate with toolName="draftCourierMessage", intent="WARN_GPS_STALE", confidence=0.92 — assert autoApprove=true.
       - Tighten "non-auto-approvable" test: seed tenant.trustLevel=3, call with toolName="applyPenalty" — assert autoApprove=false.
       - Tighten "rate-limit cap" test: pre-fill the Redis sorted-set with 50 entries; call evaluateTrustGate; assert autoApprove=false reasoning matches /rate/i.
       - Tighten "per-tool 60d demotion" test: seed two AgentAction rows for tool draftCourierMessage with rolledBackAt 30 days ago; call evaluateTrustGate for draftCourierMessage (expect autoApprove=false) AND for flagForReview (expect autoApprove=true — different tool, no demotion).
       - Add 5th test: tool not in config returns within Date.now() delta < 5ms — sanity for fast-path.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npx jest src/__tests__/agent/trustGraduation.test.ts 2>&amp;1 | tail -20 &amp;&amp; grep -c "evaluateTrustGate\|SYSTEM_AUTO_APPROVE" src/agent/trustGraduation.ts | grep -v '^#' &amp;&amp; grep -c "writeAutoApprovedAction" src/agent/ledger.ts | grep -v '^#'</automated>
  </verify>
  <done>
    trustGraduation.ts exists with ≥150 lines + exports SYSTEM_AUTO_APPROVE_USER_ID + TOOL_TRUST_CONFIG + evaluateTrustGate;
    ledger.ts has writeAutoApprovedAction wrapper;
    seed.ts upserts system:auto-approve User idempotently;
    All 5 trustGraduation.test.ts cases PASS;
    Fast-path test confirms sub-5ms branch for allowAutoApprove=false tools.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.2: Wire trust gate into agent/registry.ts invoke() + memory compactor + prune worker</name>
  <files>
    backend/src/agent/registry.ts,
    backend/src/agent/memoryCompactor.ts,
    backend/src/queues/memoryPruneWorker.ts,
    backend/src/server.ts,
    backend/src/__tests__/agent/memoryCompactor.test.ts,
    backend/src/__tests__/queues/memoryPruneWorker.test.ts
  </files>
  <behavior>
    - registry.ts ToolDefinition gains 3 optional fields: allowAutoApprove, minTrustLevel, autoApproveBelowConfidence (passed through to TOOL_TRUST_CONFIG if engineer prefers per-tool registration — but Phase 11 keeps the central TOOL_TRUST_CONFIG dict as source of truth; the ToolDefinition fields are reserved for Phase 12 standing-rules wiring).
    - registry.ts invoke() at line 148 (approval gate) gets Phase 11 insertion:
      Before staging PendingAgentAction, call evaluateTrustGate. If autoApprove=true:
        - mint ctx2 = { ...ctx, userId: SYSTEM_AUTO_APPROVE_USER_ID }
        - fall through to execute branch using ctx2
        - In the success branch, AFTER execute returns successfully, call writeAutoApprovedAction with originalProposal=input + reasoning=trustDecision.reasoning + outcome="success" (Pitfall 8 — audit AFTER execute, not before)
        - In the failure branch (execute throws), call writeAutoApprovedAction with outcome="failure" + errorMessage
      If autoApprove=false: continue with existing PendingAgentAction.create flow.
    - Per D-02: auto-approved actions surface in /decisions inbox pre-marked APPROVED with "auto" badge. NOTE: AgentAction.source="auto" rows are NOT PendingAgentAction rows — they show up in /decisions audit log (already exists per Phase 2). The Phase 2 /decisions audit projector needs no changes (rows render in audit; pending inbox unchanged) — this is correct per Open Question 2 recommendation in RESEARCH.

    - memoryCompactor.ts exports compactMemoriesForTenant(tenantId) → { keysCompacted, rowsCompressed }:
      1. For each (tenantId, key) group with > 20 rows older than 30 days:
         - keep latest 20 verbatim
         - older rows: compress into one synthetic row with source='observation_compressed', value={ compressedFrom: N, summary: { firstAt, lastAt, sourcesCounts, keyExamples } }, confidence: 0.3
         - delete original old rows in a transaction with the create
      2. NEVER touch rows where source IN ('founder_pinned', 'user_correction') OR key LIKE 'dismissed:%' OR value.protected = true (the value JSON contains a protected:true flag for founder context — extend with this 3rd exemption).
      3. Return counts.

    - memoryPruneWorker.ts:
      - BullMQ Queue MEMORY_PRUNE_QUEUE = "memory-prune"
      - getMemoryPruneQueue() / startMemoryPruneWorker() singletons (mirror scheduledBriefingsWorker.ts pattern)
      - cron "0 3 * * *" tz "Asia/Kuwait" via upsertJobScheduler with id "mp:daily"
      - processPruneTick():
        1. For each tenant: prisma.agentMemory.deleteMany where createdAt < now-90d AND source NOT IN ('founder_pinned', 'user_correction') AND NOT(key startsWith 'dismissed:') AND NOT(value.protected = true — use Prisma JSON path filter prisma.$queryRaw or fallback to in-memory filter for portability). For safety: batch with LIMIT 10000 (Security DoS T-W2-memory-DoS).
        2. Call compactMemoriesForTenant(tenantId)
        3. recordMetricEvent event="memory_pruned" properties { rowsDeleted, keysCompacted, rowsCompressed, cutoff }
      - Return { tenantsProcessed, rowsDeleted } (matches Wave 0 RED test shape).

    - server.ts side-effect import: add `import { startMemoryPruneWorker } from "./queues/memoryPruneWorker"; startMemoryPruneWorker();` after existing scheduledBriefingsWorker bootstrap.

    - Wave 0 memoryCompactor.test.ts + memoryPruneWorker.test.ts cases turn GREEN.
  </behavior>
  <action>
    1. Edit backend/src/agent/registry.ts:
       - Add import: `import { evaluateTrustGate, SYSTEM_AUTO_APPROVE_USER_ID } from "./trustGraduation"; import { writeAutoApprovedAction } from "./ledger";`
       - In invoke(), locate the existing approval gate at line ~148:
         ```typescript
         if (tool.requiresApproval && !ctx.userId) {
           const pending = await prisma.pendingAgentAction.create({ ... });
           return { status: "pending_approval", pendingActionId: pending.id };
         }
         ```
       - Replace with:
         ```typescript
         if (tool.requiresApproval && !ctx.userId) {
           // Phase 11 — trust gate insertion. RESEARCH Pattern 3 + Pitfall 8.
           const trustDecision = await evaluateTrustGate({
             tenantId: ctx.tenantId,
             toolName: name,
             confidence: opts.confidence ?? 0,
             subjectType: opts.subjectType,
             subjectId: opts.subjectId,
             intent: (input as any)?.intent,
           });

           if (trustDecision.autoApprove) {
             // Mint synthetic user; fall through to execute below. AUDIT after execute (Pitfall 8).
             ctx = { ...ctx, userId: SYSTEM_AUTO_APPROVE_USER_ID };
             const startedAt = Date.now();
             try {
               const output = await tool.execute(ctx, input);
               // Audit on success
               await writeAutoApprovedAction({
                 tenantId: ctx.tenantId,
                 toolName: name,
                 originalProposal: input,
                 outcome: "success",
                 reasoning: trustDecision.reasoning,
                 agentRunId: ctx.runId,
                 subjectType: opts.subjectType,
                 subjectId: opts.subjectId,
                 latencyMs: Date.now() - startedAt,
               });
               await prisma.agentToolCall.create({
                 data: { runId: ctx.runId, toolName: name, input: input as any, output: (output ?? null) as any, durationMs: Date.now() - startedAt, approvedBy: ctx.userId },
               });
               return { status: "executed", output };
             } catch (err) {
               const message = err instanceof Error ? err.message : String(err);
               await writeAutoApprovedAction({
                 tenantId: ctx.tenantId,
                 toolName: name,
                 originalProposal: input,
                 outcome: "failure",
                 reasoning: trustDecision.reasoning,
                 errorMessage: message,
                 agentRunId: ctx.runId,
                 latencyMs: Date.now() - startedAt,
               });
               await prisma.agentToolCall.create({
                 data: { runId: ctx.runId, toolName: name, input: input as any, error: message, durationMs: Date.now() - startedAt, approvedBy: ctx.userId },
               });
               return { status: "error", error: message };
             }
           }

           // Trust gate denied → existing v1 PendingAgentAction path
           const pending = await prisma.pendingAgentAction.create({
             data: {
               tenantId: ctx.tenantId,
               runId: ctx.runId,
               agentId: ctx.agentId,
               toolName: name,
               input: input as any,
               recommendation: opts.recommendation ?? "approve",
               reasoning: opts.reasoning ?? "",
               confidence: opts.confidence ?? 0,
               priorityScore: opts.priorityScore ?? 0,
               subjectType: opts.subjectType,
               subjectId: opts.subjectId,
             },
           });
           return { status: "pending_approval", pendingActionId: pending.id };
         }
         ```
       - Keep existing "Execute + audit" branch (post-gate) unchanged — that branch handles ctx.userId already set (human approvals via /decisions).

    2. Create backend/src/agent/memoryCompactor.ts (≥80 lines):
       - Export compactMemoriesForTenant(tenantId) per behavior spec.
       - Helper summarize(rows) returns a JSON object: { firstAt: ISO, lastAt: ISO, count: N, keyExamples: rows.slice(0,3).map(r => r.value), sourcesCounts: tally of source values }.
       - Use Prisma transaction (prisma.$transaction) for the create-then-delete atomicity.
       - For the protected:true JSON filter: read into memory + filter client-side (Prisma JSON path filtering varies by PG version; in-memory is safe for typical 90d row volumes). Add inline comment noting this tradeoff.

    3. Create backend/src/queues/memoryPruneWorker.ts (≥100 lines):
       - Mirror scheduledBriefingsWorker.ts structure (queue singleton + Redis guard + bind/unbind + Worker boot).
       - Export MEMORY_PRUNE_QUEUE = "memory-prune".
       - processPruneTick() per behavior spec. Batch deletes with LIMIT 10000 per call (loop until count returned < 10000) to avoid long-tx locks.
       - Tick data: { tickIso: ISO } — no per-tenant params, the worker iterates all tenants.
       - startMemoryPruneWorker(): boot Worker if Redis available; on cold start ALSO upsertJobScheduler "mp:daily" with pattern "0 3 * * *" tz "Asia/Kuwait".

    4. Edit backend/src/server.ts:
       - Add `import { startMemoryPruneWorker } from "./queues/memoryPruneWorker";` near other queue imports.
       - In the worker bootstrap section (after startScheduledBriefingsWorker()), add `startMemoryPruneWorker();`

    5. Update Wave 0 tests:
       - memoryCompactor.test.ts: seed 30 AgentMemory rows for tenant+key, run compactMemoriesForTenant, assert (a) 1 row with source='observation_compressed' exists, (b) original count went 30→11 (1 compressed + top-20 kept). For founder_pinned/dismissed/protected tests, seed exempt rows and assert they're untouched. For latestMemoryByKey semantics: seed a verbatim row newer than the compressed row, verify latest query (orderBy createdAt desc) returns the verbatim row.
       - memoryPruneWorker.test.ts: seed rows >90d old + rows <90d old, call processPruneTick(), assert deleteMany count matches >90d count minus exempt rows, assert MetricEvent("memory_pruned") was recorded (mock recordMetricEvent or query metricEvent table).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npx jest src/__tests__/agent/memoryCompactor.test.ts src/__tests__/queues/memoryPruneWorker.test.ts 2>&amp;1 | tail -20 &amp;&amp; grep -c "evaluateTrustGate\|writeAutoApprovedAction\|SYSTEM_AUTO_APPROVE_USER_ID" src/agent/registry.ts | grep -v '^#' &amp;&amp; grep -c "startMemoryPruneWorker" src/server.ts | grep -v '^#'</automated>
  </verify>
  <done>
    registry.ts invoke() trust-gate insertion compiles + tests pass;
    memoryCompactor.ts + memoryPruneWorker.ts both exist;
    server.ts boots memoryPruneWorker;
    Wave 0 memoryCompactor.test.ts cases turn GREEN with real assertions (not placeholders);
    Wave 0 memoryPruneWorker.test.ts cases turn GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.3: Admin trust route — POST /api/admin/tenants/:id/trust + GET</name>
  <files>
    backend/src/routes/admin/trust.ts,
    backend/src/server.ts
  </files>
  <behavior>
    - GET /api/admin/tenants/:id/trust → { tenantId, trustLevel, autoApproveCapDaily, rollbacks30d, recentAutoActions: AgentAction[] } — read-only view for founder dashboard.
    - POST /api/admin/tenants/:id/trust body { trustLevel?: 0|1|2|3, autoApproveCapDaily?: number 0..200, briefingEmailRecipients?: string[] } → updates Tenant columns + writes AgentAction.toolName="adminUpdateTrust" with source="decisions" + approverId=req.user.userId for full audit trail.
    - Both routes use authMiddleware + requireSuperAdmin (NO tenantScope — admin routes are tenant-agnostic, tenantId is in :id param per superAdmin.ts doc comment).
    - Zod input validator clamps trustLevel ∈ {0,1,2,3} and autoApproveCapDaily ∈ [0, 200] (Security V5).
    - briefingEmailRecipients: array of email strings; max 10 per tenant; each validated with z.string().email().
    - Mount in server.ts: `app.use("/api/admin/tenants", adminTrustRouter);`
    - Error handling: try/catch returning { error: message } per CLAUDE.md.
  </behavior>
  <action>
    1. Create backend/src/routes/admin/trust.ts (≥60 lines):
       - import express, prisma, requireSuperAdmin, authMiddleware, z from zod, writeAgentAction.
       - Router with two handlers per behavior spec.
       - Zod schema:
         ```typescript
         const updateTrustSchema = z.object({
           trustLevel: z.number().int().min(0).max(3).optional(),
           autoApproveCapDaily: z.number().int().min(0).max(200).optional(),
           briefingEmailRecipients: z.array(z.string().email()).max(10).optional(),
         });
         ```
       - GET handler: prisma.tenant.findUnique by params.id, agentAction.count by tenantId + rolledBackAt + last 30d, agentAction.findMany by tenantId + source='auto' + orderBy createdAt desc limit 20.
       - POST handler: tenant.update with parsed payload + writeAgentAction with toolName="adminUpdateTrust" + originalProposal=payload + reasoning="Admin trust update by " + req.user.email + outcome="success" + source="decisions" + approverId=req.user.userId.
       - 404 if tenant not found.

    2. Edit backend/src/server.ts:
       - Add `import adminTrustRouter from "./routes/admin/trust";`
       - Mount: `app.use("/api/admin/tenants", authMiddleware, requireSuperAdmin, adminTrustRouter);`
         (Note: authMiddleware + requireSuperAdmin applied at mount level so each handler in the router file inherits; this matches existing Phase 2 /api/admin/onboarding pattern.)

    3. lint:tenant must pass for the new admin/trust.ts. Since it doesn't directly query tenant-scoped models (it queries by params.id which IS the tenantId), the requireSuperAdmin assertion provides the security boundary. Add `// eslint-disable-next-line no-prisma-without-tenant -- admin route: tenantId comes from URL param + requireSuperAdmin enforces super-admin scope per superAdmin.ts doc comment` above the prisma.tenant.findUnique/update + the prisma.agentAction queries.

    4. Add a smoke test in trustGraduation.test.ts or a new admin/trust.test.ts:
       - POST /api/admin/tenants/:id/trust with valid body + super-admin JWT → 200, tenant.trustLevel updated, AgentAction audit row written
       - POST with non-super-admin JWT → 403
       - POST with trustLevel=99 (out of range) → 400 validation error
       (This is OPTIONAL polish — the route handler doesn't need to be GREEN-tested in Wave 2 as long as it compiles and lint:tenant passes. Phase verification in Wave 4 will exercise it end-to-end.)
  </action>
  <verify>
    <automated>cd backend &amp;&amp; [ -f src/routes/admin/trust.ts ] &amp;&amp; wc -l src/routes/admin/trust.ts | awk '{print $1}' | grep -E "^[6-9][0-9]|^[1-9][0-9][0-9]" &amp;&amp; grep -c "adminTrustRouter\|/api/admin/tenants" src/server.ts | grep -v '^#' &amp;&amp; npm run lint:tenant 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    routes/admin/trust.ts exists with ≥60 lines;
    server.ts mounts the router at /api/admin/tenants;
    npm run lint:tenant exits 0 (eslint-disable comments are present where needed);
    GET + POST handlers compile and follow CLAUDE.md error-handling convention (try/catch + { error: message }).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ToolRegistryImpl.invoke() → evaluateTrustGate | the most security-critical insertion in the entire phase; trust math runs before tool execution |
| AgentAction.approverId FK → User.id system:auto-approve | synthetic user; FK constraint enforces a real DB row exists |
| Redis sorted-set rate cap | per-tenant ZCARD atomically capped; commit-then-verify pattern (T-11-W2-rate-bypass) |
| POST /api/admin/tenants/:id/trust | super-admin only; founder-only path to bump design partner trust |
| memoryPruneWorker → prisma.agentMemory.deleteMany | tenant-iterating delete; batched LIMIT 10000 (T-11-W2-memory-DoS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-W2-01 | Elevation of Privilege | trust-gate bypass via crafted confidence value | mitigate | confidence clamped to [0,1] via Zod in tool input validators (existing per registry.ts); evaluateTrustGate also requires perToolRollbacks60d=0 derived server-side from AgentAction — not caller-controllable |
| T-11-W2-02 | Tampering | auto-approve rate-cap bypass via concurrent worker race | mitigate | Redis ZADD is atomic; ZCARD check AFTER add (commit-then-verify per Pattern 4 + Pitfall 6); rate cap entry never deleted, only TTL-expires after 25h |
| T-11-W2-03 | Repudiation | AgentAction.outcome=success when execute actually threw (Pitfall 8) | mitigate | Audit row written INSIDE try/catch — success branch writes outcome=success only AFTER execute returns; catch branch writes outcome=failure + errorMessage |
| T-11-W2-04 | Denial of Service | memoryPruneWorker hard-deletes millions of rows in one tx | mitigate | Batch with LIMIT 10000 per Prisma deleteMany call; loop until count < 10000; one tenant at a time |
| T-11-W2-05 | Information Disclosure | admin trust route leaks AgentAction history of one tenant to super-admin viewing another | mitigate | Route is super-admin gated (requireSuperAdmin); super-admin has cross-tenant authority by design; eslint-disable-next-line comment documents the boundary |
| T-11-W2-06 | Spoofing | someone gains shell access + manually writes AgentAction.source="auto" without going through trust gate | accept | DB-level integrity not enforced; superuser DB access is already game-over. Operational mitigation: audit log of admin trust changes via writeAgentAction toolName=adminUpdateTrust |
| T-11-W2-07 | Tampering | TOOL_TRUST_CONFIG accidentally adds applyPenalty to allowAutoApprove | mitigate | Done criteria explicitly require allowAutoApprove=false for applyPenalty + suspendDriver; trustGraduation.test.ts assertion locks this in |
</threat_model>

<verification>
1. cd backend && npx jest src/__tests__/agent/trustGraduation.test.ts → All 5 cases GREEN (incl. fast-path perf marker)
2. cd backend && npx jest src/__tests__/agent/memoryCompactor.test.ts → All cases GREEN (compress + 3 exemption types + latestMemoryByKey semantics)
3. cd backend && npx jest src/__tests__/queues/memoryPruneWorker.test.ts → All cases GREEN (delete + exempt + MetricEvent)
4. cd backend && grep -c "applyPenalty\|suspendDriver" src/agent/trustGraduation.ts → ≥2 (explicit deny entries present)
5. cd backend && grep -A2 "applyPenalty" src/agent/trustGraduation.ts | grep -c "allowAutoApprove: false" → ≥1 (locked in)
6. cd backend && grep -c "evaluateTrustGate\|writeAutoApprovedAction\|SYSTEM_AUTO_APPROVE_USER_ID" src/agent/registry.ts → ≥3 (trust gate wired)
7. cd backend && grep -c "MEMORY_PRUNE_QUEUE\|0 3 \* \* \*" src/queues/memoryPruneWorker.ts → ≥2 (queue name + cron)
8. cd backend && grep -c "startMemoryPruneWorker" src/server.ts → ≥1 (boot wired)
9. cd backend && grep -c "system:auto-approve" prisma/seed.ts → ≥1 (system user seed)
10. cd backend && npm run lint:tenant → exits 0 (no new violations)
</verification>

<success_criteria>
- Phase 11 trust gate inserted at registry.ts approval-gate site; non-allow-listed tools fall through to v1 path with zero behavior change
- draftCourierMessage (intent=WARN_GPS_STALE|WARN_LATE_CLOCKIN) + flagForReview can now auto-execute on trust-graduated tenants
- applyPenalty + suspendDriver provably can NEVER auto-execute (explicit deny in TOOL_TRUST_CONFIG, tested)
- Per-tool 60-day demotion isolates trust losses (Pitfall 6 honored)
- Per-tenant Redis rate cap prevents auto-approve runaways (Pattern 4)
- AgentMemory daily prune keeps long-term store useful; founder/user-correction/dismissed/protected rows safe (Pitfall 3)
- Founder can bump design-partner trustLevel via POST /api/admin/tenants/:id/trust (D-01 manual graduation path)
- Auto-approved actions visible in /decisions audit (Phase 2 surface, no changes needed) with source="auto"
- Audit row written AFTER execute, not before (Pitfall 8)
</success_criteria>

<output>
After completion, create .planning/phases/11-trust-v2-briefings/11-02-SUMMARY.md capturing:
- trustGraduation.ts file size + TOOL_TRUST_CONFIG entries (which tools allow vs deny)
- registry.ts trust-gate insertion site (line numbers before/after)
- memoryCompactor + memoryPruneWorker test pass rate
- admin/trust.ts route mount path
- system:auto-approve user FK seeded
- All Wave 0 RED tests for trust + memory turn GREEN
</output>
