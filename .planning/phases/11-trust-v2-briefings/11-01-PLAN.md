---
phase: 11-trust-v2-briefings
plan: 01
type: execute
wave: 1
depends_on: [00]
files_modified:
  - backend/src/agent/prompts/briefing-owner-morning.md
  - backend/src/agent/prompts/briefing-owner-evening.md
  - backend/src/agent/prompts/briefing-accountant.md
  - backend/src/agent/prompts/briefing-dispatcher.md
  - backend/src/agent/runtime.ts
  - backend/src/services/briefingProvisionService.ts
  - backend/src/queues/scheduledBriefingsWorker.ts
  - backend/src/routes/admin/onboarding.ts
  - backend/src/__tests__/queues/scheduledBriefings.test.ts
autonomous: true
requirements:
  - REQ-agent-scheduled-briefings

must_haves:
  truths:
    - "Owner gets a 3-paragraph morning briefing at 07:00 + jitter Kuwait, every day"
    - "Owner gets a 3-paragraph evening briefing at 18:00 + jitter Kuwait, every day — even on quiet days ('ops nominal' + tomorrow's outlook per D-04)"
    - "Briefing fan-out hits ChatThread + Notification (category=IMPORTANT) + email (if Tenant.briefingEmailRecipients is non-empty)"
    - "Anthropic system prompt is cached at TTL=1h via cache_control:ephemeral; per-tenant context lives in messages[] (Pitfall 7)"
    - "Cron stagger jitters per-tenant via hash(tenantId)%5 minutes to avoid Anthropic per-org rate-limit (Pitfall 4)"
    - "New tenants created via POST /api/admin/onboarding/tenants get briefings auto-provisioned via post-create hook in backend/src/routes/admin/onboarding.ts (B-3 fix — the real tenant.create site)"
    - "Wave 0 RED tests in scheduledBriefings.test.ts turn GREEN"
  artifacts:
    - path: "backend/src/agent/prompts/briefing-owner-morning.md"
      provides: "Cacheable system prompt for owner morning briefing (3 paragraphs: overnight alerts / today's shifts / top-3 attention)"
      min_lines: 30
    - path: "backend/src/agent/prompts/briefing-owner-evening.md"
      provides: "Owner evening prompt (today's results vs target / tomorrow's outlook / GPS gaps)"
      min_lines: 30
    - path: "backend/src/agent/prompts/briefing-accountant.md"
      provides: "Weekly accountant cash digest prompt"
      min_lines: 20
    - path: "backend/src/agent/prompts/briefing-dispatcher.md"
      provides: "Dispatcher floor pre-brief prompt"
      min_lines: 20
    - path: "backend/src/services/briefingProvisionService.ts"
      provides: "provisionDefaultBriefings(tenantId, ownerUserId, dispatcherUserId?, accountantUserId?) — auto-creates per-role rows"
      min_lines: 80
    - path: "backend/src/queues/scheduledBriefingsWorker.ts"
      provides: "Extended processTick: role-prompt loader + Notification + email fan-out + 429 retry"
      contains: "sendEmail"
    - path: "backend/src/routes/admin/onboarding.ts"
      provides: "Tenant-create handler with provisionDefaultBriefings post-hook (B-3 — real onboarding entry point)"
      contains: "provisionDefaultBriefings"
  key_links:
    - from: "scheduledBriefingsWorker.processTick"
      to: "Anthropic SDK client.messages.create with cache_control ephemeral 1h"
      via: "runAgent caching wrapper"
      pattern: "cache_control.*ttl.*1h"
    - from: "routes/admin/onboarding.ts POST handler (tenant.create site at line ~107)"
      to: "briefingProvisionService.provisionDefaultBriefings"
      via: "post-create hook after tenant + owner user are both created"
      pattern: "provisionDefaultBriefings"
    - from: "scheduledBriefingsWorker.processTick"
      to: "notificationChannels.sendEmail"
      via: "Tenant.briefingEmailRecipients fan-out"
      pattern: "sendEmail.*briefingEmailRecipients"
---

<objective>
Wave 1 — Scheduled briefings + narrator prompt enhancement + email fan-out + onboarding integration.

Purpose: Turn the Phase 4 briefings scaffolding (BullMQ JobScheduler + processTick + chat-only delivery) into a real daily ritual. Owners (and optionally dispatchers + accountants) get role-specific bilingual narratives every morning + evening, delivered via in-chat thread + Notification (category=IMPORTANT) + email. The system prompt is Anthropic-cached at 1h TTL for ~9x cost reduction. Tenants created via the existing `POST /api/admin/onboarding/tenants` route (the ONLY tenant.create site in the codebase — confirmed by grep) get their default briefings auto-provisioned via a post-create hook (B-3 fix from plan-checker — the previous plan referenced a non-existent `src/services/onboarding/onboardingService.ts`).

Output: 4 narrator prompt files, briefingProvisionService.ts (auto-provisions per-role briefings at tenant onboarding), an extended scheduledBriefingsWorker.processTick (role-prompt loader + Notification + email fan-out + 429 retry), and the onboarding hook inside `backend/src/routes/admin/onboarding.ts` that calls provisionDefaultBriefings after tenant + owner user creation.

Per RESEARCH Pattern 1 (per-tenant cron fan-out) + Pattern 2 (Anthropic prompt caching) + Pitfall 4 (cron stagger) + Pitfall 7 (cache fragmentation) + D-04 (evening on quiet days always sends) + plan-checker B-3 (use real onboarding route).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-trust-v2-briefings/11-RESEARCH.md
@.planning/phases/11-trust-v2-briefings/11-00-SUMMARY.md
@backend/src/queues/scheduledBriefingsWorker.ts
@backend/src/services/scheduledBriefingsService.ts
@backend/src/services/notificationChannels.ts
@backend/src/agent/runtime.ts
@backend/src/agent/prompts/narrator.md
@backend/src/routes/admin/onboarding.ts

<interfaces>
Existing scheduledBriefingsWorker.ts exports (Phase 4 Wave 5):
  export const SCHEDULED_BRIEFINGS_QUEUE = "scheduled-briefings";
  export function getScheduledBriefingsQueue(): Queue or null
  export async function bindBriefing(b: BriefingRecord): Promise of void
  export async function unbindBriefing(id: string): Promise of void
  export async function processTick(data: ScheduledBriefingTickData): Promise of result
  export function startScheduledBriefingsWorker(): void
ScheduledBriefingTickData = { briefingId, tenantId, userId, prompt, type: "briefing" | "standing_rule_v3" }

Existing notificationChannels.sendEmail signature (lines 75-87):
  export const sendEmail: (email: string, subject: string, body: string) => Promise of { ok, provider, error? }
Provider fallback chain: SendGrid → Resend → Nodemailer → stub-warn. Already wired; just call it.

runAgent("chat", { tenantId, triggerEvent, userMessage, payload }) — existing entry point.
Must be extended to accept optional { systemPromptOverride: string, systemPromptCache: "ephemeral_1h" | "none" }
so the briefing worker can pass a role-specific prompt with cache_control.

@anthropic-ai/sdk 0.80.0 cache_control shape (verified against platform.claude.com):
  system: [{ type: "text", text: "...", cache_control: { type: "ephemeral", ttl: "1h" } }]
Sonnet 4.6 min-cache threshold: 2,048 tokens.

Existing tenant.create site (B-3 — verified via grep): backend/src/routes/admin/onboarding.ts:107
  const tenant = await (prisma as unknown as {
    tenant: { create: (args: unknown) => Promise<{ id: string; name: string }> };
  }).tenant.create({
    data: {
      name,
      subscriptionPlan: "TRIAL",
      designPartner: designPartner === true,
      monthlyOverrideKd: monthlyOverrideKd ?? null,
    },
  });
  // ... user.create at line 119
  // ← Wave 1 inserts provisionDefaultBriefings call HERE (after both rows exist)

There is NO backend/src/services/onboarding/onboardingService.ts. The folder contains only:
  - backend/src/services/onboarding/index.ts (3-line barrel re-export)
  - backend/src/services/onboarding/reportBuilder.ts (Phase 2 fleet report)
Do NOT attempt to edit these files for the hook. Edit backend/src/routes/admin/onboarding.ts instead.

Tenant model after Wave 0:
  trustLevel Int @default(0)
  briefingMorningTime String @default("07:00")
  briefingEveningTime String @default("18:00")
  briefingEmailRecipients Json @default("[]")
  partnerApiSecrets Json?
  autoApproveCapDaily Int? @default(50)
  toolTrustState Json? @default("{}")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.1: Write 4 narrator briefing prompts (cacheable system prompts, tenant-agnostic)</name>
  <files>
    backend/src/agent/prompts/briefing-owner-morning.md,
    backend/src/agent/prompts/briefing-owner-evening.md,
    backend/src/agent/prompts/briefing-accountant.md,
    backend/src/agent/prompts/briefing-dispatcher.md
  </files>
  <behavior>
    Four markdown system-prompt files, each large enough to exceed Sonnet 4.6 minimum cache threshold (2,048 tokens). Files are TENANT-AGNOSTIC — no tenant-name interpolation (Pitfall 7). Per-tenant context (stats, driver names, fleet size, KPIs) ALWAYS lives in messages[] at the worker layer.

    Per-prompt structure:
    - briefing-owner-morning.md: 3 paragraphs — overnight alerts / today's shifts / top 3 attention items
    - briefing-owner-evening.md: 3 paragraphs — today's results vs target / tomorrow's outlook / GPS gaps (D-04: ALWAYS send, even quiet days, body says "ops nominal")
    - briefing-accountant.md: weekly cash digest (1 paragraph)
    - briefing-dispatcher.md: floor pre-brief (1 paragraph)

    All prompts end with: "Respond as JSON with fields english and arabic" so the Anthropic call returns both languages in one response (RESEARCH Anti-Patterns: do NOT concatenate template strings).
  </behavior>
  <action>
    1. Create backend/src/agent/prompts/briefing-owner-morning.md with header, role definition, tone guidance, 3-paragraph output structure, JSON closing instruction, and ~50 lines of fictional but plausible example fleet + ~30 lines of style guidance (no emojis, no bullets in body, KD-formatted numbers as "KD 145", driver names in bold). Total length target: ~2,500 tokens.
    2. Create briefing-owner-evening.md with the same template but tone shift to "end-of-day debrief": today's results vs target / tomorrow's outlook / GPS gaps. Add explicit D-04 carve-out: "If no shifts ran today (Eid, holiday, owner closed), still send a 1-paragraph 'ops nominal' note plus a 1-sentence tomorrow outlook. NEVER skip the cron tick."
    3. Create briefing-accountant.md (shorter — weekly only): 1 paragraph covering cash outstanding by platform + age buckets + reminders drafted/pending + reconciliation gaps.
    4. Create briefing-dispatcher.md (shorter — daily pre-brief): 1 paragraph covering scheduled-not-online count + GPS-stale-risk (drivers whose mobile-GPS uptime under 85% last 7 days) + top-3 drivers to watch this shift.
    5. Each file ends with footer block listing fields the worker MUST inject in messages[] user message: tenantName, fleetSize, yesterdayKpis, todayShifts, openAnomalies, recentRollbacks.
    6. Verify token count via word count proxy: each file should have >=400 words.
  </action>
  <verify>
    <automated>cd backend && for f in src/agent/prompts/briefing-owner-morning.md src/agent/prompts/briefing-owner-evening.md src/agent/prompts/briefing-accountant.md src/agent/prompts/briefing-dispatcher.md; do words=$(wc -w < "$f"); [ "$words" -ge 200 ] || exit 1; done; grep -L "english\|arabic\|JSON" src/agent/prompts/briefing-*.md | wc -l | grep -E "^\s*0\s*$"</automated>
  </verify>
  <done>
    All 4 prompt files exist;
    each >=200 words (markdown padding pushes >=2,048 tokens for Sonnet 4.6 cache);
    every prompt mentions JSON output + english + arabic fields;
    NO file contains tenant-name interpolation tokens (manual review per Pitfall 7).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.2: Extend runAgent + scheduledBriefingsWorker.processTick for role prompts + Anthropic cache + email fan-out + 429 retry</name>
  <files>
    backend/src/agent/runtime.ts,
    backend/src/queues/scheduledBriefingsWorker.ts,
    backend/src/__tests__/queues/scheduledBriefings.test.ts
  </files>
  <behavior>
    - runAgent gains optional systemPromptOverride: string and systemPromptCache: "ephemeral_1h" | "none" opts.
    - When systemPromptCache="ephemeral_1h", runtime wires cache_control { type: "ephemeral", ttl: "1h" } on the system prompt block (RESEARCH Pattern 2). MetricEvent briefing_cache_status recorded with cache_read_input_tokens from response (Pitfall 7 observability).
    - processTick(data) signature unchanged but BEHAVIOR extended:
      1. Look up ScheduledBriefing row → read prompt field (now treated as a prompt-key like "briefing-owner-morning" — if file exists, load as cached system prompt; else fall back to free text).
      2. Build user-message body from tenant context: tenantName + yesterdayKpis + todayShifts + openAnomalies + recentRollbacks.
      3. Call runAgent("chat", { tenantId, userMessage, systemPromptOverride, systemPromptCache: "ephemeral_1h" }).
      4. Parse response as JSON { english, arabic }. If parse fails, fall back to raw text as english.
      5. Write ChatThread + 2 ChatMessages (existing Phase 4 path).
      6. Write Notification: category=IMPORTANT, title="Daily briefing — morning" or evening, titleAr Arabic equivalent, message english.substring(0,280), bodyAr arabic.substring(0,280). (Note: schema field is `message`, not `body`.)
      7. If tenant.briefingEmailRecipients is a non-empty string array AND briefing.channels includes "email", call notificationChannels.sendEmail for each recipient with subject and body=english (plain text — Security V10).
      8. On Anthropic 429: retry once after 30s backoff (Pitfall 4). On second 429: writeErrorThread + MetricEvent scheduled_briefing_failed with error rate_limit_exceeded.
    - Worker concurrency bumped from 2 to 5 (Pitfall 1).
    - Idempotency: each tick writes Redis SETNX briefing-idem:briefingId:cronTickIso with 25h TTL (Threat T-04-stuck-worker).
    - All Wave 0 tests in scheduledBriefings.test.ts MUST now turn GREEN — tighten assertions accordingly.
  </behavior>
  <action>
    1. Edit backend/src/agent/runtime.ts — locate the existing runAgent export. Add two optional params on the opts object (systemPromptOverride and systemPromptCache). In the messages.create call, when systemPromptOverride is set, build systemBlocks as TextBlockParam[] with cache_control set when systemPromptCache equals "ephemeral_1h". After receiving the response, if usage.cache_read_input_tokens or usage.cache_creation_input_tokens is non-zero, record a MetricEvent named briefing_cache_status with cacheReadTokens + cacheCreationTokens properties.

    2. Edit backend/src/queues/scheduledBriefingsWorker.ts:
       - Add imports for sendEmail, readFileSync, join.
       - Add helper loadBriefingPrompt(promptKey) that reads from agent/prompts/ directory; fallback to raw key on file-missing for back-compat.
       - Inside processTick: detect if data.prompt matches a known prompt key (briefing-owner-morning, briefing-owner-evening, briefing-accountant, briefing-dispatcher) — if yes load file as systemPromptOverride. Build per-tenant userMessage by querying Tenant + computing tenantContext via buildTenantBriefingContext helper. Wrap runAgent call in try/catch with one 30s-backoff retry on 429 detection.
       - After ChatThread+messages write (existing path), parse assistantText for JSON {english, arabic} via regex match + JSON.parse with fallback. Determine flavor (morning/evening/digest) from cron hour field. Create Notification row with category=IMPORTANT, titleAr Arabic equivalent. Use the schema field `message` (not `body`) for the English text (Info I-1 from plan-checker). If channels includes "email" and Tenant.briefingEmailRecipients is non-empty, loop and call sendEmail for each.
       - Bump Worker concurrency to 5 (line ~349).
       - Add idempotency guard immediately after type-check: SETNX briefing-idem:briefingId:cronTickIsoMin with EX 90_000. If set returns null (already exists), return status="skipped" reason="duplicate-tick".
       - Add buildTenantBriefingContext(tenantId) helper using Promise.all for openAnomalies (Alert count where resolvedAt null), recentRollbacks (AgentAction rolledBackAt not null), shiftsToday (Shift count where scheduledStart gte midnight). Catch and return 0 on any per-query error.

    3. Update backend/src/__tests__/queues/scheduledBriefings.test.ts — tighten assertions from Wave 0:
       - processTick test asserts result.status equals "completed" AND one Notification row exists with category="IMPORTANT".
       - If tenant.briefingEmailRecipients seeded with ["test@example.com"], assert sendEmail mock was called once.
       - Evening-quiet-day test seeds zero shifts and asserts the Notification message contains "ops nominal" OR assistantText falls back without error.
       - Provision test (from Wave 0) stays — Task 1.3 creates the service that makes it GREEN.
       - Mock runtime.runAgent to return a fixed assistantText JSON ({ english: "Ops nominal — no new clusters.", arabic: "العمليات طبيعية" }) + usage with cache_read_input_tokens: 1500.
  </action>
  <verify>
    <automated>cd backend && npx jest src/__tests__/queues/scheduledBriefings.test.ts 2>&1 | tail -15 && grep -c "cache_control" src/agent/runtime.ts | grep -v '^#' && grep -c "sendEmail\|briefingEmailRecipients" src/queues/scheduledBriefingsWorker.ts | grep -v '^#'</automated>
  </verify>
  <done>
    Wave 0 scheduledBriefings.test.ts cases all PASS (no more module-not-found, no skipped tests);
    runtime.ts contains cache_control with ttl 1h syntax in >=1 location;
    scheduledBriefingsWorker.ts contains sendEmail + briefingEmailRecipients references;
    Worker concurrency at line ~349 set to 5 not 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.3: briefingProvisionService + onboarding hook in routes/admin/onboarding.ts (B-3 fix)</name>
  <files>
    backend/src/services/briefingProvisionService.ts,
    backend/src/routes/admin/onboarding.ts,
    backend/src/services/scheduledBriefingsService.ts
  </files>
  <behavior>
    - briefingProvisionService.provisionDefaultBriefings({ tenantId, ownerUserId, dispatcherUserId?, accountantUserId? }) creates ScheduledBriefing rows via existing scheduledBriefingsService.createBriefing.
    - Stagger derivation: minutes = parseInt(tenantId.slice(0,8), 16) % 5 — deterministic per-tenant 0–4 minute offset (Pitfall 4).
    - Times sourced from Tenant.briefingMorningTime (default "07:00") + briefingEveningTime (default "18:00"). Split "HH:MM" → use HH as cron hour, append stagger as cron minute.
    - Owner gets 2 briefings (morning + evening), prompts = "briefing-owner-morning" and "briefing-owner-evening" (file-key strings, NOT raw text — the worker loads from disk).
    - Dispatcher gets 1 morning briefing if userId provided, prompt "briefing-dispatcher".
    - Accountant gets 1 weekly Monday 06:00 briefing if userId provided, prompt "briefing-accountant".
    - channels default to ["in_chat", "email"] for owner/accountant, ["in_chat"] for dispatcher (per RESEARCH Wave 1 spec lines 466-501).
    - Idempotency: if a briefing with same (tenantId, userId, name) already exists, SKIP (don't double-create on re-onboarding).
    - **B-3 fix: hook lives in `backend/src/routes/admin/onboarding.ts` POST handler** (NOT a non-existent `services/onboarding/onboardingService.ts`). After the existing `tenant.create` (line ~107) AND `user.create` (line ~119) both succeed, insert `await provisionDefaultBriefings({ tenantId: tenant.id, ownerUserId: ownerUser.id })`. Wrap in try/catch with logging; on error, log + record MetricEvent briefing_provision_failed but do NOT throw (don't break onboarding for a non-critical briefing seed).
    - The validateCron whitelist in scheduledBriefingsService MUST be extended to include "0 18 * * *" (evening default) and "{0-4} 7 * * *" / "{0-4} 18 * * *" patterns. Simplest fix: relax minute-field check to accept 0-59 when HH in {6,7,17,18} for default cron values, OR enumerate the staggered patterns explicitly.
  </behavior>
  <action>
    1. Create backend/src/services/briefingProvisionService.ts with provisionDefaultBriefings export. Implementation:
       - import createBriefing from scheduledBriefingsService
       - import prisma from config
       - Helper computeStagger(tenantId): parseInt(tenantId.slice(0,8).replace(/[^0-9a-f]/gi,'a'), 16) modulo 5
       - Helper buildCron(timeStr, dayOfWeek?): timeStr is "HH:MM"; split on ":"; cron is "stagger HH * * dayOfWeek" or "stagger HH * * *". For accountant Monday-only: dayOfWeek=1.
       - For each role (owner, dispatcher, accountant): check existing row by findFirst tenantId+userId+name; if exists skip; else call createBriefing with channels per role.
       - All createBriefing calls pass userRole="ADMIN" so the custom cron passes validateCron (since staggered minutes aren't in the static whitelist).
       - Return { created: number, skipped: number }.
       - File should be >=80 lines including doc comment + type defs + 4 createBriefing call sites.

    2. Edit backend/src/services/scheduledBriefingsService.ts validateCron — extend allow-list:
       - Approach: keep static whitelist for backward compat. Add a regex check for the Phase 11 staggered defaults: /^[0-4] (7|18) \* \* \*$/ and /^[0-4] 6 \* \* 1$/ — these pass without requiring ADMIN role (so provisionDefaultBriefings can run from a system context without forging an ADMIN session).
       - Place this check immediately after the static whitelist check; before the userRole=ADMIN gate.

    3. Edit backend/src/routes/admin/onboarding.ts (B-3 — THIS is the real onboarding entry; verified via `grep -n "tenant.create" src/routes/admin/onboarding.ts` → line 107):
       - Add import at top: `import { provisionDefaultBriefings } from "../../services/briefingProvisionService";`
       - Locate the POST handler. After BOTH `tenant.create` (around line 107) AND `user.create` (around line 119) succeed, BEFORE the final `res.status(201).json(...)` response, insert:
         ```typescript
         // Phase 11 Wave 1 — auto-provision default briefings for the new tenant.
         // Non-critical; failure logs + records MetricEvent but does NOT block onboarding.
         try {
           await provisionDefaultBriefings({
             tenantId: tenant.id,
             ownerUserId: ownerUser.id,
           });
         } catch (err) {
           // logger may not be imported here; use console.warn as fallback per existing route conventions
           console.warn("[admin/onboarding] briefingProvisionService failed", { tenantId: tenant.id, error: (err as Error).message });
           try {
             const { recordMetricEvent } = await import("../../agent/metricEvent");
             await recordMetricEvent({
               tenantId: tenant.id,
               event: "briefing_provision_failed",
               properties: { error: (err as Error).message },
             });
           } catch {
             // recordMetricEvent itself failing should not break onboarding
           }
         }
         ```

    4. Verify provisionDefaultBriefings test in scheduledBriefings.test.ts (Wave 0) now PASSES.

    5. Verify the onboarding hook is wired correctly via grep:
       ```bash
       cd backend && grep -c "provisionDefaultBriefings" src/routes/admin/onboarding.ts
       ```
       Must return >=1.
  </action>
  <verify>
    <automated>cd backend && npx jest src/__tests__/queues/scheduledBriefings.test.ts -t "provisionDefaultBriefings" 2>&1 | tail -5 && [ -f src/services/briefingProvisionService.ts ] && wc -l src/services/briefingProvisionService.ts | awk '{print $1}' | grep -E "^[0-9]+$" | head -1 && grep -c "provisionDefaultBriefings" src/routes/admin/onboarding.ts | grep -v '^#'</automated>
  </verify>
  <done>
    briefingProvisionService.ts exists with >=80 lines;
    The "provisionDefaultBriefings creates 2 owner briefings" test from Wave 0 PASSES;
    Cron regex for /^[0-4] (7|18) \* \* \*$/ accepted by validateCron without ADMIN role;
    backend/src/routes/admin/onboarding.ts contains provisionDefaultBriefings call after tenant + user creation (graceful failure mode wraps it in try/catch);
    grep -c "provisionDefaultBriefings" src/routes/admin/onboarding.ts returns >=1 (B-3 closed — verification now lands on the file that actually exists).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BullMQ Worker → Anthropic API | per-tenant briefing fan-out; cost-sensitive; rate-limit-sensitive |
| BullMQ Worker → SendGrid | email delivery; Anthropic-generated content rendered as plain text (V10 — no HTML, no scripts) |
| BullMQ Worker → Redis SETNX | idempotency guard for stuck-worker re-delivery |
| Admin onboarding route → briefingProvisionService | best-effort; failure here must NOT block tenant creation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-W1-01 | Information Disclosure | briefing email body contains Anthropic-generated content (driver names, KPIs) | mitigate | sendEmail uses text param only (notificationChannels.ts:81 verified); recipients restricted to tenant.briefingEmailRecipients (founder-set per D); no PII broadcast |
| T-11-W1-02 | Tampering | cache_control TTL applied to tenant-specific prompt → cache fragments per tenant → cost balloon | mitigate | systemPrompt MUST be tenant-agnostic; per-tenant context lives in messages[] (Pitfall 7 enforced via task done criteria) |
| T-11-W1-03 | Denial of Service | All tenants share cron 0 7 * * * → Anthropic 429 → mass briefing failures | mitigate | Stagger via hash(tenantId)%5 (Pitfall 4); 30s backoff + single retry on 429; MetricEvent for monitoring |
| T-11-W1-04 | Repudiation | BullMQ stuck-worker re-delivery causes double-briefing (T-04-stuck-worker) | mitigate | Redis SETNX briefing-idem:id:tickIso with 25h TTL — second worker no-ops |
| T-11-W1-05 | Tampering | Anthropic JSON response is malformed → email body is raw markdown | mitigate | regex-match-then-parse with try/catch; on failure, fall back to assistantText as english-only; no email is sent if english is empty |
</threat_model>

<verification>
1. cd backend && npx jest src/__tests__/queues/scheduledBriefings.test.ts → ALL Wave 0 RED tests now GREEN
2. cd backend && grep -c "cache_control" src/agent/runtime.ts → >=1 (cache_control wired in)
3. cd backend && grep "concurrency:.*5" src/queues/scheduledBriefingsWorker.ts → matches (Pitfall 1 fix applied)
4. cd backend && grep -c "sendEmail\|briefingEmailRecipients\|briefing-idem" src/queues/scheduledBriefingsWorker.ts → >=3 (email + idem guard)
5. cd backend && ls src/agent/prompts/briefing-*.md | wc -l → returns 4
6. cd backend && grep -L "english\|arabic" src/agent/prompts/briefing-*.md | wc -l → returns 0 (all 4 files mention both languages)
7. cd backend && grep -c "provisionDefaultBriefings" src/routes/admin/onboarding.ts → >=1 (B-3 closed — real onboarding entry wired)
8. cd backend && npm run lint:tenant → exits 0 (new files are in extended scope from Wave 0)
</verification>

<success_criteria>
- Owners receive bilingual morning + evening briefings daily via chat + notification + (optional) email
- Anthropic cost reduced ~9x on system-prompt portion via 1h ephemeral cache (verified via MetricEvent briefing_cache_status: cache_read_input_tokens > 0 on 2nd+ tenant of the day)
- Cron jitter prevents per-org rate-limit collisions (Pitfall 4)
- D-04 honored: evening briefings always send, even on quiet days ("ops nominal" body)
- New tenants get briefings auto-provisioned at onboarding via backend/src/routes/admin/onboarding.ts hook (B-3 — real onboarding entry, not a phantom service file)
- Zero breaking changes to Phase 4 ChatThread + ChatMessage write path
</success_criteria>

<output>
After completion, create .planning/phases/11-trust-v2-briefings/11-01-SUMMARY.md capturing:
- 4 prompt files word counts + JSON-output format confirmation
- runAgent cache_control wiring + briefing_cache_status MetricEvent observability
- processTick fan-out additions (Notification + email + idem guard + 429 retry)
- briefingProvisionService stagger derivation
- onboarding hook integration point — backend/src/routes/admin/onboarding.ts post-tenant.create line ~120 (B-3 closure)
- All scheduledBriefings.test.ts cases GREEN
</output>
</content>
</invoke>