---
phase: 11-trust-v2-briefings
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - backend/prisma/migrations/20260407010000_add_platform_settings_fields/migration.sql
  - backend/prisma/migrations/20260420000000_notifications_baseline/migration.sql
  - backend/src/routes/talabat.ts
  - backend/src/routes/keeta.ts
  - backend/src/routes/vehicles.ts
  - backend/src/services/aiInsightsEngine.ts
  - backend/src/services/violationEngine.ts
  - backend/package.json
  - backend/src/__tests__/lint/di0101.test.ts
  - backend/src/__tests__/migrations/di0102.test.ts
  - .planning/phases/11-trust-v2-briefings/11-VERIFICATION.md
autonomous: true
requirements:
  - DI-01-01
  - DI-01-02
  - REQ-agent-scheduled-briefings
  - REQ-agent-trust-graduation
  - REQ-agent-long-term-memory
  - REQ-ingest-partner-api-conversations

must_haves:
  truths:
    - "DI-01-01 closed: npm run lint:tenant -- src/ exits 0 — all 184 pre-existing violations triaged (REAL = tenantId added; FALSE POSITIVE = eslint-disable-next-line with justification)"
    - "DI-01-02 closed: prisma migrate dev from a fresh DB applies all baseline + Phase 11 migrations without P3006 — surgical CREATE TABLE IF NOT EXISTS prepends on the 2 affected baseline migrations (per D-05)"
    - "lint:tenant npm script now globs src/ (no narrow scope) — guards against future regressions"
    - "Phase 11 Wave 0 migration applied via prisma migrate dev (or migrate deploy for prod-like env)"
    - "Full backend test suite GREEN: npm test passes; npm run lint:tenant passes; npm run lint passes"
    - "Frontend test suite GREEN (no Phase 11 frontend changes ship; just verify nothing broke)"
    - "VERIFICATION.md captures phase-completion criteria with evidence (test counts, lint counts, migration ID, test runs)"
  artifacts:
    - path: "backend/prisma/migrations/20260407010000_add_platform_settings_fields/migration.sql"
      provides: "PlatformSettings baseline now self-contained (CREATE TABLE IF NOT EXISTS prepend)"
      contains: "CREATE TABLE IF NOT EXISTS"
    - path: "backend/prisma/migrations/20260420000000_notifications_baseline/migration.sql"
      provides: "Notification baseline now self-contained (CREATE TABLE IF NOT EXISTS prepend)"
      contains: "CREATE TABLE IF NOT EXISTS"
    - path: "backend/package.json"
      provides: "lint:tenant globs src/ (broad)"
      contains: "src/"
    - path: ".planning/phases/11-trust-v2-briefings/11-VERIFICATION.md"
      provides: "Phase 11 completion certification"
      min_lines: 60
  key_links:
    - from: "migration 20260407010000_add_platform_settings_fields"
      to: "schema.prisma PlatformSettings model"
      via: "CREATE TABLE IF NOT EXISTS prepend"
      pattern: "CREATE TABLE IF NOT EXISTS \"PlatformSettings\""
    - from: "migration 20260420000000_notifications_baseline"
      to: "schema.prisma Notification model"
      via: "CREATE TABLE IF NOT EXISTS prepend"
      pattern: "CREATE TABLE IF NOT EXISTS \"Notification\""
    - from: "package.json lint:tenant"
      to: "all backend/src/**/*.ts files"
      via: "broadened glob with 0 violations"
      pattern: "lint:tenant.*src/"
---

<objective>
Wave 4 — DI-01-01 + DI-01-02 closeout + Phase 11 verification.

Purpose: Pay down the two long-standing Phase 1 defects that have blocked clean migrations and broad lint enforcement since 2026-05-09. Apply Wave 0's Tenant migration. Run full test suite. Author VERIFICATION.md certifying phase completion.

Output: 184 lint:tenant violations triaged across 35 files, surgical CREATE TABLE IF NOT EXISTS prepends on 2 baseline migrations, package.json lint:tenant glob broadened to src/, Phase 11 migration applied, all tests green, VERIFICATION.md.

Per RESEARCH §"DI-01-01 closeout" + §"DI-01-02 closeout" + D-05 (surgical prepends not full rebase) + Phase 1 deferred-items.md DI-01-01 instructions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-trust-v2-briefings/11-RESEARCH.md
@.planning/phases/11-trust-v2-briefings/11-00-SUMMARY.md
@.planning/phases/11-trust-v2-briefings/11-01-SUMMARY.md
@.planning/phases/11-trust-v2-briefings/11-02-SUMMARY.md
@.planning/phases/11-trust-v2-briefings/11-03-SUMMARY.md
@.planning/phases/01-backend-agent-spine-data-architecture/deferred-items.md
@backend/package.json
@backend/eslint-rules/no-prisma-without-tenant.js

<interfaces>
DI-01-01 scope (per Phase 1 deferred-items.md):
  184 violations across 35 files. Examples:
    - src/routes/talabat.ts: 27 violations across talabatSession queries
    - src/routes/keeta.ts: numerous violations on driver, shift, orderLog
    - src/routes/vehicles.ts: 6 violations on vehicle queries
    - src/services/aiInsightsEngine.ts: 3 driver.findMany calls without tenantId
    - src/services/violationEngine.ts: device + driver lookups without scoping

Triage protocol (per RESEARCH A8: ~30 real bugs + ~150 false positives):
  - REAL: query reads/writes records that SHOULD be tenant-scoped — add `tenantId: req.tenantId` or `tenantId: ctx.tenantId` to the where clause.
  - FALSE POSITIVE: query is intentionally cross-tenant (e.g., super-admin tooling, system-level operations, lookups by primary-key UUID that's globally unique like AgentAction.id) — add `// eslint-disable-next-line no-prisma-without-tenant -- <one-line justification>` directly above the prisma call.

DI-01-02 scope (per RESEARCH D-05: surgical, not rebase):
  Two baseline migrations have ALTER TABLE statements for tables created by prisma db push (no CREATE TABLE migration exists):
    1. backend/prisma/migrations/<earliest>/migration.sql with ALTER TABLE "PlatformSettings" — prepend `CREATE TABLE IF NOT EXISTS "PlatformSettings" (... columns matching schema.prisma at that historical point ...)`
    2. Same for Notification.
  The PREPENDED CREATE TABLE statements use:
    - column types matching schema.prisma at that historical point (we can take a conservative snapshot — even if today's schema has more columns, the prepended CREATE only needs ENOUGH columns to satisfy the immediate ALTER TABLE statements that follow; later ADD COLUMN statements in later migrations will catch up)
    - `IF NOT EXISTS` so the migration stays idempotent on existing dev/prod DBs
  This is the minimal change that makes `prisma migrate dev --create-only` succeed from a fresh DB.

Wave 0 staged migration to apply:
  backend/prisma/migrations/20260514000000_phase11_trust_briefings_partnerapi/migration.sql
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: DI-01-01 — Triage 184 lint:tenant violations + broaden glob</name>
  <files>
    backend/src/routes/talabat.ts,
    backend/src/routes/keeta.ts,
    backend/src/routes/vehicles.ts,
    backend/src/services/aiInsightsEngine.ts,
    backend/src/services/violationEngine.ts,
    backend/package.json,
    backend/src/__tests__/lint/di0101.test.ts
  </files>
  <behavior>
    - All 184 lint:tenant violations triaged: REAL violations get tenantId added; FALSE POSITIVE violations get inline eslint-disable comments with one-line justification.
    - lint:tenant npm script glob simplifies from the long enumeration to just `src/` (broad scope).
    - npm run lint:tenant exits 0 against the broadened scope.
    - di0101.test.ts (Wave 0 RED) turns GREEN.
    - NO behavioral change to any route — only tenantId scoping (which IS a behavior change for any query that was previously cross-tenant by accident; investigate each REAL fix to ensure it doesn't break existing flows).

    Per-file triage:
    - routes/talabat.ts (27 violations): most are talabatSession queries — likely missing tenantId. Add `tenantId: req.tenantId` to where clauses. Where the route is admin-only (e.g., bulk operations), use eslint-disable + justification.
    - routes/keeta.ts: driver/shift/orderLog queries — add tenantId. Use req.tenantId (set by tenantScope middleware).
    - routes/vehicles.ts: vehicle queries — same pattern.
    - services/aiInsightsEngine.ts: driver.findMany — likely passed tenantId via fn arg; thread it through.
    - services/violationEngine.ts: device + driver lookups — same.

    Strategy: run the broadened lint, capture the full violation list, work file-by-file. Each file edit must be small + reviewable. After each file, re-run lint:tenant to confirm violation count decreases.
  </behavior>
  <action>
    1. Establish baseline — broaden the lint:tenant glob temporarily:
       ```bash
       cd backend && npm run lint:tenant -- src/ 2>&1 | tee /tmp/lint-tenant-baseline.txt
       wc -l /tmp/lint-tenant-baseline.txt  # ~184+ violations expected
       ```

    2. For each file in the baseline output, open and inspect each violation:
       - If the surrounding context shows the query SHOULD be tenant-scoped (route handler reading user-facing data, service called from tenant context): add tenantId. Look for `req.tenantId`, `req.user.tenantId`, or `ctx.tenantId` in scope.
       - If the query is intentionally global (e.g., admin scripts, system bootstrap, lookups by globally-unique UUID PKs): add an eslint-disable-next-line comment with one-line justification.

       Example REAL fix (talabat.ts hypothetical):
       ```typescript
       // Before
       const sessions = await prisma.talabatSession.findMany({ where: { active: true } });
       // After
       const sessions = await prisma.talabatSession.findMany({ where: { tenantId: req.tenantId, active: true } });
       ```

       Example FALSE POSITIVE fix:
       ```typescript
       // eslint-disable-next-line no-prisma-without-tenant -- admin reconciliation script; runs once at migration time
       const allTenants = await prisma.tenant.findMany();
       ```

    3. Per-file iteration (target order, by violation count descending):
       a. backend/src/routes/talabat.ts (~27 violations)
       b. backend/src/routes/keeta.ts (~? violations — measure)
       c. backend/src/services/aiInsightsEngine.ts (~3 violations)
       d. backend/src/services/violationEngine.ts (~? violations)
       e. backend/src/routes/vehicles.ts (~6 violations)
       f. Remaining files in the baseline output

       After each file edit:
       ```bash
       cd backend && npm run lint:tenant -- src/ 2>&1 | wc -l
       ```
       Confirm count is going down monotonically.

    4. After ALL violations triaged, update package.json lint:tenant script to use simple broad glob:
       - Replace the long path enumeration with: `"lint:tenant": "eslint src/ --ext .ts --no-eslintrc --resolve-plugins-relative-to . --rulesdir eslint-rules --parser @typescript-eslint/parser --parser-options=ecmaVersion:2022,sourceType:module --rule \"{\\\"no-prisma-without-tenant\\\":\\\"error\\\"}\" --no-error-on-unmatched-pattern"`
       - Save package.json.

    5. Confirm di0101.test.ts (Wave 0) now PASSES:
       ```bash
       cd backend && npx jest src/__tests__/lint/di0101.test.ts
       ```

    6. Run full test suite to confirm no regressions from the tenantId additions:
       ```bash
       cd backend && npm test 2>&1 | tail -30
       ```
       If any test fails, inspect — likely a test fixture is missing tenantId on a seed row. Fix seed/test fixtures rather than reverting the production code change.

    7. Commit each file group separately (or all at once if changes are small) with message like:
       "fix(tenant-scope): close DI-01-01 — triage 184 lint:tenant violations across 35 files"
  </action>
  <verify>
    <automated>cd backend &amp;&amp; npm run lint:tenant 2>&amp;1 | tail -5 &amp;&amp; npx jest src/__tests__/lint/di0101.test.ts 2>&amp;1 | tail -5 &amp;&amp; grep -c "src/\"" package.json | grep -v '^#' &amp;&amp; npm test 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    npm run lint:tenant exits 0 against broadened src/ glob;
    di0101.test.ts GREEN;
    package.json lint:tenant script uses `src/` not the long enumeration;
    npm test passes (no regressions from tenantId additions).
  </done>
</task>

<task type="auto">
  <name>Task 4.2: DI-01-02 — Surgical CREATE TABLE IF NOT EXISTS prepends + apply Phase 11 migration + Wave 4 fresh-migrate smoke</name>
  <files>
    backend/prisma/migrations/20260407010000_add_platform_settings_fields/migration.sql,
    backend/prisma/migrations/20260420000000_notifications_baseline/migration.sql,
    backend/src/__tests__/migrations/di0102.test.ts
  </files>
  <behavior>
    - Identify the 2 specific baseline migrations that contain ALTER TABLE statements for tables (PlatformSettings + Notification) that were never CREATEd in any prior migration (i.e., were created via `prisma db push`).
    - Prepend CREATE TABLE IF NOT EXISTS statements with the minimal column set needed for the immediate ALTER statements in that file to succeed. Use snake_case + IF NOT EXISTS for idempotency.
    - Apply Phase 11 Wave 0 migration: `prisma migrate dev --name phase11_trust_briefings_partnerapi` (this runs the migration AND regenerates Prisma Client).
    - Run di0102.test.ts smoke (Wave 0): `RUN_FRESH_MIGRATE_SMOKE=1 npx jest src/__tests__/migrations/di0102.test.ts` — spins fresh Docker postgres, runs prisma migrate dev --create-only, asserts success.
  </behavior>
  <action>
    1. Identify the affected baseline migrations:
       ```bash
       cd backend && ls prisma/migrations/ | head -10
       # Find migrations referencing ALTER TABLE "PlatformSettings" or ALTER TABLE "Notification"
       grep -l "ALTER TABLE \"PlatformSettings\"" prisma/migrations/*/migration.sql | head -5
       grep -l "ALTER TABLE \"Notification\"" prisma/migrations/*/migration.sql | head -5
       ```
       Use the EARLIEST migration that ALTERs each table (the one that should have CREATEd it but didn't).

    2. For the PlatformSettings baseline migration:
       - Read the migration.sql
       - At the top (BEFORE the first ALTER TABLE statement), prepend:
         ```sql
         -- DI-01-02 closeout (Phase 11 Wave 4) — surgical CREATE TABLE IF NOT EXISTS.
         -- Original migration assumed PlatformSettings table existed from prisma db push.
         -- Prepending the CREATE makes this migration self-contained (works on fresh DB)
         -- while staying idempotent on existing DBs (IF NOT EXISTS).
         CREATE TABLE IF NOT EXISTS "PlatformSettings" (
           "id" TEXT NOT NULL,
           "tenantId" TEXT NOT NULL,
           "platform" TEXT NOT NULL,
           "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
           "updatedAt" TIMESTAMP(3) NOT NULL,
           CONSTRAINT "PlatformSettings_pkey" PRIMARY KEY ("id")
         );
         ```
         (Use the actual minimal column set needed for the ALTER statements that follow. Read schema.prisma's PlatformSettings model + cross-reference which columns the immediate ALTER touches.)
       - Save migration.sql

    3. Repeat for Notification baseline migration with appropriate minimal CREATE TABLE.

    4. Verify the prepended SQL is syntactically valid:
       ```bash
       cd backend && cat prisma/migrations/20260407010000_add_platform_settings_fields/migration.sql | head -30
       ```

    5. Apply Phase 11 Wave 0 migration:
       ```bash
       cd backend && npx prisma migrate dev --name phase11_wave0_apply
       ```
       (This should be a NO-OP on dev DB since Wave 0 already ran `prisma migrate dev` or `db push` indirectly — but in case the migration is still pending, this applies it. Confirms migration_lock.toml is happy.)

    6. Run fresh-DB smoke test:
       ```bash
       cd backend && RUN_FRESH_MIGRATE_SMOKE=1 npx jest src/__tests__/migrations/di0102.test.ts 2>&1 | tail -10
       ```
       This runs scripts/test-fresh-migrate.sh which spins a Docker postgres and runs `prisma migrate dev --create-only` against it. Must exit 0.

    7. Regenerate Prisma Client to pick up new Tenant columns:
       ```bash
       cd backend && npx prisma generate
       ```

    8. Run full backend suite to confirm new Tenant columns don't break existing tests:
       ```bash
       cd backend && npm test 2>&1 | tail -20
       ```
  </action>
  <verify>
    <automated>cd backend &amp;&amp; grep -c "CREATE TABLE IF NOT EXISTS \"PlatformSettings\"\|CREATE TABLE IF NOT EXISTS \"Notification\"" prisma/migrations/*/migration.sql 2>/dev/null | grep -v '^#' &amp;&amp; npx prisma validate &amp;&amp; npm test 2>&amp;1 | grep -E "Tests:.*passed|FAIL" | tail -3</automated>
  </verify>
  <done>
    ≥2 migrations have CREATE TABLE IF NOT EXISTS prepends;
    prisma validate exits 0;
    Phase 11 Wave 0 migration applied (visible in `npx prisma migrate status`);
    di0102.test.ts smoke (RUN_FRESH_MIGRATE_SMOKE=1) PASSES;
    Full backend test suite passes — no regressions from new Tenant columns.
  </done>
</task>

<task type="auto">
  <name>Task 4.3: Phase verification — full suite green + author VERIFICATION.md</name>
  <files>
    .planning/phases/11-trust-v2-briefings/11-VERIFICATION.md
  </files>
  <behavior>
    - VERIFICATION.md captures phase-completion criteria with evidence:
      * Test counts: backend total, breakdown by Phase 11 test domains
      * Lint: lint + lint:tenant exit codes + violation counts (should be 0)
      * Migration: which migration applied, prisma migrate status output snippet
      * REQ coverage: each REQ-* lists the tests/code paths satisfying it
      * Decisions D-01..D-05 traceability: each decision lists the file(s) implementing it
      * Threat model: STRIDE register from each plan with disposition
      * Founder-gated action items: what user must do post-deploy
    - File ≥60 lines.
    - Frontend tests: just confirm `npm test` in frontend/ exits 0 — no Phase 11 frontend changes, so this is a regression check.
  </behavior>
  <action>
    1. Run the full check suite and capture outputs:
       ```bash
       cd backend && npm test 2>&1 | tail -20  > /tmp/backend-tests.txt
       cd backend && npm run lint:tenant 2>&1 | tail -10 > /tmp/lint-tenant.txt
       cd backend && npm run lint 2>&1 | tail -10 > /tmp/lint.txt
       cd backend && npx prisma migrate status 2>&1 | tail -10 > /tmp/migrate-status.txt
       cd frontend && npm test 2>&1 | tail -10 > /tmp/frontend-tests.txt
       ```
       All four backend commands MUST exit 0.

    2. Author .planning/phases/11-trust-v2-briefings/11-VERIFICATION.md with sections:

       ```markdown
       # Phase 11 Verification — Scheduled Briefings + Trust Graduation v2 + Mature Memory + Partner-API Outreach + DI Closeout

       **Completed:** 2026-05-13
       **Status:** GREEN — phase certified complete.

       ## Plans

       | Plan | Wave | Status | Files Modified | Notes |
       |------|------|--------|----------------|-------|
       | 11-00 | 0 | GREEN | schema.prisma + migration.sql + 9 RED tests + lint glob | All scaffolding landed |
       | 11-01 | 1 | GREEN | 4 prompts + runtime.ts + scheduledBriefingsWorker.ts + briefingProvisionService.ts + onboarding hook | Bilingual briefings live; Anthropic 1h cache wired |
       | 11-02 | 2 | GREEN | trustGraduation.ts + registry.ts + ledger.ts + memoryCompactor.ts + memoryPruneWorker.ts + admin/trust.ts + seed.ts | Trust gate inserted at registry.ts:148; memory pruner running daily 03:00 |
       | 11-03 | 3 | GREEN | 4 partnerApi/ files + registry.ts + partnerApiPullWorker.ts + admin/partnerApi.ts | Keeta live; Talabat + Deliveroo skeletons |
       | 11-04 | 4 | GREEN | DI-01-01 + DI-01-02 closeout + VERIFICATION | 184 violations triaged; baseline migrations self-contained |

       ## Test Suite

       | Suite | Result | Count | Notes |
       |-------|--------|-------|-------|
       | Backend (npm test) | PASSED | <fill from /tmp/backend-tests.txt> | Includes 9 new Phase 11 test files |
       | Backend (lint) | EXIT 0 | 0 errors, 0 warnings | <paste relevant grep> |
       | Backend (lint:tenant) | EXIT 0 | 0 violations | DI-01-01 closed |
       | Backend (fresh-migrate smoke) | EXIT 0 | — | DI-01-02 closed |
       | Frontend (npm test) | PASSED | <fill> | No Phase 11 frontend changes; regression check only |

       ## REQ Coverage

       | REQ-ID | Implementation | Tests | Status |
       |--------|----------------|-------|--------|
       | REQ-agent-scheduled-briefings | Wave 1 — briefingProvisionService.ts + 4 prompts + extended processTick | src/__tests__/queues/scheduledBriefings.test.ts (3 cases) | ✓ |
       | REQ-agent-trust-graduation | Wave 2 — trustGraduation.ts + registry.ts insertion + per-tool config | src/__tests__/agent/trustGraduation.test.ts (5 cases) | ✓ |
       | REQ-agent-long-term-memory | Wave 2 — memoryCompactor.ts + memoryPruneWorker.ts daily 03:00 cron | src/__tests__/agent/memoryCompactor.test.ts + queues/memoryPruneWorker.test.ts | ✓ |
       | REQ-ingest-partner-api-conversations | Wave 3 — KeetaPartnerApiAdapter live; Talabat/Deliveroo stubs | src/__tests__/services/ingest/partnerApi/{keeta,fallback}.test.ts | ✓ |
       | DI-01-01 | Wave 4 — 184 violations triaged + glob broadened to src/ | src/__tests__/lint/di0101.test.ts | ✓ |
       | DI-01-02 | Wave 4 — CREATE TABLE IF NOT EXISTS prepends on 2 baseline migrations | src/__tests__/migrations/di0102.test.ts + scripts/test-fresh-migrate.sh | ✓ |

       ## Decision Traceability

       | Decision | Implementation File(s) | Status |
       |----------|------------------------|--------|
       | D-01: Tenant.trustLevel default 0 | schema.prisma + migration.sql + trustGraduation.ts | ✓ |
       | D-02: Auto-approved actions visible in /decisions with auto badge | AgentAction.source='auto'; Phase 2 audit projector renders unchanged | ✓ |
       | D-03: Partner-API 30-day backfill on first sync | keeta.ts resolveRangeOrBackfill | ✓ |
       | D-04: Evening briefing always sends on quiet days | briefing-owner-evening.md carve-out | ✓ |
       | D-05: DI-01-02 surgical CREATE TABLE IF NOT EXISTS prepends | 2 migration.sql prepends | ✓ |

       ## Threat Model Summary

       <Copy-paste STRIDE registers from each plan; show all dispositions are mitigate or accept (no open).>

       ## Founder Action Items (Post-Deploy)

       1. **Bump design-partner-1 trust level**: Once design partner #1 has 30 days of clean AgentAction history, POST /api/admin/tenants/:id/trust { "trustLevel": 2, "autoApproveCapDaily": 50 } to graduate them to v2-full.
       2. **Configure briefing email recipients**: POST /api/admin/tenants/:id/trust { "briefingEmailRecipients": ["owner@fleet.com"] } per tenant.
       3. **Secure Keeta partner-API credentials**: Engage Keeta partner ops; once API key is issued, POST /api/admin/partnerApi/credentials with { tenantId, platform: "KEETA", apiKey, baseUrl, fleetId }. The encrypted credential is stored in Tenant.partnerApiSecrets; daily 06:00 cron picks it up. First sync backfills 30 days.
       4. **Talabat + Deliveroo partner-API**: Skeletons reserved; defer to Phase 12 once contracts are signed (or 5+ paying fleets per ROADMAP gate).

       ## Files Created / Modified

       <Auto-generated list — paste output of `git diff --name-only HEAD~N HEAD` since Phase 11 start>

       ## Known Caveats / Deferred

       - PORTAL_CRED_KEY env var must be set in production for partnerApi/keeta.ts to work; Wave 0 staged the column but did NOT verify the env var. Add to deploy checklist.
       - Anthropic prompt cache requires Sonnet 4.6 and cache_control field support; verified against SDK 0.80.0 + platform.claude.com docs. If model is downgraded, cache silently disables (no failure mode change).
       - Talabat + Deliveroo partner-API stubs remain NotAvailable until contracts are signed; CompositeAdapter fallthrough ensures Phase 6 scrapers + XLSX continue working unchanged.

       ## Sign-Off

       Phase 11 closed: 2026-05-13. Ready for Phase 12 (Standing Rules + Forecasting).
       ```

    3. Validate VERIFICATION.md by:
       - cd backend && wc -l ../.planning/phases/11-trust-v2-briefings/11-VERIFICATION.md → ≥60 lines
       - Contains every required section (Plans, Test Suite, REQ Coverage, Decision Traceability, Threat Model, Founder Action Items, Files, Caveats, Sign-Off)
  </action>
  <verify>
    <automated>cd backend &amp;&amp; [ -f ../.planning/phases/11-trust-v2-briefings/11-VERIFICATION.md ] &amp;&amp; wc -l ../.planning/phases/11-trust-v2-briefings/11-VERIFICATION.md | awk '{print $1}' | grep -E "^([6-9][0-9]|[1-9][0-9]{2,})$" &amp;&amp; grep -c "REQ Coverage\|Decision Traceability\|Threat Model\|Founder Action Items\|Sign-Off" ../.planning/phases/11-trust-v2-briefings/11-VERIFICATION.md | grep -v '^#'</automated>
  </verify>
  <done>
    11-VERIFICATION.md exists with ≥60 lines;
    All required sections present;
    All test/lint/migrate commands documented as EXIT 0;
    Founder action items listed (4 items);
    Caveats + sign-off present.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 35 legacy route/service files → tenantId scoping | DI-01-01 closure adds tenantId to dozens of queries; risk of breaking existing flows if a test fixture is missing tenantId |
| 2 baseline migrations → fresh postgres | DI-01-02 closure prepends idempotent CREATE TABLE IF NOT EXISTS; new dev environments must apply cleanly |
| Phase 11 Wave 0 migration → prod DB | additive only (already mitigated in Plan 00); applied via prisma migrate deploy in CI/CD |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-W4-01 | Tampering | tenantId addition silently changes query results in production | mitigate | Test suite runs after each file's triage; npm test must pass; manual review of high-traffic routes (talabat.ts, keeta.ts); rollback plan = git revert + redeploy |
| T-11-W4-02 | Repudiation | eslint-disable comment hides a REAL violation as false-positive | mitigate | Each eslint-disable-next-line MUST carry a one-line justification; code review pass for the commit batch |
| T-11-W4-03 | Tampering | CREATE TABLE IF NOT EXISTS prepend column types differ from actual prod DB schema | accept | IF NOT EXISTS means the prepend is a NO-OP on existing DBs; type mismatch only matters in fresh-DB rebuild which is checked by di0102 smoke test |
| T-11-W4-04 | Denial of Service | Phase 11 migration apply takes a table lock | mitigate | All 5 ADD COLUMN statements use IF NOT EXISTS + DEFAULT; PG12+ metadata-only ops; pattern already validated in Phase 2 Wave 5 |
</threat_model>

<verification>
1. cd backend && npm run lint:tenant → EXIT 0 (DI-01-01 closed)
2. cd backend && npm run lint:tenant -- src/ → EXIT 0 against full src/ tree (confirms broadened scope works)
3. cd backend && npx prisma migrate status → shows 20260514000000_phase11_trust_briefings_partnerapi APPLIED
4. cd backend && RUN_FRESH_MIGRATE_SMOKE=1 npx jest src/__tests__/migrations/di0102.test.ts → EXIT 0 (DI-01-02 closed)
5. cd backend && npm test → all 188+ tests + 9 new Phase 11 tests = ~200+ tests PASSING
6. cd backend && npm run lint → EXIT 0
7. cd frontend && npm test → EXIT 0 (regression check; no Phase 11 frontend work)
8. [ -f .planning/phases/11-trust-v2-briefings/11-VERIFICATION.md ] && wc -l shows ≥60 lines
9. grep "REQ-agent-scheduled-briefings\|REQ-agent-trust-graduation\|REQ-agent-long-term-memory\|REQ-ingest-partner-api-conversations\|DI-01-01\|DI-01-02" .planning/phases/11-trust-v2-briefings/11-VERIFICATION.md → finds ≥6 (full REQ coverage)
10. Phase status: cd backend && grep "Phase 11" ../.planning/STATE.md (Wave 4 updates STATE.md last)
</verification>

<success_criteria>
- DI-01-01 closed: 184 violations triaged, lint:tenant globs src/ broadly, future violations caught in CI
- DI-01-02 closed: fresh `prisma migrate dev` works from clean checkout, baseline migrations are self-contained
- Phase 11 migration applied to dev DB (and ready for production via prisma migrate deploy)
- Full backend test suite green (~200 tests including the 9 new Phase 11 tests)
- Frontend test suite green (no regressions)
- Phase 11 VERIFICATION.md authored with full evidence trail
- Founder has a clear list of post-deploy actions (4 items)
</success_criteria>

<output>
After completion, create .planning/phases/11-trust-v2-briefings/11-04-SUMMARY.md (or finalize 11-VERIFICATION.md) capturing:
- DI-01-01 violation count: 184 → 0 (split: ~N real fixes + ~M eslint-disable justifications)
- DI-01-02 prepends: 2 baseline migrations + 1 fresh-DB smoke test pass
- Phase 11 migration applied — Tenant has 5 new columns
- Full test counts before/after
- Phase 11 cleanly closed; ready for Phase 12
- Update .planning/STATE.md with last_activity + progress.completed_phases bump (4 → 5 if including ✓ marker)
</output>
