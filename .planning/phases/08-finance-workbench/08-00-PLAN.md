---
phase: 08-finance-workbench
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/package.json
  - backend/src/__tests__/agent/tools/action/applyPenalty.test.ts
  - backend/src/__tests__/agent/tools/action/suspendDriver.test.ts
  - backend/src/__tests__/agent/tools/action/reassignShift.test.ts
  - backend/src/__tests__/agent/tools/action/recordCashSettlement.test.ts
  - backend/src/__tests__/agent/tools/action/sendCourierMessage.test.ts
  - backend/src/__tests__/agent/tools/action/createTrainingTask.test.ts
  - backend/src/__tests__/agent/tools/action/escalateToHumanSupervisor.test.ts
  - backend/src/__tests__/agent/tools/action/generatePayrollAdjustment.test.ts
  - backend/src/__tests__/agent/tools/action/applyPenalty.suspend-cascade.test.ts
  - backend/src/__tests__/decisions/cardProjector.phase8.test.ts
  - backend/src/__tests__/decisions/dualApproval.phase8.test.ts
  - backend/src/__tests__/services/finance/rollbackDispatcher.test.ts
  - backend/src/__tests__/services/finance/reconciliation.test.ts
  - backend/src/__tests__/services/finance/payrollComputer.test.ts
  - backend/src/__tests__/services/finance/bankFileExporter.test.ts
  - backend/src/__tests__/services/finance/pnlAggregator.test.ts
  - backend/src/__tests__/routes/cash.reconciliation.test.ts
  - backend/src/__tests__/routes/financePayroll.test.ts
  - backend/src/__tests__/routes/financeInvoices.test.ts
  - backend/src/__tests__/routes/financeExpenses.test.ts
  - backend/src/__tests__/routes/financePnl.test.ts
  - backend/src/__tests__/routes/auditRollback.phase8.test.ts
  - frontend/src/__tests__/finance/FinanceLayout.test.tsx
  - frontend/src/__tests__/finance/ReconciliationGrid.test.tsx
  - frontend/src/__tests__/finance/PayrollRunTable.test.tsx
  - frontend/src/__tests__/finance/PnlChart.test.tsx
  - frontend/src/__tests__/finance/ActionHistoryTable.test.tsx
autonomous: true
requirements:
  - REQ-finance-cash-workbench
  - REQ-finance-payroll
  - REQ-finance-invoices
  - REQ-finance-expenses-pl
  - REQ-agent-action-tools

must_haves:
  truths:
    - "Every Wave 1-4 task has a failing test waiting under __tests__/ that it will turn green"
    - "8 RED tool test files exist (applyPenalty, suspendDriver, reassignShift, recordCashSettlement, sendCourierMessage, createTrainingTask, escalateToHumanSupervisor, generatePayrollAdjustment) — one per new action tool"
    - "1 integration test asserts applyPenalty with penaltyType=ACCOUNT_SUSPENSION cascades into Driver.status=SUSPENDED + Shift.updateMany(BOOKED -> CANCELLED) + DriverRestriction.create — all inside one prisma.$transaction"
    - "cardProjector.phase8.test.ts asserts PHASE_8_LIVE_TOOLS Set contains all 10 names (draftCourierMessage + sendCourierMessage + applyPenalty + suspendDriver + reassignShift + flagForReview + createTrainingTask + recordCashSettlement + escalateToHumanSupervisor + generatePayrollAdjustment) AND toolIsLive computed from the UNION of PHASE_2_LIVE_TOOLS and PHASE_8_LIVE_TOOLS"
    - "dualApproval.phase8.test.ts re-runs the Phase 2 T-02-14 race against applyPenalty: two simultaneous approves on the same PendingAgentAction — first returns 200 + writes ONE Penalty row + ONE AgentAction row; second returns 409 + writes zero additional rows"
    - "rollbackDispatcher.test.ts asserts each of the 8 new tools has a registered REVERSERS entry: applyPenalty -> Penalty.penaltyStatus=OVERTURNED; suspendDriver -> Driver.status=ACTIVE; reassignShift -> Shift.driverId=previousDriverId; recordCashSettlement -> throws 'Cash settlement rollback requires accountant manual reversal' (canonical message per W5); generatePayrollAdjustment -> throws when PayrollRun.status=FROZEN; sendCourierMessage soft-rolls via Notification.type prefix flag (NotificationDeliveryStatus enum has no CANCELLED value); createTrainingTask -> TrainingTask.status=CANCELLED; escalateToHumanSupervisor -> no-op reverser (information-only)"
    - "auditRollback.phase8.test.ts asserts POST /api/audit/agent-actions/:id/rollback dispatches via REVERSERS[toolName] — NOT the inlined Phase 2 if/else — and that unsupported tools still 400"
    - "reconciliation.test.ts asserts GET /api/cash/reconciliation returns 4 age buckets (0-3d, 3-7d, 7-14d, 14d+) with totalGap (Decimal-safe, KD 3-decimals) AND filters out platform=AMERICANA per CON-cash-platform-coverage"
    - "payrollComputer.test.ts asserts a driver with 3 EFFECTIVE Penalty rows (penaltyValue='2.500','3.000','1.500') in the pay window gets a deduction line of KD 7.000 on the payslip; pay window boundary respected (Penalty.createdAt < windowStart NOT included)"
    - "bankFileExporter.test.ts asserts a 300-row export streams in <500ms peak heap delta and the resulting buffer parses back to 300 rows with header line IBAN,Amount,Reference"
    - "pnlAggregator.test.ts asserts /api/finance/pnl returns { months: 3 rows ascending, quarter: 1 row } for Q2 2026 with revenue + expense sums per platform"
    - "5 frontend RED tests exist (FinanceLayout role gate, ReconciliationGrid render, PayrollRunTable render, PnlChart Recharts shape, ActionHistoryTable filters)"
    - "lint:tenant glob extended to include backend/src/routes/finance*.ts + backend/src/services/finance/ + the 8 new tool files under backend/src/agent/tools/action/"
  artifacts:
    - path: backend/src/__tests__/agent/tools/action/applyPenalty.test.ts
      provides: "RED tests for applyPenalty: tenant-scoped Driver lookup, Penalty.create with EFFECTIVE status, optional violations.connect, ACCOUNT_SUSPENSION cascade hook, throws on cross-tenant driver"
      contains: "applyPenalty"
    - path: backend/src/__tests__/agent/tools/action/suspendDriver.test.ts
      provides: "RED tests: Driver.status -> SUSPENDED inside prisma.$transaction, Shift.updateMany for future BOOKED -> CANCELLED, DriverRestriction.create with TEMPORARY type and computed endDate"
      contains: "suspendDriver"
    - path: backend/src/__tests__/agent/tools/action/reassignShift.test.ts
      provides: "RED tests: Shift.driverId atomically reassigned, previousDriverId captured in tool output for rollback, refuses when new driver has overlapping shift"
      contains: "reassignShift"
    - path: backend/src/__tests__/agent/tools/action/recordCashSettlement.test.ts
      provides: "RED tests: CashRecord.collectionAmount + pendingDues + status=SETTLED in one tx with CashTransaction.create; Decimal-safe arithmetic; throws on already-SETTLED"
      contains: "recordCashSettlement"
    - path: backend/src/__tests__/agent/tools/action/sendCourierMessage.test.ts
      provides: "RED tests: chains from draftCourierMessage Notification id; enqueues NotificationDelivery channel=WHATSAPP status=QUEUED; idempotent on the messageId"
      contains: "sendCourierMessage"
    - path: backend/src/__tests__/agent/tools/action/createTrainingTask.test.ts
      provides: "RED tests: TrainingTask.create tenant-scoped; status=ASSIGNED; optional sourceViolationId link; rejects unknown trainingType"
      contains: "createTrainingTask"
    - path: backend/src/__tests__/agent/tools/action/escalateToHumanSupervisor.test.ts
      provides: "RED tests: Notification.create with severity=HIGH + category=IMPORTANT routed to all SUPERVISOR users of tenant"
      contains: "escalateToHumanSupervisor"
    - path: backend/src/__tests__/agent/tools/action/generatePayrollAdjustment.test.ts
      provides: "RED tests: PayrollAdjustment.create with items[] inside tx; refuses when target PayrollRun.status=FROZEN; ties to driverId+period unique"
      contains: "generatePayrollAdjustment"
    - path: backend/src/__tests__/agent/tools/action/applyPenalty.suspend-cascade.test.ts
      provides: "RED integration test: applyPenalty(penaltyType=ACCOUNT_SUSPENSION, penaltyValue='7') triggers Driver.update + Shift.updateMany + DriverRestriction.create — verify all three persisted on success and ALL rolled back if any one throws"
      contains: "ACCOUNT_SUSPENSION"
    - path: backend/src/__tests__/decisions/cardProjector.phase8.test.ts
      provides: "RED test: PHASE_8_LIVE_TOOLS Set exported from cardProjector.ts contains exactly the 10 named tools; toolIsLive = PHASE_2_LIVE_TOOLS.has(name) || PHASE_8_LIVE_TOOLS.has(name)"
      contains: "PHASE_8_LIVE_TOOLS"
    - path: backend/src/__tests__/decisions/dualApproval.phase8.test.ts
      provides: "RED concurrency test: two simultaneous POST /api/decisions/:id/approve calls on the same Penalty-class PendingAgentAction. Verifies optimistic-lock pattern (updateMany where {id, resolvedAt: null}) catches the race. First call returns 200; second returns 409 with no extra Penalty or AgentAction rows written."
      contains: "Already resolved"
    - path: backend/src/__tests__/services/finance/rollbackDispatcher.test.ts
      provides: "RED test asserting REVERSERS table shape and per-tool soft-rollback behaviour (NEVER prisma.*.delete; always status flips; cash + payroll throw 'requires manual reversal')"
      contains: "REVERSERS"
    - path: backend/src/__tests__/services/finance/reconciliation.test.ts
      provides: "RED test: bucketed gap report excludes AMERICANA; respects platform query filter; Decimal subtraction is precision-safe; empty result returns empty buckets array (NOT 500)"
      contains: "AMERICANA"
    - path: backend/src/__tests__/services/finance/payrollComputer.test.ts
      provides: "RED test: weekly pay window summing base + per-shift incentives - EFFECTIVE-penalty deductions; respects Penalty.createdAt boundary; excludes OVERTURNED penalties"
      contains: "payrollComputer"
    - path: backend/src/__tests__/services/finance/bankFileExporter.test.ts
      provides: "RED test: streamPayrollCsv writes header IBAN,Amount,Reference + N rows; 300-row run completes in <500ms; output parses back deterministically"
      contains: "streamPayrollCsv"
    - path: backend/src/__tests__/services/finance/pnlAggregator.test.ts
      provides: "RED test: aggregatePnl(month) returns {revenue: {byPlatform}, expenses: {byCategory}, net}; aggregatePnl(quarter) sums 3 months; tenant-scoped"
      contains: "aggregatePnl"
    - path: backend/src/__tests__/routes/cash.reconciliation.test.ts
      provides: "RED supertest: GET /api/cash/reconciliation?platform=KEETA returns 4 buckets; ?platform=AMERICANA returns 400 'Americana excluded per CON-cash-platform-coverage'"
      contains: "/reconciliation"
    - path: backend/src/__tests__/routes/financePayroll.test.ts
      provides: "RED supertest: POST /api/finance/payroll/runs creates PayrollRun (DRAFT); POST /:id/freeze flips FROZEN; GET /:id/export streams CSV; RBAC = ADMIN|ACCOUNTANT"
      contains: "/api/finance/payroll"
    - path: backend/src/__tests__/routes/financeInvoices.test.ts
      provides: "RED supertest: GET /api/finance/invoices lists per-platform; ?platform=AMERICANA INCLUDED (invoices cover all 4 platforms per Open Question #3)"
      contains: "/api/finance/invoices"
    - path: backend/src/__tests__/routes/financeExpenses.test.ts
      provides: "RED supertest: POST /api/finance/expenses creates Expense (category enum); GET lists tenant-scoped + paginated"
      contains: "/api/finance/expenses"
    - path: backend/src/__tests__/routes/financePnl.test.ts
      provides: "RED supertest: GET /api/finance/pnl?period=month&from=2026-04-01 returns month rollup; ?period=quarter returns quarter rollup"
      contains: "/api/finance/pnl"
    - path: backend/src/__tests__/routes/auditRollback.phase8.test.ts
      provides: "RED supertest: POST /api/audit/agent-actions/:id/rollback for applyPenalty audit row -> Penalty.penaltyStatus=OVERTURNED + audit.rolledBackAt set; for recordCashSettlement -> 400 'requires manual reversal'"
      contains: "OVERTURNED"
    - path: frontend/src/__tests__/finance/FinanceLayout.test.tsx
      provides: "RED test: /finance/* layout redirects non-ACCOUNTANT/non-ADMIN users to /decisions; ACCOUNTANT lands on /finance/cash by default"
      contains: "FinanceLayout"
    - path: frontend/src/__tests__/finance/ReconciliationGrid.test.tsx
      provides: "RED test: renders 4 age-bucket cards (0-3d, 3-7d, 7-14d, 14d+) with totalGap formatted as 'KD X.XXX' (3-decimals)"
      contains: "0-3d"
    - path: frontend/src/__tests__/finance/PayrollRunTable.test.tsx
      provides: "RED test: table renders driver rows with base + deductions + net; Freeze button only enabled for DRAFT status; Export CSV button only enabled for FROZEN"
      contains: "Freeze"
    - path: frontend/src/__tests__/finance/PnlChart.test.tsx
      provides: "RED test: Recharts BarChart receives month-axis data + revenue/expense series; uses Sierra palette tokens (sand-* / primary / talabat / keeta)"
      contains: "BarChart"
    - path: frontend/src/__tests__/finance/ActionHistoryTable.test.tsx
      provides: "RED test: /finance/history renders AgentAction rows filtered by toolName=Phase8 tool set; Rollback button visible only for rollback-eligible tools per dispatcher"
      contains: "rollback-eligible"
    - path: backend/package.json
      provides: "lint:tenant script scope expanded to cover the 8 new tool files + services/finance/ + routes/finance*.ts"
      contains: "src/services/finance"
  key_links:
    - from: backend/src/__tests__/decisions/cardProjector.phase8.test.ts
      to: backend/src/services/decisions/cardProjector.ts
      via: "imports PHASE_2_LIVE_TOOLS + PHASE_8_LIVE_TOOLS exports; toolIsLive is union check"
      pattern: "PHASE_8_LIVE_TOOLS"
    - from: backend/src/__tests__/services/finance/rollbackDispatcher.test.ts
      to: backend/src/services/finance/rollbackDispatcher.ts
      via: "imports REVERSERS table; asserts shape"
      pattern: "REVERSERS"
    - from: backend/src/__tests__/routes/auditRollback.phase8.test.ts
      to: backend/src/routes/audit.ts
      via: "Wave 4 will refactor inline Phase 2 if/else into a REVERSERS dispatch"
      pattern: "REVERSERS\\[audit\\.toolName\\]"
    - from: backend/package.json
      to: backend/src/services/finance
      via: "lint:tenant glob includes src/services/finance/ + src/routes/finance*.ts + Phase-8 tool files"
      pattern: "src/services/finance"

threat_model:
  trust_boundaries: []  # Wave 0 ships tests + lint scope only — no production code, no new trust boundaries
  threats: []           # Test scaffolding poses no STRIDE threat surface
---

<objective>
Wave 0 — Build the safety net BEFORE any Phase 8 production code lands. Phase 8 is the moat phase: 8 new write tools, a per-tool rollback dispatcher, a 5-tab accountant workbench, and the cardProjector promotion that flips 7 audit-only proposals into live side effects. Every one of those surfaces must have a RED test waiting before the implementing wave starts. This plan ships:

1. **8 RED tool test files** — one per new action tool: `applyPenalty`, `suspendDriver`, `reassignShift`, `recordCashSettlement`, `sendCourierMessage`, `createTrainingTask`, `escalateToHumanSupervisor`, `generatePayrollAdjustment`. Each asserts the tenant-scoped Driver/CashRecord/Shift lookup, the `prisma.$transaction` atomicity (mock `prisma.$transaction` and verify all mutations run inside its callback), the Zod input contract, and the structured output (`{ ok, ...idsOfSpawnedEntities }`).
2. **1 RED integration test** for the `applyPenalty(penaltyType=ACCOUNT_SUSPENSION)` cascade — proves Driver/Shift/DriverRestriction mutations all roll back together if any one throws.
3. **2 RED decision-surface tests** — `cardProjector.phase8.test.ts` asserts `PHASE_8_LIVE_TOOLS` Set shape and the union semantics with `PHASE_2_LIVE_TOOLS`; `dualApproval.phase8.test.ts` re-runs the Phase 2 T-02-14 optimistic-lock race against a Penalty-class card.
4. **5 RED finance-service tests** — `rollbackDispatcher` (per-tool REVERSERS table), `reconciliation` (age buckets, Decimal-safe, Americana excluded), `payrollComputer` (Penalty deductions inside pay window), `bankFileExporter` (300-row stream <500ms), `pnlAggregator` (month/quarter cuts).
5. **6 RED route tests** — `cash.reconciliation`, `financePayroll`, `financeInvoices`, `financeExpenses`, `financePnl`, `auditRollback.phase8` (per-tool dispatch verification).
6. **5 RED frontend tests** — `FinanceLayout` (role gate), `ReconciliationGrid`, `PayrollRunTable`, `PnlChart`, `ActionHistoryTable`.
7. **lint:tenant glob extension** — add `src/agent/tools/action/applyPenalty.ts`, `suspendDriver.ts`, `reassignShift.ts`, `recordCashSettlement.ts`, `sendCourierMessage.ts`, `createTrainingTask.ts`, `escalateToHumanSupervisor.ts`, `generatePayrollAdjustment.ts`, `src/services/finance/`, and `src/routes/finance*.ts` to the lint:tenant glob. The runtime `prismaExtensions.ts` guard is a safety net; the static lint rule is the gate.

Per the Nyquist rule (`<verify>` MUST include `<automated>`), every Wave 1-4 task has a failing test waiting for it. Wave 0 produces the failing tests; Waves 1-4 turn them green.

Purpose: Phase 8 mutates money + status + schedule data. Every regression must be caught before it reaches design partner #1's books. The dual-approval race (T-02-14) is the most consequential pitfall — re-asserting it against the Phase 8 tool set is non-negotiable. The rollback dispatcher is the second moat: every soft-rollback path is tested before audit.ts is refactored away from the inline Phase 2 if/else.

Output: 28 RED test files (22 backend + 6 frontend — see `files_modified`) + extended `lint:tenant` glob. `cd backend && npx jest --testPathPattern='(agent/tools/action|decisions/(cardProjector|dualApproval)\.phase8|services/finance|routes/(cash\.reconciliation|finance|auditRollback))'` exits non-zero (RED). `cd frontend && npm test -- src/__tests__/finance` exits non-zero (RED). `cd backend && npm run lint:tenant` exits 0 (the new globs match no files yet; `--no-error-on-unmatched-pattern` keeps the gate green until Wave 1+ ships the source files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-finance-workbench/08-RESEARCH.md
@.planning/intel/constraints.md

# Existing patterns the executor will mirror
@backend/jest.config.js
@backend/package.json
@backend/src/__tests__/agent/tools/tenantIsolation.test.ts
@backend/src/__tests__/agent/ledger.test.ts
@backend/src/__tests__/decisions/approveFlow.test.ts
@backend/src/__tests__/decisions/cardProjector.test.ts
@backend/src/__tests__/mocks/agentMocks.ts
@backend/src/__tests__/mocks/config.ts

# Existing files Phase 8 wraps (do NOT modify in Wave 0)
@backend/src/agent/registry.ts
@backend/src/agent/ledger.ts
@backend/src/agent/tools/action/draftCourierMessage.ts
@backend/src/agent/tools/action/flagForReview.ts
@backend/src/agent/tools/action/proposeCashReminder.ts
@backend/src/agent/tools/action/index.ts
@backend/src/services/decisions/cardProjector.ts
@backend/src/routes/decisions.ts
@backend/src/routes/audit.ts
@backend/src/routes/cash.ts
@backend/src/routes/penalties.ts
@backend/prisma/schema.prisma
@backend/src/config/prismaExtensions.ts
@frontend/vitest.config.ts
@frontend/src/__tests__/decisions/DecisionsList.test.tsx

<interfaces>
<!-- Existing Phase-1/Phase-2 contracts the RED tests assert against. Phase 8 inherits these verbatim. -->

From backend/src/agent/registry.ts:
```typescript
export interface ToolContext {
  tenantId: string;
  agentId: string;
  runId: string;
  actorRole: UserRole;
  userId?: string;  // set ONLY when a human approves
}
export interface ToolDefinition<I, O> {
  name: string;
  description: string;
  inputSchema: Anthropic.Tool["input_schema"];
  inputValidator: z.ZodType<I>;
  sideEffect: "read" | "write" | "notify";
  requiredRole: UserRole[];
  requiresApproval: boolean;
  allowedAgents: string[];
  editableParams?: string[];
  strict?: boolean;
  execute: (ctx: ToolContext, input: I) => Promise<O>;
}
```

From backend/src/services/decisions/cardProjector.ts:
```typescript
export const PHASE_2_LIVE_TOOLS: ReadonlySet<string> = new Set(["draftCourierMessage"]);
// Phase 8 ADDS (do not remove the Phase 2 export):
//   export const PHASE_8_LIVE_TOOLS: ReadonlySet<string> = new Set([
//     "draftCourierMessage", "sendCourierMessage", "applyPenalty", "suspendDriver",
//     "reassignShift", "flagForReview", "createTrainingTask", "recordCashSettlement",
//     "escalateToHumanSupervisor", "generatePayrollAdjustment",
//   ]);
// toolIsLive = PHASE_2_LIVE_TOOLS.has(name) || PHASE_8_LIVE_TOOLS.has(name)
```

From backend/src/routes/decisions.ts (Phase 2 optimistic-lock pattern Phase 8 inherits verbatim):
```typescript
const claim = await prisma.pendingAgentAction.updateMany({
  where: { id: pa.id, resolvedAt: null } as any,
  data: { resolvedAt: new Date(), resolution: "approved", resolvedBy: userId },
});
if (claim.count !== 1) return res.status(409).json({ error: "Already resolved" });
```

From backend/prisma/schema.prisma (existing models Phase 8 reads/writes — see schema.prisma:780-825 for CashRecord+CashTransaction; :675-714 for Shift; :580-598 for DriverRestriction; :1766-1813 for Violation+Penalty; :2258-2298 for AgentAction):
- Driver.status: ACTIVE | INACTIVE | SUSPENDED (+ others per schema)
- Shift.status: BOOKED | ACTIVE | COMPLETED | CANCELLED | NO_SHOW
- CashRecord.status: PENDING | PARTIAL | SETTLED (per CashStatus enum)
- Penalty.penaltyStatus: EFFECTIVE | COMPLETED | OVERTURNED (String, default EFFECTIVE)
- Penalty <-> Violation: implicit m2m via `violations Violation[]` on Penalty + `penalties Penalty[]` on Violation
- AgentAction.outcome: success | failure | rolled_back
- DriverRestriction.type: TEMPORARY | PERMANENT | DOCUMENT_HOLD (RestrictionType enum)
- AgentAction has rolledBackAt + rolledBackById + rollbackReason columns (Phase 2 added) — Phase 8 reuses
- Platform enum (schema.prisma:16): KEETA | TALABAT | DELIVEROO | AMERICANA — used by CashRecord filter

Wave 1 will ADD these 9 NEW models + 2 nullable Driver columns + 1 composite Penalty index (do NOT exist yet; Wave 0 tests will fail with "module not found" or "model not found" until the migration runs):
- TrainingTask — assignment tracker for createTrainingTask
- PayrollRun + PayrollLine + PayrollAdjustment — payroll computation primitives
- Invoice + InvoiceLine — incoming platform invoices (4 platforms); Invoice gains a status='ISSUED' value gating P&L revenue inclusion (W3)
- Expense — fleet expense ledger (vehicle/fuel/rent/utilities/salary categories)
- BankFile — export audit row pointing at the streamed CSV
- ReviewFlag — flagForReview side-effect target (W2 — flagForReview promoted to real side effect)
- Driver.contractedRateKd (Decimal?(10,3)) + Driver.iban (String?) — additive nullable columns enabling payroll computation + bank-file IBAN field

**NotificationDelivery is REUSED, not redeclared.** It already exists at schema.prisma:1688-1712 with the shape `{recipient, body, channel: NotificationChannel, status: NotificationDeliveryStatus enum QUEUED|SENDING|SENT|FAILED|DEAD, idempotencyKey, sourceType, sourceId, ...}`. sendCourierMessage writes to this existing model. CANCELLED is NOT a valid enum value — the Wave 4 reverser uses a different soft-rollback strategy (Notification.metadata flag, not the NotificationDelivery.status enum).

The Wave-0 tests that reference these models will fail with Prisma type-errors until Wave 1's `prisma migrate dev` runs. That's the dedicated migration verification gate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 16 backend tool + service RED tests</name>
  <files>
    backend/src/__tests__/agent/tools/action/applyPenalty.test.ts,
    backend/src/__tests__/agent/tools/action/suspendDriver.test.ts,
    backend/src/__tests__/agent/tools/action/reassignShift.test.ts,
    backend/src/__tests__/agent/tools/action/recordCashSettlement.test.ts,
    backend/src/__tests__/agent/tools/action/sendCourierMessage.test.ts,
    backend/src/__tests__/agent/tools/action/createTrainingTask.test.ts,
    backend/src/__tests__/agent/tools/action/escalateToHumanSupervisor.test.ts,
    backend/src/__tests__/agent/tools/action/generatePayrollAdjustment.test.ts,
    backend/src/__tests__/agent/tools/action/applyPenalty.suspend-cascade.test.ts,
    backend/src/__tests__/decisions/cardProjector.phase8.test.ts,
    backend/src/__tests__/decisions/dualApproval.phase8.test.ts,
    backend/src/__tests__/services/finance/rollbackDispatcher.test.ts,
    backend/src/__tests__/services/finance/reconciliation.test.ts,
    backend/src/__tests__/services/finance/payrollComputer.test.ts,
    backend/src/__tests__/services/finance/bankFileExporter.test.ts,
    backend/src/__tests__/services/finance/pnlAggregator.test.ts
  </files>
  <read_first>
    backend/src/__tests__/agent/tools/tenantIsolation.test.ts (Phase-1 two-tenant fixture pattern — Phase 8 tool tests mirror this exactly to prove cross-tenant rejection),
    backend/src/__tests__/agent/ledger.test.ts (writeAgentAction mock + assertion pattern),
    backend/src/__tests__/decisions/approveFlow.test.ts (Phase 2's T-02-14 dual-approval race — Phase 8 copies the structure for the Penalty-class card),
    backend/src/__tests__/decisions/cardProjector.test.ts (existing PHASE_2_LIVE_TOOLS assertion),
    backend/src/__tests__/mocks/agentMocks.ts (jest.mock("../../config", () => ({ prisma: ... })) pattern + prisma mock surface — Phase 8 extends with prisma.penalty, prisma.driverRestriction, prisma.cashTransaction, prisma.notificationDelivery, prisma.trainingTask, prisma.payrollRun, prisma.payrollAdjustment, prisma.invoice, prisma.expense, prisma.bankFile, prisma.$transaction mock),
    backend/src/agent/tools/action/draftCourierMessage.ts (canonical live-tool shape — Phase 8 tools mirror this exactly),
    backend/src/agent/tools/action/flagForReview.ts (audit-only stub pattern),
    backend/src/agent/tools/action/proposeCashReminder.ts (audit-only stub Phase 8 supersedes with recordCashSettlement),
    backend/src/agent/registry.ts (defineTool + ToolContext + invoke contract),
    backend/src/agent/ledger.ts (writeAgentAction signature),
    backend/src/services/decisions/cardProjector.ts (PHASE_2_LIVE_TOOLS export + toolIsLive logic — Phase 8 adds PHASE_8_LIVE_TOOLS export and the UNION),
    backend/src/routes/decisions.ts (approve route optimistic-lock pattern lines 344-356 per RESEARCH.md Pitfall 1),
    backend/src/routes/audit.ts (Phase 2 inline rollback handler lines 163-249 — Phase 8 Wave 4 refactors this into the REVERSERS dispatch),
    backend/src/routes/cash.ts (existing cash routes + pagination helpers Phase 8 reuses for /reconciliation),
    backend/src/routes/penalties.ts (existing manual POST /api/penalties — Phase 8 must NOT race with the agent path; the dualApproval test asserts isolation),
    backend/prisma/schema.prisma (lines 780-825 CashRecord+CashTransaction; lines 675-714 Shift; lines 580-598 DriverRestriction; lines 1766-1813 Violation+Penalty m2m; lines 2225-2298 PendingAgentAction+AgentAction — read EXACT column names so test mocks line up with Wave 1+ implementations),
    .planning/phases/08-finance-workbench/08-RESEARCH.md (Pattern 1-3, Pitfalls 1-6, Code Examples, RBAC table — every test pulls its assertion shape from this file)
  </read_first>
  <action>
Create 16 RED Jest test files under `backend/src/__tests__/`.

**RED-discipline rule:** Each test runs a real expectation. Imports of files that don't exist (e.g. `../../../../agent/tools/action/applyPenalty`) cause Jest module-not-found failures — that IS the RED state. Do NOT use `it.todo` or `describe.skip`. End each describe block with a comment `// RED — turned GREEN by Wave N` so reviewers see intent.

**Common mock harness (every test file mirrors this preamble):**

```typescript
jest.mock("../../../config", () => ({
  prisma: {
    driver: { findFirst: jest.fn(), update: jest.fn(), updateMany: jest.fn() },
    penalty: { create: jest.fn(), updateMany: jest.fn(), findFirst: jest.fn() },
    violation: { findFirst: jest.fn() },
    driverRestriction: { create: jest.fn() },
    shift: { update: jest.fn(), updateMany: jest.fn(), findMany: jest.fn() },
    cashRecord: { findFirst: jest.fn(), update: jest.fn() },
    cashTransaction: { create: jest.fn() },
    pendingAgentAction: { updateMany: jest.fn(), findFirst: jest.fn() },
    agentAction: { create: jest.fn(), findFirst: jest.fn(), updateMany: jest.fn(), update: jest.fn() },
    notification: { create: jest.fn(), updateMany: jest.fn() },
    notificationDelivery: { create: jest.fn(), findFirst: jest.fn() },
    trainingTask: { create: jest.fn(), updateMany: jest.fn() },
    payrollRun: { findFirst: jest.fn(), create: jest.fn(), update: jest.fn() },
    payrollAdjustment: { create: jest.fn() },
    invoice: { findMany: jest.fn(), count: jest.fn() },
    expense: { create: jest.fn(), findMany: jest.fn(), count: jest.fn() },
    bankFile: { create: jest.fn() },
    user: { findMany: jest.fn() },
    $transaction: jest.fn(async (cb: any) => cb({
      // tx delegates re-use the prisma jest.fn mocks — set up per-test
      // so the same assertions work whether the code uses prisma.x or tx.x
    })),
  },
}));
```

**Test file specs (each describe block follows `describe("Phase 8 / REQ-X: <surface>", ...)` per Phase-2 convention):**

1. **`agent/tools/action/applyPenalty.test.ts`** — `describe("Phase 8 / REQ-agent-action-tools: applyPenalty")` with 7 RED tests:
   - "registers in toolRegistry with name='applyPenalty', sideEffect='write', requiresApproval=true, requiredRole includes ADMIN/OPS_MANAGER/ACCOUNTANT"
   - "Zod rejects when reason is <20 chars; rejects unknown penaltyType"
   - "execute(ctx, input) calls prisma.driver.findFirst({where:{id, tenantId:ctx.tenantId}}); throws 'Driver not in tenant scope' when null"
   - "execute creates Penalty row with penaltyStatus='EFFECTIVE', driverId from input, tenantId from ctx; connects sourceViolationId via violations.connect when provided"
   - "execute wraps the body in prisma.$transaction; mock $transaction to throw inside callback and assert NO Penalty row is created (atomicity)"
   - "execute returns { ok: true, penaltyId: string, penaltyType: enum-value } shape — Wave 2 audit row uses penaltyId as subjectId"
   - "execute with penaltyType='ACCOUNT_SUSPENSION' delegates the suspend cascade (driver.update + shift.updateMany + driverRestriction.create); see applyPenalty.suspend-cascade.test.ts for the integration assertion"

2. **`agent/tools/action/suspendDriver.test.ts`** — `describe("Phase 8 / REQ-agent-action-tools: suspendDriver")` with 6 RED tests:
   - "registers with sideEffect='write', requiresApproval=true, requiredRole=['ADMIN','OPS_MANAGER']"
   - "Zod rejects durationDays<1 or >365; rejects reason<20 chars"
   - "execute flips Driver.status='SUSPENDED' tenant-scoped"
   - "execute calls Shift.updateMany where {driverId, scheduledStart: {gte: now}, status:'BOOKED'} -> {status:'CANCELLED'} (cascades future shifts only)"
   - "execute creates DriverRestriction with type='TEMPORARY', startDate=now, endDate=now+durationDays*86400000, reason=input.reason"
   - "throws when Driver not in tenant scope; prisma.$transaction wrapper guarantees all three writes commit or none do"

3. **`agent/tools/action/reassignShift.test.ts`** — `describe("Phase 8 / REQ-agent-action-tools: reassignShift")` with 5 RED tests:
   - "Zod requires shiftId, newDriverId, reason; rejects when newDriverId === currentDriverId"
   - "execute loads Shift tenant-scoped + captures previousDriverId in tool output (rollback needs it)"
   - "refuses when newDriver has an overlapping BOOKED Shift (returns { ok:false, error:'Driver already booked at that time' })"
   - "execute Shift.update {driverId: newDriverId} inside prisma.$transaction"
   - "returns { ok: true, shiftId, previousDriverId, newDriverId } — Wave 4 rollback REVERSERS.reassignShift reads previousDriverId from originalProposal"

4. **`agent/tools/action/recordCashSettlement.test.ts`** — `describe("Phase 8 / REQ-agent-action-tools: recordCashSettlement")` with 6 RED tests:
   - "Zod amountKd is Decimal-safe (zod number >=0.001); method in {CASH, BANK_TRANSFER, MOBILE_WALLET}"
   - "execute loads CashRecord tenant-scoped; throws 'Already settled' when status='SETTLED'"
   - "execute updates CashRecord {collectionAmount: input.amountKd, pendingDues: max(0, salesAmount - amountKd), status: 'SETTLED', notes: input.note ?? prior}"
   - "execute creates CashTransaction {tenantId, driverId, type:'COLLECTION', amount, description: `Settled by ${ctx.userId} via Workbench`}"
   - "both writes happen inside prisma.$transaction; throwing inside cashTransaction.create rolls back the CashRecord.update (atomic books) — proven by mocking $transaction to throw and asserting cashRecord.update was NOT committed"
   - "Decimal arithmetic: salesAmount=10.500 - amountKd=3.250 -> pendingDues=7.250 (string assertion to avoid float drift)"

5. **`agent/tools/action/sendCourierMessage.test.ts`** — `describe("Phase 8 / REQ-agent-action-tools: sendCourierMessage")` with 5 RED tests. Writes to the EXISTING `NotificationDelivery` model (schema.prisma:1688-1712) — NOT a new model:
   - "Zod requires notificationId (from a prior draftCourierMessage Notification.id) or {driverId + bodyEnglish} pair"
   - "execute loads Notification tenant-scoped (when notificationId provided); throws on cross-tenant"
   - "execute enqueues NotificationDelivery using the EXISTING shape: {tenantId, channel:'WHATSAPP', recipient: driver.phone, body, status:'QUEUED', idempotencyKey: notificationId, sourceType:'agentRunId', sourceId: ctx.runId} — Phase 9 worker will drain QUEUED rows"
   - "execute is idempotent via the existing `idempotencyKey @unique` constraint — a second invocation with the same notificationId hits the unique-violation path and returns the existing deliveryId (no duplicate row)"
   - "execute returns { ok: true, deliveryId, channel:'WHATSAPP' }"

6. **`agent/tools/action/createTrainingTask.test.ts`** — `describe("Phase 8 / REQ-agent-action-tools: createTrainingTask")` with 4 RED tests:
   - "Zod requires driverId, trainingType (enum: GENERIC | LATE_CLOCKIN | GPS | CASH_HANDLING | ORDER_REJECTION); rejects unknown values"
   - "execute creates TrainingTask {tenantId, driverId, trainingType, status:'ASSIGNED', sourceViolationId: input.sourceViolationId ?? null, dueDate: now+7d}"
   - "execute loads Driver tenant-scoped; throws on cross-tenant"
   - "execute returns { ok: true, trainingTaskId, dueDate }"

7. **`agent/tools/action/escalateToHumanSupervisor.test.ts`** — `describe("Phase 8 / REQ-agent-action-tools: escalateToHumanSupervisor")` with 5 RED tests:
   - "Zod requires driverId, reason (min 20 chars), maxRecipients (number, default 25, hard max 100); rejects maxRecipients<1 or maxRecipients>100"
   - "execute fans out a Notification {category:'IMPORTANT', severity:'HIGH', title:`Escalation: ${driver.name}`, message:reason} to up to input.maxRecipients users of the tenant where role IN ('SUPERVISOR','OPS_MANAGER','ADMIN'), ordered by createdAt asc"
   - "when input.maxRecipients is omitted, defaults to 25 (matches T-08-W1-06 mitigation)"
   - "execute returns { ok: true, notificationIds: string[], recipientCount: number }"
   - "tenant isolation: a tenant-B supervisor does NOT receive the notification"

8. **`agent/tools/action/generatePayrollAdjustment.test.ts`** — `describe("Phase 8 / REQ-agent-action-tools: generatePayrollAdjustment")` with 5 RED tests:
   - "Zod requires driverId, period (YYYY-Www string), items: array of {label, amountKd: Decimal-safe, kind: 'DEDUCTION'|'BONUS'}"
   - "execute loads PayrollRun for {tenantId, period}; refuses when PayrollRun.status='FROZEN' (returns {ok:false, error:'Run is frozen; rollback via accountant counter-entry'})"
   - "execute creates PayrollAdjustment {tenantId, driverId, runId, items: Json} inside prisma.$transaction"
   - "execute computes sumDeductions - sumBonuses and asserts non-negative net adjustment unless explicitly overridden"
   - "returns { ok: true, adjustmentId, netKd }"

9. **`agent/tools/action/applyPenalty.suspend-cascade.test.ts`** — `describe("Phase 8 / REQ-agent-action-tools: applyPenalty ACCOUNT_SUSPENSION cascade")` with 3 RED integration tests:
   - "happy path: applyPenalty {penaltyType:'ACCOUNT_SUSPENSION', penaltyValue:'7', reason: 'three strikes documented'} -> Penalty row + Driver.status='SUSPENDED' + Shift.updateMany ran + DriverRestriction.create ran with endDate ~7 days from now"
   - "atomicity: mock DriverRestriction.create to throw; assert NO Penalty row + Driver.status unchanged + Shift.updateMany rolled back — proves all four mutations live inside ONE prisma.$transaction (Pitfall 4 mitigation)"
   - "penaltyValue parsing: penaltyValue='14' -> endDate ~14 days; missing penaltyValue -> defaults to 7 days per RESEARCH.md Code Examples"

10. **`decisions/cardProjector.phase8.test.ts`** — `describe("Phase 8 / REQ-agent-action-tools: PHASE_8_LIVE_TOOLS projection")` with 4 RED tests:
    - "PHASE_8_LIVE_TOOLS Set contains exactly: draftCourierMessage, sendCourierMessage, applyPenalty, suspendDriver, reassignShift, flagForReview, createTrainingTask, recordCashSettlement, escalateToHumanSupervisor, generatePayrollAdjustment"
    - "PHASE_2_LIVE_TOOLS Set is unchanged (still contains only draftCourierMessage) — backwards compat"
    - "projectPendingAction sets toolIsLive=true when PHASE_2_LIVE_TOOLS.has(name) OR PHASE_8_LIVE_TOOLS.has(name) — verified for all 10 Phase 8 tool names"
    - "projectPendingAction sets toolIsLive=false for an unknown tool name (defensive default)"

11. **`decisions/dualApproval.phase8.test.ts`** — `describe("Phase 8 / REQ-agent-action-tools: dual-approval race (T-02-14 extended)")` with 2 RED tests:
    - "two simultaneous POST /api/decisions/:id/approve calls on the same applyPenalty-class PendingAgentAction. The first request returns 200 + writes ONE Penalty row + ONE AgentAction row. The second request returns 409 (Already resolved). Assert prisma.penalty.create call count === 1 and prisma.agentAction.create call count === 1 across both requests."
    - "T-02-14 invariant holds for recordCashSettlement-class card too (only one CashRecord update + one CashTransaction row)"
    - Use supertest with two parallel `app.inject` / `request(app)` calls and `Promise.all` to fire them concurrently; the optimistic-lock pattern (lines 344-356 of routes/decisions.ts) returns count=0 to the second writer.

12. **`services/finance/rollbackDispatcher.test.ts`** — `describe("Phase 8 / REQ-agent-action-tools: REVERSERS table")` with 9 RED tests (one per reversible tool + table-shape assertion):
    - "REVERSERS is an object with keys exactly: draftCourierMessage, applyPenalty, suspendDriver, reassignShift, sendCourierMessage, createTrainingTask, escalateToHumanSupervisor, recordCashSettlement, generatePayrollAdjustment"
    - "REVERSERS.applyPenalty(audit, tenantId, userId) -> prisma.penalty.updateMany {id: subjectId, tenantId} -> {penaltyStatus:'OVERTURNED'} (NEVER prisma.penalty.delete, per CON-audit-row-shape + RESEARCH.md anti-patterns)"
    - "REVERSERS.suspendDriver -> prisma.driver.updateMany {id, tenantId} -> {status:'ACTIVE'}"
    - "REVERSERS.reassignShift reads previousDriverId from audit.originalProposal -> prisma.shift.updateMany {id: shiftId, tenantId} -> {driverId: previousDriverId}; throws when previousDriverId absent (defensive)"
    - "REVERSERS.recordCashSettlement THROWS Error('Cash settlement rollback requires accountant manual reversal') — per Pitfall 3 (runningBalance corruption). W5 resolution: this is the canonical error message; the route's auditWindowOk path uses the SAME message + 409 status code so both code paths converge."
    - "REVERSERS.generatePayrollAdjustment checks PayrollRun.status; throws when FROZEN, otherwise marks PayrollAdjustment.status='REVERSED'"
    - "REVERSERS.sendCourierMessage soft-rolls the original Notification (when audit.agentRunId present) by setting Notification.type prefix to 'AGENT_SEND_ROLLED_BACK_'. The NotificationDelivery row is NOT mutated — the existing `NotificationDeliveryStatus` enum has no CANCELLED value (QUEUED|SENDING|SENT|FAILED|DEAD only). Phase 9 worker will see the parent Notification flag and skip the delivery. If NotificationDelivery has already SENT, the message is gone — rollback is documentation-only at that point."
    - "REVERSERS.createTrainingTask -> prisma.trainingTask.updateMany {id: subjectId, tenantId} -> {status:'CANCELLED'}"
    - "REVERSERS.escalateToHumanSupervisor is a no-op reverser (escalation is information-only; no entity to undo). Returns without throwing — audit.rolledBackAt still gets stamped by the caller."

13. **`services/finance/reconciliation.test.ts`** — `describe("Phase 8 / REQ-finance-cash-workbench: bucketed gap report")` with 6 RED tests:
    - "buildReconciliation({tenantId, today, platform:'KEETA'}) returns 4 buckets at thresholds 0, 3, 7, 14 days. Each bucket { threshold, count, totalGap }."
    - "totalGap = Σ(salesAmount - collectionAmount) for CashRecord.status IN (PENDING, PARTIAL); SETTLED rows excluded"
    - "Decimal-safe arithmetic: a single record with salesAmount=10.500, collectionAmount=3.250 produces gap=7.250 (string compare to avoid float drift)"
    - "platform='AMERICANA' returns empty buckets array (CON-cash-platform-coverage — Americana drivers don't carry cash); function does NOT 500"
    - "no platform filter -> rows across KEETA+TALABAT+DELIVEROO only (default filter excludes AMERICANA)"
    - "tenant isolation: tenant-A rows do not appear in tenant-B reconciliation"

14. **`services/finance/payrollComputer.test.ts`** — `describe("Phase 8 / REQ-finance-payroll: weekly run computation")` with 6 RED tests:
    - "computeWeeklyPay({tenantId, driverId, periodStart, periodEnd}) returns {base, incentives, deductions, net} all Decimal-safe"
    - "deductions sum EFFECTIVE Penalty rows where Penalty.createdAt >= periodStart AND < periodEnd; OVERTURNED penalties excluded"
    - "incentives sum Shift.bonus-or-equivalent during the window (use schema-actual column name; verify against schema.prisma during Wave 0 read)"
    - "base = Driver.contractedRateKd × actualHoursMinutes/60 (Decimal-safe)"
    - "net = base + incentives - deductions; if deductions > base+incentives, net is clamped at 0 (Kuwait labour-law cannot net-negative; flag for review in Wave 3)"
    - "tenant isolation: tenant-A penalties cannot deduct from tenant-B driver pay"

15. **`services/finance/bankFileExporter.test.ts`** — `describe("Phase 8 / REQ-finance-payroll: bank-file CSV stream")` with 4 RED tests:
    - "streamPayrollCsv(payrollRunId, tenantId) writes a header line 'IBAN,Amount,Reference' then one line per non-zero PayrollLine"
    - "300-line run completes within 500ms wall clock + <50MB peak heap delta (uses Node Transform stream OR papaparse stream API — both acceptable per A4)"
    - "output parses back deterministically: csv.parse(buffer) === input rows (length + each field)"
    - "tenant isolation: streaming a run for tenant-A never reads tenant-B PayrollLine rows; cross-tenant runId returns 404 not 200"

16. **`services/finance/pnlAggregator.test.ts`** — `describe("Phase 8 / REQ-finance-expenses-pl: month + quarter cuts")` with 5 RED tests:
    - "aggregatePnl({tenantId, period:'month', from:'2026-04-01'}) returns { revenue: { byPlatform: {KEETA, TALABAT, DELIVEROO, AMERICANA} }, expenses: { byCategory: {FUEL, MAINTENANCE, RENT, OFFICE_SALARY, UTILITIES, OTHER} }, net: Decimal }"
    - "aggregatePnl(period:'quarter') sums 3 months; from='2026-04-01' returns Q2 2026 totals"
    - "tenant isolation: aggregator never sums another tenant's rows"
    - "Decimal-safe (no float widening) — assertion against Prisma Decimal.toString() not toNumber()"
    - "empty tenant returns zero-valued shape (NOT undefined / null) — Recharts consumer in Wave 3 relies on stable keys"

**Existing tests guard:** Wave 0 must NOT modify any existing test file. The Phase-2 `decisions/cardProjector.test.ts` continues to assert `PHASE_2_LIVE_TOOLS` contains only `draftCourierMessage` — Phase 8's `cardProjector.phase8.test.ts` is the NEW file that asserts the union semantics.
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/Darb/backend && [[ $(ls src/__tests__/agent/tools/action/applyPenalty.test.ts src/__tests__/agent/tools/action/suspendDriver.test.ts src/__tests__/agent/tools/action/reassignShift.test.ts src/__tests__/agent/tools/action/recordCashSettlement.test.ts src/__tests__/agent/tools/action/sendCourierMessage.test.ts src/__tests__/agent/tools/action/createTrainingTask.test.ts src/__tests__/agent/tools/action/escalateToHumanSupervisor.test.ts src/__tests__/agent/tools/action/generatePayrollAdjustment.test.ts src/__tests__/agent/tools/action/applyPenalty.suspend-cascade.test.ts src/__tests__/decisions/cardProjector.phase8.test.ts src/__tests__/decisions/dualApproval.phase8.test.ts src/__tests__/services/finance/rollbackDispatcher.test.ts src/__tests__/services/finance/reconciliation.test.ts src/__tests__/services/finance/payrollComputer.test.ts src/__tests__/services/finance/bankFileExporter.test.ts src/__tests__/services/finance/pnlAggregator.test.ts 2>/dev/null | wc -l | tr -d ' ') -eq 16 ]] && cd /Users/mac/Documents/Darb/backend && npx jest --testPathPattern='(agent/tools/action/(applyPenalty|suspendDriver|reassignShift|recordCashSettlement|sendCourierMessage|createTrainingTask|escalateToHumanSupervisor|generatePayrollAdjustment)|decisions/(cardProjector|dualApproval)\.phase8|services/finance)' 2>&1 | grep -E "FAIL|Cannot find module" | head -1</automated>
  </verify>
  <done>
    16 backend RED test files exist (8 tool tests + 1 cascade integration + 2 decisions extensions + 5 finance services). `cd backend && npx jest --testPathPattern='(agent/tools/action/(applyPenalty|suspendDriver|reassignShift|recordCashSettlement|sendCourierMessage|createTrainingTask|escalateToHumanSupervisor|generatePayrollAdjustment)|decisions/(cardProjector|dualApproval)\.phase8|services/finance)'` exits non-zero (RED — modules don't yet exist). `cd backend && npm test -- --testPathPattern='(agent/walkingSkeleton|agent/ledger|agent/memory|decisions/approveFlow|decisions/cardProjector\.test)'` still GREEN (Phase 1+2 not regressed).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 6 backend route RED tests</name>
  <files>
    backend/src/__tests__/routes/cash.reconciliation.test.ts,
    backend/src/__tests__/routes/financePayroll.test.ts,
    backend/src/__tests__/routes/financeInvoices.test.ts,
    backend/src/__tests__/routes/financeExpenses.test.ts,
    backend/src/__tests__/routes/financePnl.test.ts,
    backend/src/__tests__/routes/auditRollback.phase8.test.ts
  </files>
  <read_first>
    backend/src/__tests__/decisions/approveFlow.test.ts (Phase 2 supertest pattern — Phase 8 routes mirror this),
    backend/src/routes/cash.ts (existing /api/cash routes Phase 8 extends with /reconciliation),
    backend/src/routes/audit.ts (existing /api/audit/agent-actions/:id/rollback handler Phase 8 refactors in Wave 4),
    backend/src/routes/decisions.ts (RBAC + tenantScope + authMiddleware composition pattern),
    backend/src/middleware/rbac.ts (rbac("ADMIN","ACCOUNTANT") helper),
    .planning/phases/08-finance-workbench/08-RESEARCH.md (RBAC table — every new route's role gate is sourced from there)
  </read_first>
  <action>
Create 6 RED supertest files following the Phase 2 pattern: spin up an Express app instance with the route mounted, inject mock auth (`req.user = {userId, tenantId, role}`), assert response shape + audit-row side effects.

**`routes/cash.reconciliation.test.ts`** — `describe("Phase 8 / REQ-finance-cash-workbench: GET /api/cash/reconciliation")` with 6 RED tests:
- "200 with 4 age buckets for ?platform=KEETA"
- "200 with 4 buckets across all 3 platforms when ?platform absent (default = KEETA+TALABAT+DELIVEROO)"
- "400 when ?platform=AMERICANA (excluded per CON-cash-platform-coverage)"
- "401 when no auth"
- "403 when role=SUPERVISOR (RBAC = ADMIN|ACCOUNTANT only)"
- "200 buckets respect tenant scope — tenant-B records do not appear in tenant-A response"

**`routes/financePayroll.test.ts`** — `describe("Phase 8 / REQ-finance-payroll: payroll lifecycle")` with 7 RED tests:
- "POST /api/finance/payroll/runs with {period:'2026-W17'} returns 201 + PayrollRun {id, status:'DRAFT', periodStart, periodEnd}"
- "GET /api/finance/payroll/runs/:id returns the run + lines array (one PayrollLine per active driver)"
- "POST /api/finance/payroll/runs/:id/freeze flips status DRAFT -> FROZEN; cannot freeze twice (409)"
- "GET /api/finance/payroll/runs/:id/export streams text/csv with header 'IBAN,Amount,Reference' + N body lines; Content-Disposition: attachment; filename=payroll-<period>.csv"
- "POST /api/finance/payroll/runs/:id/export creates a BankFile audit row (NOT a stored file — just the audit)"
- "RBAC: ACCOUNTANT can POST run; cannot freeze if role=ADMIN-only-action; verify final RBAC table from RESEARCH.md"
- "tenant isolation: cross-tenant runId returns 404"

**`routes/financeInvoices.test.ts`** — `describe("Phase 8 / REQ-finance-invoices: per-platform invoices")` with 5 RED tests:
- "GET /api/finance/invoices returns paginated Invoice[] across all 4 platforms (Americana INCLUDED per Open Question #3)"
- "?platform=AMERICANA returns rows; ?status=UNRECONCILED filters"
- "POST /api/finance/invoices/:id/reconcile flips status UNRECONCILED -> RECONCILED + writes BankReceiptId reference"
- "RBAC = ADMIN|ACCOUNTANT for POST; ADMIN|ACCOUNTANT|VIEWER for GET"
- "tenant isolation across all endpoints"

**`routes/financeExpenses.test.ts`** — `describe("Phase 8 / REQ-finance-expenses-pl: expense ledger")` with 5 RED tests:
- "POST /api/finance/expenses {category:'FUEL', amountKd:25.500, vendor, dateIncurred} returns 201 + Expense row"
- "Zod rejects unknown category enum value"
- "GET /api/finance/expenses paginated + ?from + ?to + ?category filters"
- "DELETE /api/finance/expenses/:id soft-deletes (deletedAt timestamp) — preserves audit per CON-audit-row-shape"
- "RBAC = ADMIN|ACCOUNTANT"

**`routes/financePnl.test.ts`** — `describe("Phase 8 / REQ-finance-expenses-pl: P&L month + quarter")` with 4 RED tests:
- "GET /api/finance/pnl?period=month&from=2026-04-01 returns { revenue: { byPlatform: {...4 keys} }, expenses: { byCategory: {...6 keys} }, net }"
- "GET /api/finance/pnl?period=quarter&from=2026-04-01 returns Q2 2026 sums (3 months merged)"
- "?period=invalid returns 400"
- "tenant isolation enforced"

**`routes/auditRollback.phase8.test.ts`** — `describe("Phase 8 / REQ-agent-action-tools: rollback dispatch via REVERSERS")` with 9 RED tests:
- "POST /api/audit/agent-actions/:id/rollback for applyPenalty audit row -> Penalty.penaltyStatus='OVERTURNED' + AgentAction.rolledBackAt set + outcome='rolled_back'"
- "...for suspendDriver -> Driver.status='ACTIVE' + audit.rolledBackAt set"
- "...for reassignShift reads previousDriverId from audit.originalProposal -> Shift.driverId reverted"
- "...for recordCashSettlement -> 409 'Cash settlement rollback requires accountant manual reversal' (CashRecord NOT mutated, audit.rolledBackAt NOT set) — W5 resolution: 409 not 400, canonical message from dispatcher"
- "...for generatePayrollAdjustment where PayrollRun.status='FROZEN' -> 409 with explanatory message (W5 — 409 for semantic refusal, 400 for unsupported tool)"
- "...for createTrainingTask -> TrainingTask.status='CANCELLED'"
- "...for sendCourierMessage -> original Notification.type prefix flipped to 'AGENT_SEND_ROLLED_BACK_' (W1/B1 — NotificationDeliveryStatus enum has no CANCELLED value; Notification.metadata flag is the rollback signal Phase 9 worker honours)"
- "...for escalateToHumanSupervisor -> audit.rolledBackAt stamped but no entity mutated (information-only escalation)"
- "RBAC = ADMIN|OPS_MANAGER only; ACCOUNTANT cannot rollback even for ACCOUNTANT-approved actions (per RESEARCH.md RBAC table)"

**Existing tests guard:** Wave 0 must NOT touch `routes/cash.ts`, `routes/audit.ts`, `routes/decisions.ts` source files. The supertest fixture imports the (yet-to-exist) Wave 3 route handlers and fails with module-not-found until those land.
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/Darb/backend && [[ $(ls src/__tests__/routes/cash.reconciliation.test.ts src/__tests__/routes/financePayroll.test.ts src/__tests__/routes/financeInvoices.test.ts src/__tests__/routes/financeExpenses.test.ts src/__tests__/routes/financePnl.test.ts src/__tests__/routes/auditRollback.phase8.test.ts 2>/dev/null | wc -l | tr -d ' ') -eq 6 ]] && cd /Users/mac/Documents/Darb/backend && npx jest --testPathPattern='routes/(cash\.reconciliation|finance(Payroll|Invoices|Expenses|Pnl)|auditRollback\.phase8)' 2>&1 | grep -E "FAIL|Cannot find module" | head -1</automated>
  </verify>
  <done>
    6 route RED test files exist. `cd backend && npx jest --testPathPattern='routes/(cash\.reconciliation|finance(Payroll|Invoices|Expenses|Pnl)|auditRollback\.phase8)'` exits non-zero (RED). `cd backend && npm test -- --testPathPattern='routes/(audit|decisions|cash\.test)'` still GREEN (Phase 2 audit + decisions + Phase 1 cash routes unaffected).
  </done>
</task>

<task type="auto">
  <name>Task 3: Create 5 frontend RED tests for /finance/* surfaces</name>
  <files>
    frontend/src/__tests__/finance/FinanceLayout.test.tsx,
    frontend/src/__tests__/finance/ReconciliationGrid.test.tsx,
    frontend/src/__tests__/finance/PayrollRunTable.test.tsx,
    frontend/src/__tests__/finance/PnlChart.test.tsx,
    frontend/src/__tests__/finance/ActionHistoryTable.test.tsx
  </files>
  <read_first>
    frontend/vitest.config.ts (existing Vitest + jsdom config — extends `include` if needed),
    frontend/src/__tests__/setup.ts (existing @testing-library/jest-dom import),
    frontend/src/__tests__/decisions/DecisionsList.test.tsx (Phase 2 frontend component test pattern Phase 8 mirrors),
    frontend/src/__tests__/admin/DarbsReadReport.test.tsx (Phase 2 admin component test pattern),
    frontend/tailwind.config.ts (Sierra palette tokens: sand-*, primary, talabat, keeta — Phase 8 charts use these),
    frontend/src/components/shared/StatCard.tsx (existing card primitive Phase 8 ReconciliationGrid will wrap),
    frontend/src/components/decisions/DecisionsList.tsx (existing Phase 2 list pattern),
    frontend/next.config.mjs (Next.js 14 App Router config — /finance/* is under (dashboard) group),
    .planning/phases/08-finance-workbench/08-RESEARCH.md (Wave 4 layout snippet — role gate redirects non-ACCOUNTANT/ADMIN to /decisions; default tab is /finance/cash)
  </read_first>
  <action>
Create 5 RED Vitest + React Testing Library files under `frontend/src/__tests__/finance/`. Each test imports a component that DOES NOT YET EXIST — Vitest fails with module-not-found, which is the RED state until Waves 2-4 ship the components.

**`FinanceLayout.test.tsx`** — `describe("Phase 8 / REQ-finance-cash-workbench: FinanceLayout role gate")` with 4 RED tests:
- "ACCOUNTANT session renders children + LeftRail with 5 nav items (Cash / Payroll / Invoices / Expenses / History)"
- "ADMIN session renders children (Admin can view all finance pages)"
- "OPS_MANAGER session triggers redirect to /decisions (mock next/navigation redirect; assert it was called with '/decisions')"
- "VIEWER session: layout renders BUT LeftRail action items (Freeze Payroll, Export CSV) are hidden — read-only audit access only"
- Imports `FinanceLayout` from `@/app/(dashboard)/finance/layout` (does not exist yet — RED).
- Mocks `@/lib/auth::getServerSession`.

**`ReconciliationGrid.test.tsx`** — `describe("Phase 8 / REQ-finance-cash-workbench: ReconciliationGrid")` with 5 RED tests:
- "renders 4 age-bucket cards labeled '0-3d', '3-7d', '7-14d', '14d+'"
- "each card formats totalGap as 'KD X.XXX' (3 decimal places — KD = 1000 fils per RESEARCH.md)"
- "platform filter dropdown shows KEETA / TALABAT / DELIVEROO ONLY (Americana absent per CON-cash-platform-coverage)"
- "clicking a bucket opens a side-drawer listing CashRecord rows under that bucket"
- "loading state shows 4 skeleton cards; error state shows ErrorState component"
- Imports `ReconciliationGrid` from `@/components/finance/cash/ReconciliationGrid` (RED).

**`PayrollRunTable.test.tsx`** — `describe("Phase 8 / REQ-finance-payroll: PayrollRunTable")` with 6 RED tests:
- "renders one row per PayrollLine with columns: Driver, Base (KD), Incentives (KD), Deductions (KD), Net (KD)"
- "all KD amounts render with 3-decimal precision"
- "Freeze button enabled when run.status=='DRAFT'; disabled with tooltip otherwise"
- "Export CSV button enabled when run.status=='FROZEN'; disabled with tooltip otherwise"
- "Per-row 'Adjust' button opens a drawer with editable items[] (label, amountKd, kind) — feeds generatePayrollAdjustment proposal"
- "Sierra palette: header row uses bg-sand-100; net column uses text-primary"
- Imports `PayrollRunTable` from `@/components/finance/payroll/PayrollRunTable` (RED).

**`PnlChart.test.tsx`** — `describe("Phase 8 / REQ-finance-expenses-pl: PnlChart (Recharts)")` with 4 RED tests:
- "renders a Recharts BarChart with month-axis (x-axis) + revenue stack + expense stack + net line"
- "data prop: { months: [{month: '2026-04', revenue: {KEETA, TALABAT, DELIVEROO, AMERICANA}, expenses: {FUEL,...}}]} drives the chart"
- "uses Sierra palette tokens — Recharts Cell fill props pull from tailwind theme: revenue stack uses talabat/keeta/primary; expense stack uses sand-300/sand-400/sand-500"
- "period toggle (Month / Quarter) re-fetches and re-renders"
- Imports `PnlChart` from `@/components/finance/pnl/PnlChart` (RED).
- Mock Recharts ResponsiveContainer with a fixed 800x400 frame so jsdom can render.

**`ActionHistoryTable.test.tsx`** — `describe("Phase 8 / REQ-agent-action-tools: ActionHistoryTable")` with 5 RED tests:
- "renders AgentAction rows from GET /api/audit/agent-actions filtered to Phase 8 tool names by default"
- "row shows: Date, Tool, Driver (linked to Driver File), Outcome (success/failure/rolled_back), Approver"
- "Rollback button visible only for rollback-eligible tools: applyPenalty, suspendDriver, reassignShift, createTrainingTask, sendCourierMessage; NOT for recordCashSettlement (per dispatcher's manual-reversal rule)"
- "click Rollback opens ConfirmModal; on confirm calls POST /api/audit/agent-actions/:id/rollback; on 200 row gets rolled_back chip + audit.rolledBackAt set"
- "filter by toolName, outcome, approverId, dateFrom, dateTo — assertions against mocked /api/audit response"
- Imports `ActionHistoryTable` from `@/components/finance/history/ActionHistoryTable` (RED).
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/Darb/frontend && [[ $(ls src/__tests__/finance/FinanceLayout.test.tsx src/__tests__/finance/ReconciliationGrid.test.tsx src/__tests__/finance/PayrollRunTable.test.tsx src/__tests__/finance/PnlChart.test.tsx src/__tests__/finance/ActionHistoryTable.test.tsx 2>/dev/null | wc -l | tr -d ' ') -eq 5 ]] && cd /Users/mac/Documents/Darb/frontend && npm test -- src/__tests__/finance 2>&1 | grep -E "FAIL|failed|Cannot find module" | head -1</automated>
  </verify>
  <done>
    5 frontend RED test files exist under src/__tests__/finance/. `cd frontend && npm test -- src/__tests__/finance` exits non-zero (RED — components not built yet). Existing Phase-2 frontend tests `src/__tests__/decisions/` + `src/__tests__/admin/` still GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 4: Extend lint:tenant scope to Phase 8 new surfaces</name>
  <files>
    backend/package.json
  </files>
  <read_first>
    backend/package.json (current line 17 — read EXACT lint:tenant script; preserve all flags and existing globs verbatim),
    backend/eslint-rules/no-prisma-without-tenant.js (the rule definition Phase 8 reuses),
    .planning/phases/08-finance-workbench/08-RESEARCH.md (Pitfall 6: tenant guard regression in finance routes; Don't Hand-Roll table line "Rely on prismaExtensions.ts runtime guard + lint:tenant static rule extended to services/finance/")
  </read_first>
  <action>
Edit the existing `lint:tenant` script in `backend/package.json` to add Phase 8 paths to the glob. Current script (verify by reading line 17 of `backend/package.json` first; preserve EVERY existing glob verbatim including the Phase 6 `src/services/ingest/` and Phase 5/4/3/2/1 entries):

```
"lint:tenant": "eslint src/agent/ src/__tests__/agent/ src/routes/aiChiefOfStaff.ts src/routes/ai.ts src/agent/tools/action/ src/services/decisions/ src/services/billing/ src/services/onboarding/ src/routes/decisions.ts src/routes/admin/ src/middleware/superAdmin.ts src/queues/onboardingBackwashWorker.ts src/services/driverFile/ src/agent/tools/read/performanceTrend.ts src/routes/chat.ts src/routes/pinnedViews.ts src/routes/scheduledBriefings.ts src/services/chatHistoryService.ts src/services/scheduledBriefingsService.ts src/queues/scheduledBriefingsWorker.ts src/agent/tools/view/ src/services/activePlatformAttribution.ts src/services/r2Service.ts src/middleware/agentRateLimit.ts src/__tests__/services/activePlatformAttribution.test.ts src/services/ingest/ --ext .ts --no-eslintrc --resolve-plugins-relative-to . --rulesdir eslint-rules --parser @typescript-eslint/parser --parser-options=ecmaVersion:2022,sourceType:module --rule \"{\\\"no-prisma-without-tenant\\\":\\\"error\\\"}\" --no-error-on-unmatched-pattern"
```

Append these globs IMMEDIATELY BEFORE `--ext .ts`:
- `src/services/finance/`
- `src/routes/cash.ts` (existing file — already has tenant filters from Phase 1; the lint gate just locks them in)
- `src/routes/financePayroll.ts`
- `src/routes/financeInvoices.ts`
- `src/routes/financeExpenses.ts`
- `src/routes/financePnl.ts`

The 8 new tool files under `src/agent/tools/action/` are already covered by the existing `src/agent/tools/action/` directory glob — no new entries needed for those.

After editing, run `cd backend && npm run lint:tenant`. Expected output: exit 0 (the new globs match no source files yet thanks to `--no-error-on-unmatched-pattern`).

**Do NOT** modify any other scripts. Do NOT bump version. Do NOT modify existing globs — Phase 1-7 scope must remain verbatim.

**Why this works in Wave 0:** the `--no-error-on-unmatched-pattern` flag keeps lint:tenant exit=0 when a directory is empty. From Wave 2 onward (when services/finance/ ships), every file is gated. From Wave 3 onward (when routes/finance*.ts files ship), every route is gated. Phase 1's success criterion 4 ("automated check catches any new agent-touching route that omits tenant scoping") extends to Phase 8's surfaces from day one.
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/Darb/backend && grep -E "src/services/finance/|src/routes/financePayroll\.ts|src/routes/financeInvoices\.ts|src/routes/financeExpenses\.ts|src/routes/financePnl\.ts" package.json | wc -l | xargs -I {} test {} -ge 1 && cd /Users/mac/Documents/Darb/backend && npm run lint:tenant 2>&1 | tee /tmp/lint-tenant-phase8.txt ; echo "exit=$?" ; grep -cE "(error|warning)" /tmp/lint-tenant-phase8.txt</automated>
  </verify>
  <done>
    `backend/package.json` lint:tenant glob includes `src/services/finance/`, `src/routes/financePayroll.ts`, `src/routes/financeInvoices.ts`, `src/routes/financeExpenses.ts`, `src/routes/financePnl.ts` (and `src/routes/cash.ts` if not already present).
    `cd backend && npm run lint:tenant` exits 0. Phase 1-7 globs preserved verbatim (diff is append-only).
  </done>
</task>

</tasks>

<verification>
1. **RED tests fail with module-not-found for the right modules:** `cd backend && npx jest --testPathPattern='(agent/tools/action/(applyPenalty|suspendDriver|reassignShift|recordCashSettlement|sendCourierMessage|createTrainingTask|escalateToHumanSupervisor|generatePayrollAdjustment)|services/finance|routes/finance)' 2>&1 | grep -c "Cannot find module"` returns >=16.
2. **Phase 8 dual-approval race test exists:** `grep -c "Already resolved" backend/src/__tests__/decisions/dualApproval.phase8.test.ts` >= 1.
3. **PHASE_8_LIVE_TOOLS assertion exists:** `grep -c "PHASE_8_LIVE_TOOLS" backend/src/__tests__/decisions/cardProjector.phase8.test.ts` >= 1.
4. **REVERSERS dispatch test asserts no prisma.*.delete:** `grep -E "REVERSERS\.|OVERTURNED|prisma\.\\w+\\.delete" backend/src/__tests__/services/finance/rollbackDispatcher.test.ts` matches REVERSERS+OVERTURNED but NOT `.delete`.
5. **Frontend Phase 8 tests exist:** `find frontend/src/__tests__/finance -name "*.test.tsx" -type f | wc -l` returns 5.
6. **lint:tenant scope extended:** `grep "src/services/finance/" backend/package.json` matches.
7. **lint:tenant exits 0 with empty new scope:** `cd backend && npm run lint:tenant; echo $?` prints `0`.
8. **Existing tests untouched:** `cd backend && npm test -- --testPathPattern='(agent/walkingSkeleton|agent/ledger|agent/memory|decisions/(approveFlow|cardProjector\.test|dismissFlow))'` exits 0.
9. **Phase 2 PHASE_2_LIVE_TOOLS export unchanged:** `grep -c "PHASE_2_LIVE_TOOLS: ReadonlySet<string> = new Set" backend/src/services/decisions/cardProjector.ts` >= 1 AND the file currently still contains only "draftCourierMessage" in that Set (Wave 1 will extend it).
10. **Migration verification gate stays RED until Wave 1:** `cd backend && npx jest --testPathPattern='agent/tools/action/applyPenalty\\.test' 2>&1 | grep -c "Cannot find name 'Penalty'"` or similar Prisma-type-not-found error.
</verification>

<success_criteria>
- 16 backend RED tool/service test files committed
- 6 backend RED route test files committed
- 5 frontend RED test files committed
- `backend/package.json` lint:tenant glob extended (5 new paths)
- `cd backend && npx jest --testPathPattern='(agent/tools/action|decisions/(cardProjector|dualApproval)\.phase8|services/finance|routes/(cash\.reconciliation|finance(Payroll|Invoices|Expenses|Pnl)|auditRollback\.phase8))'` exits non-zero
- `cd frontend && npm test -- src/__tests__/finance` exits non-zero
- `cd backend && npm run lint:tenant` exits 0
- `cd backend && npm test -- --testPathPattern='(agent|decisions/(approveFlow|cardProjector\.test|dismissFlow)|services/ingest|services/driverFile|services/chatHistoryService|services/activePlatformAttribution)'` still exits 0 — Phase 1-7 tests not regressed
- No commits modify existing source files outside `package.json` line 17
</success_criteria>

<output>
After completion, create `.planning/phases/08-finance-workbench/08-00-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md` documenting:
- Test files created (count + paths per category: tool / service / route / frontend)
- Coverage map (which Wave 1+ task each RED test backs)
- lint:tenant scope diff (before/after)
- Confirmation that Phase 1-7 tests still pass
- Hand-off note for Wave 1: "Implement the 8 new tool files under backend/src/agent/tools/action/ + Wave-1 Prisma migration for the 5 new finance models to turn the tool-test + dualApproval RED tests green."
</output>
</content>
