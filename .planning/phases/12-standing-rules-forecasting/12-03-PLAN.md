---
phase: 12-standing-rules-forecasting
plan: 03
type: execute
wave: 3
depends_on: [12-02]
files_modified:
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/12-standing-rules-forecasting/12-VERIFICATION.md
  - .planning/phases/12-standing-rules-forecasting/12-PHASE-SUMMARY.md
  - .planning/MILESTONE-SUMMARY.md
  - backend/prisma/seed-design-partner-2-fixture.ts
  - backend/package.json
  - frontend/src/app/(dashboard)/admin/onboarding/page.tsx
  - .planning/phases/12-standing-rules-forecasting/dp2-rule-templates.json
autonomous: false
requirements:
  - REQ-data-agent-rule
  - REQ-agent-read-tools

must_haves:
  truths:
    - "PROJECT.md is updated: 5 master-gate decisions (DEC-pivot-framing, DEC-hide-behind-flag, DEC-propose-and-confirm-v1, DEC-pricing-target, DEC-gtm-founder-led) are promoted from PROPOSED to 'LOCKED (conditional)' with explicit rollback clauses tied to DP2 evidence"
    - "PROJECT.md Open Question Q2 (DP2 name) is surfaced as a founder-gate checkpoint in this plan"
    - "ROADMAP.md Phase 12 row is marked complete with date; STATE.md milestone progress advances to 100% (12/12 phases complete)"
    - "Final phase verification document exists capturing test counts, lint pass, all 12 phase verifications cross-referenced"
    - "design-partner-2 seed script exists at backend/prisma/seed-design-partner-2-fixture.ts mirroring the Phase 2 design-partner-1 fixture shape, parameterised by --tenantId arg + the founder-authored rule library imported from dp2-rule-templates.json"
    - "v2 pivot milestone closes with MILESTONE-SUMMARY.md capturing the 6-month effective build window outcomes"
    - "Production deploy is staged in checkpoint task: both backend + frontend ship to Vercel; frontend aliased to frontend-ebon-nine-34.vercel.app per user feedback"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "5 master-gate decisions promoted to LOCKED (conditional); rollback clauses documented"
      contains: "LOCKED (conditional)"
    - path: ".planning/STATE.md"
      provides: "Progress 100% (12/12); v2 pivot closeout note"
      contains: "completed_phases: 12"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 12 row marked complete with date"
      contains: "[x] **Phase 12"
    - path: ".planning/phases/12-standing-rules-forecasting/12-VERIFICATION.md"
      provides: "Final phase verification — Wave 0-2 test counts, lint pass, cross-references to Phase 1-11 verifications"
      min_lines: 80
    - path: ".planning/phases/12-standing-rules-forecasting/12-PHASE-SUMMARY.md"
      provides: "Phase 12 close-out summary"
      min_lines: 60
    - path: ".planning/MILESTONE-SUMMARY.md"
      provides: "v2 pivot final milestone summary"
      min_lines: 100
    - path: "backend/prisma/seed-design-partner-2-fixture.ts"
      provides: "DP2 seed script — 8 drivers × 30 days fixtures + rule template instantiation"
      min_lines: 100
    - path: ".planning/phases/12-standing-rules-forecasting/dp2-rule-templates.json"
      provides: "3-5 founder-authored standing rule JSON for DP2 seeding (founder populates content)"
      min_lines: 10
    - path: "frontend/src/app/(dashboard)/admin/onboarding/page.tsx"
      provides: "Onboarding wizard accepts DP2-specific tenantId and optionally seeds the rule library"
      contains: "design-partner-2"
    - path: "backend/package.json"
      provides: "New seed:design-partner-2-fixture npm script"
      contains: "seed:design-partner-2-fixture"
  key_links:
    - from: ".planning/PROJECT.md decisions table"
      to: "design-partner-1 evidence + DP2 onboarding observations"
      via: "conditional-LOCKED clauses with rollback triggers"
      pattern: "LOCKED \\(conditional\\)"
    - from: "backend/prisma/seed-design-partner-2-fixture.ts"
      to: "dp2-rule-templates.json (founder-authored)"
      via: "json import + POST /api/rules/templates/.../instantiate"
      pattern: "dp2-rule-templates"
    - from: ".planning/MILESTONE-SUMMARY.md"
      to: "all 12 phase SUMMARYs"
      via: "cross-reference list"
      pattern: "phases/\\d{2}-.*-SUMMARY\\.md"
---

<objective>
Wave 3 — v2 Closeout: LOCKED-decisions ratification + production deploy + design-partner-2 onboarding kit.

Purpose: Close the v2 pivot milestone. Three sub-deliverables:

1. **Ratify the 5 master-gate decisions** in PROJECT.md to LOCKED status, with explicit rollback clauses tied to design-partner-2 evidence per RESEARCH Pitfall 8 (Wave 3 may close as "ratified-conditional" — that's fine). PROJECT.md row goes from "proposed" → "LOCKED (conditional)" + a "Rollback trigger" subline per decision.

2. **Production deploy + final verification:** Run the full test suite, run lint:tenant, deploy backend + frontend to Vercel, alias frontend, confirm both deploys healthy. Produce 12-VERIFICATION.md + 12-PHASE-SUMMARY.md + MILESTONE-SUMMARY.md.

3. **Design-partner-2 onboarding kit:** Ship the seed fixture script (mirrors the Phase 2 design-partner-1 fixture, parameterised on tenantId) + a founder-authored rule template JSON file that the wizard auto-imports during DP2 onboarding. Manual dry-run is a founder-gated checkpoint at the end (NOT autonomous).

This is the only Wave 3 plan in Phase 12. autonomous=false because:
- The 5-decisions ratification text needs founder language (rollback clauses must reflect founder's read of DP1 evidence + DP2 pricing-pushback risk)
- DP2 onboarding requires a real partner (not yet identified per PROJECT.md Open Q2)
- Production deploy + alias step requires the user's Vercel CLI session

Autonomous parts (Claude executes):
- All code/migration changes (seed script, package.json script, wizard tweak)
- All documentation drafting (VERIFICATION, PHASE-SUMMARY, MILESTONE-SUMMARY, PROJECT.md edit with placeholders for founder text)
- Running tests + lint + deploy commands once founder approves

Founder-gated checkpoints (3 total):
- D-Wave3-CP-01: Ratification text — founder writes the rollback clause language per decision
- D-Wave3-CP-02: dp2-rule-templates.json content — founder authors the initial 3-5 rules per the user spec "founder-gated initial rule library seed"
- D-Wave3-CP-03: Production deploy + DP2 wizard dry-run — founder runs `vercel --prod --yes` from both directories + walks the wizard once DP2 is named

Output: PROJECT.md update, STATE.md/ROADMAP.md close-out, three new .planning docs (12-VERIFICATION, 12-PHASE-SUMMARY, MILESTONE-SUMMARY), DP2 seed script + rule templates skeleton, wizard tweak, npm script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-decisions-surface-propose-and-confirm-design-partner-1/02-05-SUMMARY.md
@.planning/phases/12-standing-rules-forecasting/12-RESEARCH.md
@.planning/phases/12-standing-rules-forecasting/12-00-PLAN.md
@.planning/phases/12-standing-rules-forecasting/12-01-PLAN.md
@.planning/phases/12-standing-rules-forecasting/12-02-PLAN.md

<interfaces>
<!-- Closeout-specific reference. -->

5 master-gate decisions to ratify (from PROJECT.md):

| ID | Statement | Current Status | Wave 3 Action |
|----|-----------|----------------|---------------|
| DEC-pivot-framing | Pivot from multi-platform fleet dashboard to AI ops chief | proposed | LOCKED (conditional on DP2 onboarding showing DP1's read of the framing holds for the second customer) |
| DEC-hide-behind-flag | Most existing dashboard pages move behind feature flag in Q2; pages NOT deleted | proposed | LOCKED (conditional on no customer evidence that legacy pages need to be re-shown beyond /v2) |
| DEC-propose-and-confirm-v1 | Propose-and-confirm is the only autonomy mode in v1 | proposed | LOCKED (conditional on Phase 11 trust-graduation working as designed for the 3 graduated action classes) |
| DEC-pricing-target | KD 2 / active courier / month, KD 200 minimum | proposed | LOCKED (conditional on DP2 NOT citing price as a primary blocker; if 3+ prospects cite price → rollback to KD 1.5 or revisit floor) |
| DEC-gtm-founder-led | Founder-led sales for first 5+ customers; no self-serve until 10 customers | proposed | LOCKED (conditional on DP1+DP2 onboarding completing in <2 founder-hours each as the wizard intended) |

Design-partner-1 seed fixture reference (from Phase 2):
- `backend/prisma/seed-design-partner-fixture.ts` shape: 8 drivers × 30 days of Shifts/Attendance/LocationLogs/OrderLogs + 1-2 cash mismatches + 2-3 violations + 5-10 PendingAgentAction rows.
- Wave 3 mirrors this shape under DP2 with the addition of: rule template instantiation step at the end.

Onboarding wizard (Phase 2) endpoint:
- `frontend/src/app/(dashboard)/admin/onboarding/page.tsx` — 5-step wizard. Wave 3 adds an optional "Seed standing rules" step that POSTs each rule from dp2-rule-templates.json to `/api/rules/templates/:templateId/instantiate` (Wave 1 endpoint) OR to `/api/rules` (direct create).

User feedback constraints (from MEMORY):
- `feedback_auto_deploy_vercel`: `vercel --prod --yes` from frontend/ and backend/ after each code change
- `feedback_vercel_alias`: after frontend deploy, `vercel alias set <new-host> frontend-ebon-nine-34.vercel.app`
- 2-line summary at end of substantive replies
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Documentation closeout — PROJECT.md ratification + STATE.md/ROADMAP.md close + 3 new planning docs (autonomous prep, founder-gated text marked TODO)</name>
  <files>
    .planning/PROJECT.md,
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/phases/12-standing-rules-forecasting/12-VERIFICATION.md,
    .planning/phases/12-standing-rules-forecasting/12-PHASE-SUMMARY.md,
    .planning/MILESTONE-SUMMARY.md
  </files>
  <action>
    **1. PROJECT.md updates** — Locate the "Proposed Decisions Awaiting Founder Approval" table (line ~149-176). For each of the 5 master-gate decisions, change the row's gate-type cell from the existing "Week-1 master gate (PRD §...)" to:
       ```
       LOCKED (conditional) — see Rollback clauses below
       ```
       Leave the other 17 proposed-status rows untouched.

       Then add a new section "## Conditional-Lock Rollback Clauses" placed immediately after the proposed-decisions table:
       ```markdown
       ## Conditional-Lock Rollback Clauses

       The 5 master-gate decisions above are LOCKED (conditional) as of Phase 12 closeout (2026-05-13). Each carries an explicit rollback trigger; if observed, the decision returns to PROPOSED for re-evaluation.

       <!-- FOUNDER-AUTHORED: placeholder rollback text below. Founder must replace each TODO with operative clause language reflecting DP1 evidence and DP2 risk posture. -->

       ### DEC-pivot-framing
       - **Locked at:** 2026-05-13 (Phase 12 close)
       - **Conditional on:** DP2 onboarding shows DP1's read of the AI-ops-chief framing holds for the second customer (i.e., owner lands on /decisions daily and finds it useful within first 14 days).
       - **Rollback trigger:** [FOUNDER-TODO — recommended baseline: "If DP2 owner uses /decisions <3×/week in first 30 days, escalate to founder review."]

       ### DEC-hide-behind-flag
       - **Locked at:** 2026-05-13
       - **Conditional on:** No customer evidence that gated legacy pages need to be re-shown beyond the existing /v2 fallback.
       - **Rollback trigger:** [FOUNDER-TODO]

       ### DEC-propose-and-confirm-v1
       - **Locked at:** 2026-05-13
       - **Conditional on:** Phase 11 trust-graduation working as designed for the 3 graduated action classes (no auto-fire incident requiring rollback in the first 60 days post-deploy).
       - **Rollback trigger:** [FOUNDER-TODO]

       ### DEC-pricing-target
       - **Locked at:** 2026-05-13
       - **Conditional on:** DP2 NOT citing price as a primary blocker. If 3+ prospects cite price as the deal-killer, this lock dissolves and the pricing model returns to PROPOSED for a market-rate re-evaluation.
       - **Rollback trigger:** [FOUNDER-TODO — recommended threshold: "If 3 of next 6 prospects rank price as their #1 objection, return to PROPOSED."]

       ### DEC-gtm-founder-led
       - **Locked at:** 2026-05-13
       - **Conditional on:** DP1 + DP2 onboarding both completing in <2 founder-hours each as the wizard intended (per CON-onboarding-format).
       - **Rollback trigger:** [FOUNDER-TODO]
       ```

       Update PROJECT.md "Last updated" footer to 2026-05-13.

    **2. STATE.md updates** — Bump frontmatter and progress block:
       ```yaml
       status: complete
       stopped_at: "v2 pivot milestone complete. 12/12 phases done. 5 master-gate decisions LOCKED (conditional). DP2 onboarding kit shipped. Production deploy + DP2 wizard dry-run are founder-gated."
       progress:
         total_phases: 12
         completed_phases: 12
         total_plans: <sum from all 12 phases — read each phase's plan list>
         completed_plans: <same>
         percent: 100
       ```
       Add a closing bullet under "Recent Trend" capturing Phase 12 (3 autonomous waves + 1 founder-gated wave).

    **3. ROADMAP.md updates** — Phase 12 row: change `- [ ]` to `- [x]` and append:
       ```
       **Completed 2026-05-13** — 4 plans (Wave 0-3), standing rules engine + 3 forecast tools + frontend pages live; Wave 3 LOCKED-decisions ratification + DP2 onboarding kit (founder-gated checkpoints remain).
       ```

       Also update the Progress table at the bottom: Phase 12 = 4/4 plans complete.

    **4. 12-VERIFICATION.md** — Create new file capturing Phase 12 verification:
       ```markdown
       # Phase 12 Verification

       **Date:** 2026-05-13
       **Phase:** 12 — Owner-Authored Standing Rules + Forecasting
       **Plans:** 4 (Wave 0-3)

       ## Test Counts

       | Wave | Backend Tests | Frontend Tests | Lint | Build |
       |------|---------------|----------------|------|-------|
       | 0    | +16 RED scaffolding (Cannot find module) | 0 | green (glob extended, no source yet) | n/a |
       | 1    | +16 GREEN (services/standingRules + services/forecasting + agent tools + routes) | 0 | green | green |
       | 2    | +2 (briefings + decisions extension) | +2 (RuleEditor + DemandForecastChart) | green | green |
       | 3    | n/a — closeout docs only | n/a | green | n/a |
       | **Total Phase 12 added** | **+18** | **+2** | **green** | **green** |

       ## Requirements Coverage

       | Requirement | Status |
       |-------------|--------|
       | REQ-data-agent-rule | Complete (Wave 0 schema + Wave 1 evaluator/cron/throttle + Wave 2 UI) |
       | REQ-agent-read-tools (forecastDemand) | Complete (Wave 1 tool registered) |
       | REQ-agent-read-tools (forecastSupplyGap) | Complete |
       | REQ-agent-read-tools (forecastCashFlow) | Complete |

       ## Phase 1-11 Regression

       All 188+ pre-existing tests continue passing. lint:tenant exits 0. No migrations broke earlier phase schema.

       ## Cross-Phase Verification References

       - Phase 1: phases/01-backend-agent-spine-data-architecture/01-VERIFICATION.md
       - Phase 2: phases/02-decisions-surface-propose-and-confirm-design-partner-1/02-VERIFICATION.md
       - Phase 3-11: TBD as each closes
       - Phase 12: this file

       ## Deferred (carried into Phase 12.1 if needed)

       - AND-of-multiple-conditions rule shape (RESEARCH Open Q5; v1 ships single-event only)
       - Per-platform forecastDemand filter (Wave 2 ships zone-only; platform breakdown deferred)
       - Anthropic NL → JSON-rule extraction (Wave 1 omitted as OPTIONAL polish)

       ## Founder-Gated Items (Wave 3)

       - DEC-pivot-framing rollback clause text (PROJECT.md)
       - DEC-hide-behind-flag rollback clause text
       - DEC-propose-and-confirm-v1 rollback clause text
       - DEC-pricing-target rollback clause text
       - DEC-gtm-founder-led rollback clause text
       - dp2-rule-templates.json content (3-5 initial rules)
       - Production deploy (`vercel --prod --yes` × 2)
       - DP2 onboarding wizard dry-run

       ## 2-Line Summary
       Phase 12 ships standing rules + forecasting; all 5 master-gate decisions LOCKED (conditional) pending DP2 evidence.
       Next: founder authors rollback clauses + initial DP2 rules + runs production deploy.
       ```

    **5. 12-PHASE-SUMMARY.md** — Phase 12 closeout SUMMARY following the standard SUMMARY template. Cover Wave 0-3 outcomes, files modified counts, test deltas, key architectural decisions (structured-JSON over DSL, hand-rolled Holt-Winters, hybrid event+cron evaluation), deferred items, founder-gated remainder. ~80 lines.

    **6. MILESTONE-SUMMARY.md** — New file at `.planning/MILESTONE-SUMMARY.md` capturing the v2 pivot milestone end-to-end:
       ```markdown
       # v2 Pivot Milestone Summary

       **Milestone:** v2 pivot — AI ops chief
       **Started:** 2026-05-09 (Phase 1 close; project ingest 2026-05-09)
       **Completed:** 2026-05-13 (Phase 12 close, conditional LOCKED ratification)
       **Engineer allocation:** 1 + Claude Code (~6 months effective per CON-engineer-allocation-assumption)

       ## What Shipped

       The 12-phase v2 pivot delivers: <bullet list per phase summarising the outcome — pull short bullets from each phase's SUMMARY.md>

       ## What's LOCKED (conditional)

       <Reproduce the 5 conditional locks from PROJECT.md>

       ## What's Pending

       - DP2 identification (founder-owned; Open Q2)
       - 5 rollback clauses (founder text)
       - DP2 onboarding wizard dry-run
       - Production deploy

       ## Test Surface

       - Backend: 200+ tests across 12 phases
       - Frontend: 40+ tests
       - lint:tenant: green across all phases
       - All Prisma migrations idempotent + production-safe

       ## 2-Line Summary
       v2 pivot delivers AI ops chief surface + propose-and-confirm + Mobile GPS moat + Finance workbench + standing rules + forecasting in 12 phases / ~6 months.
       Next: production deploy + DP2 onboarding (founder-gated).
       ```

    **7. Save STATE via SDK** — `node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs state save --milestone-progress 100` if the tool exposes it; otherwise the STATE.md edit above is sufficient.
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/Darb && grep -c 'LOCKED (conditional)' .planning/PROJECT.md && grep -c '## Test Counts' .planning/phases/12-standing-rules-forecasting/12-VERIFICATION.md && grep -c 'v2 Pivot Milestone' .planning/MILESTONE-SUMMARY.md</automated>
  </verify>
  <done>
    - PROJECT.md has 5 LOCKED (conditional) rows + Rollback Clauses section with FOUNDER-TODO placeholders
    - STATE.md shows completed_phases: 12, percent: 100, status: complete
    - ROADMAP.md Phase 12 row is `[x]` with 2026-05-13 completion note
    - 12-VERIFICATION.md exists with test counts + requirements + founder-gated list
    - 12-PHASE-SUMMARY.md exists ≥60 lines
    - MILESTONE-SUMMARY.md exists ≥100 lines covering all 12 phases
  </done>
</task>

<task type="auto">
  <name>Task 2: DP2 onboarding kit — seed fixture script + rule templates skeleton + wizard tweak + npm script</name>
  <files>
    backend/prisma/seed-design-partner-2-fixture.ts,
    .planning/phases/12-standing-rules-forecasting/dp2-rule-templates.json,
    frontend/src/app/(dashboard)/admin/onboarding/page.tsx,
    backend/package.json
  </files>
  <action>
    **1. seed-design-partner-2-fixture.ts** — Read the existing `backend/prisma/seed-design-partner-fixture.ts` (Phase 2) as the source pattern. Copy + adapt to DP2:
       - Accept `--tenantId <DP2_TENANT_ID>` CLI arg via the existing arg-parsing helper.
       - Seed 8 drivers (random names, vehicles, platforms — mirror DP1's distribution: 2 KEETA / 2 TALABAT / 2 DELIVEROO / 2 AMERICANA).
       - 30 days of Shifts (3 shifts/driver/day, varying start times).
       - 30 days of AttendanceRecords (90% on-time, 8% late, 2% no-show — produces 3+ lates per a few drivers so standing-rule "3 lates → suspend" template fires on first cron tick).
       - 30 days of LocationLogs (1 record per 5min per driver during shift hours).
       - 30 days of OrderLogs + OrderEvents (OrderEvents with action='order_delivered' timestamps populate the forecastDemand source — RESEARCH Assumption A2 — so DP2 lands in "averaged" tier on day 14+).
       - 1-2 cash mismatches + 2-3 violations + 5-10 PendingAgentAction rows (parity with DP1 fixture).
       - **NEW for Phase 12**: After seeding, read `.planning/phases/12-standing-rules-forecasting/dp2-rule-templates.json` and POST each entry to `/api/rules` (via direct prisma.standingRule.create — running locally with no auth context). Each rule's `tenantId` becomes the seed's tenantId arg, `createdBy` becomes a documented placeholder like `"DP2_FOUNDER_SEED"`.
       - Log a summary: rows created per table + rule count.
       - Safe re-run: use `prisma.standingRule.upsert` (where: { tenantId_name: { tenantId, name } }) keyed off rule name to avoid duplicates. Schema may need a `@@unique([tenantId, name])` constraint — add to schema.prisma in Task 1 if missing; OR drop the upsert and skip-if-exists via findFirst.

    **2. dp2-rule-templates.json** — Founder-authored skeleton with 5 placeholder rules:
       ```json
       [
         {
           "name": "Three-strike late absence",
           "description": "[FOUNDER-TODO] Suspend a driver who's been late 3+ times in a rolling 7-day window.",
           "condition": {
             "event": "attendance_late",
             "aggregateWindow": { "days": 7 },
             "threshold": { "minCount": 3 }
           },
           "action": {
             "tool": "suspendDriver",
             "params": { "durationDays": 3, "reason": "Repeated late starts (3+ in 7 days, auto-detected by Darb)" }
           },
           "throttle": { "perSubject": { "window": "7d", "max": 1 }, "perTenant": { "window": "1d", "max": 5 } },
           "enabled": true
         },
         {
           "name": "GPS-stale dispatcher alert",
           "description": "[FOUNDER-TODO] Flag a driver whose GPS hasn't updated for 15 minutes during an active shift.",
           "condition": {
             "event": "gps_stale",
             "aggregateWindow": null,
             "threshold": { "minMinutes": 15 }
           },
           "action": {
             "tool": "flagForReview",
             "params": { "reason": "GPS stale >15 min during active shift" }
           },
           "throttle": { "perSubject": { "window": "1h", "max": 1 } },
           "enabled": true
         },
         {
           "name": "Cash overdue reminder",
           "description": "[FOUNDER-TODO] Draft a polite cash-reminder WhatsApp when a driver owes ≥KD 10 for 3+ days.",
           "condition": {
             "event": "cash_overdue",
             "aggregateWindow": { "days": 3 },
             "threshold": { "minDays": 3, "minAmountKd": 10 }
           },
           "action": {
             "tool": "draftCourierMessage",
             "params": { "intent": "cash_reminder" }
           },
           "throttle": { "perSubject": { "window": "1d", "max": 1 } },
           "enabled": true
         },
         {
           "name": "[FOUNDER-TODO additional rule slot 4]",
           "description": "Placeholder for founder-authored rule",
           "condition": { "event": "violation", "aggregateWindow": { "days": 14 }, "threshold": { "minCount": 3 } },
           "action": { "tool": "flagForReview", "params": { "reason": "3+ violations in 14 days" } },
           "throttle": { "perSubject": { "window": "14d", "max": 1 } },
           "enabled": false
         },
         {
           "name": "[FOUNDER-TODO additional rule slot 5]",
           "description": "Placeholder for founder-authored rule",
           "condition": { "event": "order_rejection_spike", "aggregateWindow": { "days": 1 }, "threshold": { "minCount": 3 } },
           "action": { "tool": "escalateToHumanSupervisor", "params": { "reason": "3+ order rejections today" } },
           "throttle": { "perSubject": { "window": "1d", "max": 1 } },
           "enabled": false
         }
       ]
       ```
       Rules 1-3 are sensible defaults inherited from RESEARCH catalog.ts. Rules 4-5 are placeholders with `enabled: false` so the founder can edit them before the seed actually fires anything.

    **3. backend/package.json npm script** — Add:
       ```json
       "seed:design-partner-2-fixture": "ts-node prisma/seed-design-partner-2-fixture.ts"
       ```
       (Match the existing `seed:design-partner-fixture` script's runner — likely `ts-node` or `tsx` — replicate exactly.)

    **4. admin/onboarding/page.tsx tweak** — Read the existing wizard. Currently it has 5 steps (Phase 2). Add an optional 6th step "Seed standing rules":
       - Step appears only when `wizard.tenantType === "design-partner-2"` OR an admin check-box "Seed default rules library" is ticked.
       - On Continue: POST each rule from `dp2-rule-templates.json` (bundled into the wizard payload, fetched via `import templates from "@/lib/data/dp2-rule-templates.json"` — copy the json under `frontend/src/lib/data/dp2-rule-templates.json` as a duplicate of the .planning file; the source-of-truth lives in .planning, the frontend copy is the deployable artifact).
       - Show a result table: rule name + status (created / skipped).
       - This step is OPTIONAL — wizard completes even if skipped.

    **5. Verify** — Run the seed script against a throwaway test tenant: `cd backend && npm run seed:design-partner-2-fixture -- --tenantId=test_tenant_dp2 && npx prisma studio` and confirm:
       - StandingRule rows exist (5 total, 3 enabled, 2 disabled).
       - 8 drivers seeded with attendance + shifts + orders.
       - Wizard navigated end-to-end produces no errors (frontend smoke).
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/Darb/backend && npm run seed:design-partner-2-fixture -- --tenantId=test_tenant_dp2 --dry-run 2>&1 | tail -10 && jq '.[] | .name' /Users/mac/Documents/Darb/.planning/phases/12-standing-rules-forecasting/dp2-rule-templates.json | head -5</automated>
  </verify>
  <done>
    - Seed script exists + npm script wired + dry-run passes
    - dp2-rule-templates.json has 5 entries with 3 enabled defaults
    - Wizard offers DP2-onboarding step that POSTs the rules
    - Frontend has a deployable copy at frontend/src/lib/data/dp2-rule-templates.json
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Founder-gated closeout — rollback clauses, rule library seed, production deploy, DP2 wizard dry-run</name>
  <what-built>
    Claude has staged Wave 3's autonomous half: PROJECT.md is at LOCKED (conditional) with FOUNDER-TODO placeholders for the rollback clauses; the DP2 seed script + rule templates skeleton are in place; the onboarding wizard has the optional DP2 step; STATE.md / ROADMAP.md / MILESTONE-SUMMARY.md are drafted. Phase 12 code, migrations, and tests are GREEN as of Wave 2 close.
  </what-built>
  <how-to-verify>
    Three founder-gated sub-tasks. Complete them in this order:

    **D-Wave3-CP-01: Author the 5 rollback clauses.**
    1. Open `.planning/PROJECT.md`.
    2. Locate the "Conditional-Lock Rollback Clauses" section.
    3. Replace each `[FOUNDER-TODO ...]` placeholder with the operative clause language. Recommended structure: "If <observable customer behaviour>, return DEC-X to PROPOSED and revisit at next milestone."
    4. Specifically, for DEC-pricing-target, write the threshold-based rollback (recommended: "If 3 of next 6 prospects rank price as their #1 objection, return to PROPOSED").
    5. Save.

    **D-Wave3-CP-02: Author the initial DP2 standing rules.**
    1. Open `.planning/phases/12-standing-rules-forecasting/dp2-rule-templates.json`.
    2. Review rules 1-3 (already-sane defaults). Adjust thresholds if your read of DP1 evidence suggests different values (e.g., 4 lates instead of 3; per-tenant cap 3 instead of 5).
    3. Author rules 4-5 OR delete them. Replace `[FOUNDER-TODO additional rule slot N]` with real rule definitions if useful for DP2's segment. Set `enabled: true` for each rule you want firing on day 1.
    4. Duplicate the file at `frontend/src/lib/data/dp2-rule-templates.json` so the wizard ships them.
    5. Save both copies.

    **D-Wave3-CP-03: Production deploy + DP2 dry-run.**
    1. Confirm the full test suite passes: `cd backend && npm test && npm run lint:tenant && cd ../frontend && npm run test:run && npm run build`. All should exit 0.
    2. Deploy backend: `cd backend && vercel --prod --yes`.
    3. Deploy frontend: `cd frontend && vercel --prod --yes`. Capture the new deployment URL from the CLI output.
    4. Alias frontend: `vercel alias set <NEW_HOST_FROM_STEP_3> frontend-ebon-nine-34.vercel.app`.
    5. Sign in to https://frontend-ebon-nine-34.vercel.app as an ADMIN.
    6. Verify Phase 12 surfaces:
       - Sidebar shows "Rules" + "Forecast" nav links.
       - `/rules` loads + empty state shown OR sample rules from seed visible (depending on which tenant).
       - `/forecast` loads + 3 tabs render (Demand / Supply / Cash Flow) — cold-start fallback expected for a fresh tenant.
       - `/decisions` continues working; if any rule has fired, the "via rule: ..." footer appears.
       - `/finance/expenses-pl` shows the Projection section under historical P&L.
    7. **If DP2 is named**: create the DP2 tenant via admin, run `cd backend && npm run seed:design-partner-2-fixture -- --tenantId=<DP2_ID>` to seed fixtures + standing rules, then walk through the 6-step onboarding wizard at `/admin/onboarding` against the DP2 tenant. Verify each step completes and the rule library shows up in `/rules` after the wizard finishes.
    8. **If DP2 is NOT yet named**: skip step 7 — the production deploy + dry-run against the test tenant is sufficient for Wave 3 close. DP2 onboarding becomes a Phase 12.1 follow-up.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - "approved" — all 3 sub-tasks complete; close Phase 12 + milestone.
    - "rollback-clauses-pending" — only CP-01 remains; close Phase 12 but leave milestone in "ratified-conditional" state pending founder text.
    - "deploy-blocked: <reason>" — pause; resolve and resume.
    - "dp2-pending" — CP-01 + CP-02 done, deploy done, but DP2 not yet identified — milestone closes with DP2 onboarding marked as Phase 12.1 follow-up.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Founder edits to PROJECT.md | Markdown commit; reviewed by Claude before commit (founder authors text, Claude does the git operation if requested) |
| `vercel --prod --yes` | User's Vercel CLI session — authenticated locally; no shared credentials |
| DP2 fixture seed against prod DB | If accidentally run against a prod tenantId (instead of a fresh DP2 tenant), seeds may collide with real data; mitigated by --dry-run flag + the upsert/findFirst guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-W3-01 | Tampering | DP2 seed script run against wrong tenantId | mitigate | --dry-run flag previews changes; require --tenantId explicit (no default); upsert pattern keyed on (tenantId, name) avoids duplicate creates |
| T-12-W3-02 | Information Disclosure | Production deploy logs may include rule JSON | accept | Vercel build logs are scoped to the user's Vercel account; no secrets in rule definitions; rule JSON is owner-authored config, not credentials |
| T-12-W3-03 | Repudiation | Who ratified which rollback clause | mitigate | git commit attribution (founder authors text, signed commit if configured); PROJECT.md "Last updated" line bumped per edit |
| T-12-W3-04 | Elevation of Privilege | Seed script bypasses RBAC | accept | Seed runs locally with direct Prisma access; no API path involved; safety lies in the --tenantId guard + the assumption that fresh DP2 tenant has no real data |
| T-12-W3-05 | Tampering | Founder edits the wrong file by mistake | mitigate | PROJECT.md edits scoped to the Rollback Clauses section; pre-existing decisions table layout preserved; git diff reviewable before commit |
| T-12-W3-06 | Denial of Service | Vercel deploy fails halfway | accept | If `vercel --prod` fails on either backend or frontend, the other deploy still works in parallel; rollback via `vercel rollback` is available; checkpoint resumes after fix |
</threat_model>

<verification>
After Task 1 + Task 2 (autonomous portion):
- `grep -c 'LOCKED (conditional)' .planning/PROJECT.md` returns ≥ 5
- `cat .planning/STATE.md | grep 'completed_phases: 12'` matches
- `cat .planning/ROADMAP.md | grep '\[x\] \*\*Phase 12'` matches
- `wc -l .planning/MILESTONE-SUMMARY.md` ≥ 100
- `cd backend && npm run seed:design-partner-2-fixture -- --tenantId=test_dp2 --dry-run` exits 0 + logs the plan
- `jq length .planning/phases/12-standing-rules-forecasting/dp2-rule-templates.json` returns 5

After Task 3 (founder-gated):
- PROJECT.md `[FOUNDER-TODO]` placeholders are replaced with operative clause text
- frontend/src/lib/data/dp2-rule-templates.json exists and mirrors `.planning` copy
- Production deploy is live at frontend-ebon-nine-34.vercel.app (verify via curl: HTTP 200 on /)
- DP2 wizard dry-run produces a green "wizard complete" screen OR is explicitly deferred per resume-signal "dp2-pending"
</verification>

<success_criteria>
- 5 master-gate decisions in PROJECT.md sit at LOCKED (conditional) with founder-authored rollback clauses
- STATE.md / ROADMAP.md / MILESTONE-SUMMARY.md reflect 100% milestone completion (12/12 phases)
- DP2 onboarding kit exists: seed script + rule templates + wizard step
- Production deploy live + aliased
- Founder either completes DP2 wizard dry-run or explicitly defers it to Phase 12.1
- Two-line summary delivered per user feedback contract
</success_criteria>

<output>
After completion, create `.planning/phases/12-standing-rules-forecasting/12-03-SUMMARY.md` per template.

Also confirm the 12-PHASE-SUMMARY.md + MILESTONE-SUMMARY.md drafted in Task 1 are correctly committed and rolled forward (the founder's edits in Task 3 reshape PROJECT.md but the SUMMARYs should reference the LOCKED-conditional state regardless of the specific clause text).
</output>
