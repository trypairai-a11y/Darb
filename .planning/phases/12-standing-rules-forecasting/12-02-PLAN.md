---
phase: 12-standing-rules-forecasting
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - frontend/src/app/(dashboard)/rules/page.tsx
  - frontend/src/app/(dashboard)/rules/[id]/page.tsx
  - frontend/src/app/(dashboard)/rules/new/page.tsx
  - frontend/src/app/(dashboard)/rules/__tests__/RuleEditor.test.tsx
  - frontend/src/app/(dashboard)/forecast/page.tsx
  - frontend/src/app/(dashboard)/forecast/DemandForecastChart.tsx
  - frontend/src/app/(dashboard)/forecast/SupplyGapPanel.tsx
  - frontend/src/app/(dashboard)/forecast/CashFlowProjection.tsx
  - frontend/src/app/(dashboard)/forecast/__tests__/DemandForecastChart.test.tsx
  - frontend/src/components/rules/RuleBuilder.tsx
  - frontend/src/components/rules/RuleList.tsx
  - frontend/src/components/rules/RuleDryRunPreview.tsx
  - frontend/src/components/rules/RuleJsonEditor.tsx
  - frontend/src/components/rules/RuleExportImport.tsx
  - frontend/src/components/decisions/ProposalCardRuleFooter.tsx
  - frontend/src/app/(dashboard)/decisions/page.tsx
  - frontend/src/app/(dashboard)/finance/expenses-pl/page.tsx
  - frontend/src/hooks/useRules.ts
  - frontend/src/hooks/useForecast.ts
  - frontend/src/hooks/useToolSchema.ts
  - frontend/src/lib/api/rules.ts
  - frontend/src/lib/api/forecast.ts
  - frontend/src/lib/types/rules.ts
  - frontend/src/lib/types/forecast.ts
  - frontend/src/components/layout/Sidebar.tsx
  - backend/src/agent/prompts/briefing-owner.md
  - backend/src/services/briefings/narrator.ts
autonomous: true
requirements:
  - REQ-data-agent-rule
  - REQ-agent-read-tools

must_haves:
  truths:
    - "User can navigate to /rules from the sidebar and see a paginated list of standing rules with name, enabled toggle, last-fired timestamp, fire count"
    - "User can click 'New rule' or pick a template to land in the rule builder (/rules/new or /rules/[id]) with visual form + raw-JSON tab"
    - "Rule builder action.params section dynamically renders Shadcn form inputs keyed off the selected tool's editableParams (fetched via GET /api/agent/tools/schema)"
    - "User can click 'Test against last 7 days' which calls POST /api/rules/:id/dry-run and renders matches in a side preview (driver names redacted to first-initial+last-name)"
    - "User can export a rule to JSON via Copy-to-Clipboard / Download; user can paste JSON to instantiate a rule in current tenant (used by Wave 3 design-partner-2 seeding)"
    - "User can navigate to /forecast and see three tabs: Demand / Supply / Cash Flow. Each tab renders the corresponding Recharts chart fed by the agent read tool output"
    - "Cold-start tier=insufficient renders a fallback copy block ('Forecast available after 14 days of operations') rather than empty chart axes"
    - "<CashFlowProjection /> is rendered both on /forecast (3rd tab) AND on /finance/expenses-pl (as a new section under historical P&L) — shared component, two consumers"
    - "Proposal cards in /decisions that came from a standing rule show a footer 'via rule: <name>' as a clickable link routing to /rules/[id]"
    - "Morning briefing prompt (Phase 11 narrator) only emits a forecast paragraph when |forecast - 4wk_avg| > 15% in any zone-hour bucket (RESEARCH Open Q2 resolution; user decision 2)"
    - "All new frontend pages use Tailwind + Shadcn primitives + Lucide icons; no new dependencies needed (Recharts/cmdk/Shadcn already shipped)"
  artifacts:
    - path: "frontend/src/app/(dashboard)/rules/page.tsx"
      provides: "Rule list page; uses useRules hook, RuleList component"
      min_lines: 60
    - path: "frontend/src/app/(dashboard)/rules/[id]/page.tsx"
      provides: "Rule editor page; tabbed visual/JSON view; dry-run preview pane"
      min_lines: 120
    - path: "frontend/src/app/(dashboard)/rules/new/page.tsx"
      provides: "New rule landing — blank or template starter"
      min_lines: 40
    - path: "frontend/src/components/rules/RuleBuilder.tsx"
      provides: "Visual form for condition (event/window/threshold) + action (tool/params) + throttle; uses useToolSchema for dynamic params"
      min_lines: 200
    - path: "frontend/src/components/rules/RuleList.tsx"
      provides: "Paginated table — name, enabled switch, last fired, fire count, edit/delete"
      min_lines: 70
    - path: "frontend/src/components/rules/RuleDryRunPreview.tsx"
      provides: "Side panel showing dry-run matches with timestamps + redacted subject names"
      min_lines: 50
    - path: "frontend/src/components/rules/RuleJsonEditor.tsx"
      provides: "Monaco-style raw JSON editor (use textarea + JSON.parse validation if Monaco not installed; preserve no-new-deps constraint)"
      min_lines: 50
    - path: "frontend/src/components/rules/RuleExportImport.tsx"
      provides: "Export to clipboard/download .json + paste-to-instantiate dialog"
      min_lines: 50
    - path: "frontend/src/app/(dashboard)/forecast/page.tsx"
      provides: "/forecast 3-tab page (Demand / Supply / Cash); per-platform breakdown on Demand tab"
      min_lines: 80
    - path: "frontend/src/app/(dashboard)/forecast/DemandForecastChart.tsx"
      provides: "Recharts <LineChart /> + DOW heatmap (Tailwind grid 24×7); ci90 shaded band"
      min_lines: 80
    - path: "frontend/src/app/(dashboard)/forecast/SupplyGapPanel.tsx"
      provides: "Red/amber zones table with top-24 hourly shortfalls"
      min_lines: 60
    - path: "frontend/src/app/(dashboard)/forecast/CashFlowProjection.tsx"
      provides: "30/60/90 day projection chart; net = revenue - expense; r² shown"
      min_lines: 70
    - path: "frontend/src/components/decisions/ProposalCardRuleFooter.tsx"
      provides: "Footer 'via rule: <name>' shown when ProposalCard.source==='rule'"
      min_lines: 25
    - path: "frontend/src/app/(dashboard)/finance/expenses-pl/page.tsx"
      provides: "Adds <CashFlowProjection /> section under existing historical P&L"
      contains: "CashFlowProjection"
    - path: "frontend/src/hooks/useRules.ts"
      provides: "React Query hooks: useRules, useRule, useCreateRule, useUpdateRule, useDeleteRule, useDryRunRule, useRuleTemplates, useInstantiateTemplate"
      min_lines: 70
    - path: "frontend/src/hooks/useForecast.ts"
      provides: "React Query hooks: useForecastDemand, useForecastSupplyGap, useForecastCashFlow — all 5-min staleTime"
      min_lines: 40
    - path: "frontend/src/hooks/useToolSchema.ts"
      provides: "Fetches GET /api/agent/tools/schema → returns tools with editableParams; used by RuleBuilder"
      min_lines: 30
    - path: "frontend/src/lib/api/rules.ts"
      provides: "Fetch wrappers around /api/rules endpoints"
      min_lines: 50
    - path: "frontend/src/lib/api/forecast.ts"
      provides: "Fetch wrappers around forecast tool endpoints (via agent tool surface)"
      min_lines: 30
    - path: "frontend/src/lib/types/rules.ts"
      provides: "TypeScript types matching backend services/standingRules/types.ts"
      min_lines: 50
    - path: "frontend/src/lib/types/forecast.ts"
      provides: "TypeScript types matching backend services/forecasting/types.ts"
      min_lines: 30
    - path: "frontend/src/components/layout/Sidebar.tsx"
      provides: "Sidebar gains two new nav links: /rules (icon: GitBranch) + /forecast (icon: TrendingUp); ADMIN-only for /rules"
      contains: "/rules"
    - path: "backend/src/agent/prompts/briefing-owner.md"
      provides: "Adds forecast injection rule: only mention when |forecast - 4wk_avg| > 15%"
      contains: "15%"
    - path: "backend/src/services/briefings/narrator.ts"
      provides: "Anomaly-threshold guard before invoking forecastDemand in briefing template"
      contains: "0.15"
    - path: "frontend/src/app/(dashboard)/rules/__tests__/RuleEditor.test.tsx"
      provides: "Vitest component test — renders rule editor, dry-run shows matches, validation rejects bad JSON"
      min_lines: 40
    - path: "frontend/src/app/(dashboard)/forecast/__tests__/DemandForecastChart.test.tsx"
      provides: "Vitest component test — chart renders given hw tier; insufficient tier shows fallback copy"
      min_lines: 30
  key_links:
    - from: "frontend/src/components/rules/RuleBuilder.tsx"
      to: "/api/agent/tools/schema"
      via: "useToolSchema hook → React Query → fetch"
      pattern: "useToolSchema|/api/agent/tools/schema"
    - from: "frontend/src/components/decisions/ProposalCardRuleFooter.tsx"
      to: "/rules/[id]"
      via: "Next.js Link"
      pattern: "href=\\{`/rules/\\$\\{"
    - from: "frontend/src/app/(dashboard)/finance/expenses-pl/page.tsx"
      to: "frontend/src/app/(dashboard)/forecast/CashFlowProjection.tsx"
      via: "shared component import"
      pattern: "import.*CashFlowProjection.*from"
    - from: "backend/src/services/briefings/narrator.ts"
      to: "backend/src/agent/tools/read/forecastDemand.ts"
      via: "anomaly-threshold check before inclusion"
      pattern: "Math\\.abs.*-.*\\s*>.*0\\.15|fourWeekAvg"
---

<objective>
Wave 2 — Frontend `/rules` page (visual + JSON rule builder) + `/forecast` page (3-tab demand/supply/cash) + integrations into existing surfaces (/decisions footer link, /finance/expenses-pl cash flow section, morning briefing anomaly threshold).

Purpose: Make the Wave 1 backend usable by the owner. The rule builder is the v3 autonomy primitive's owner-facing UI — without it, rules are invisible. The forecast page surfaces the three new agent read tools where owners look. The integrations close the loops: rule-fired proposals are traceable back to their source rule; cash-flow projections live where accountants already work; the morning briefing only mentions forecasts when they matter.

Per user decisions:
- D-01 (rule-fired proposals): show footer "via rule: X" link on rule-source cards in /decisions
- D-02 (briefing forecasts): only include when |forecast - 4wk_avg| > 15% (anomaly threshold)
- D-03 (cash flow surface): shared <CashFlowProjection /> rendered on BOTH /forecast and /finance/expenses-pl
- D-04 (AND of conditions): deferred to Phase 12.1 — Wave 2 ships single-condition only
- D-05 (rule export/import): Wave 2 includes JSON export/import for DP2 seeding
- D-06 (DP2 trust level): default 0 — no change needed in Wave 2 frontend

Output: 2 new app routes (/rules, /forecast), 6 components under components/rules/, 3 components under app/(dashboard)/forecast/, 1 component under components/decisions/, 4 hooks, 4 lib files, sidebar update, finance page extension, briefing prompt + narrator extension, 2 Vitest component tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-standing-rules-forecasting/12-RESEARCH.md
@.planning/phases/12-standing-rules-forecasting/12-01-PLAN.md

<interfaces>
<!-- Backend contracts the frontend consumes. Extracted from Wave 1 plan + existing patterns. -->

From backend Wave 1 routes (the Wave 2 hooks call these directly):
```
GET    /api/rules                          → { data: StandingRule[], pagination }
POST   /api/rules                          (body: StandingRule) → 201 StandingRule
GET    /api/rules/:id                      → StandingRule
PUT    /api/rules/:id                      (body: partial) → StandingRule
DELETE /api/rules/:id                      → 204
POST   /api/rules/:id/dry-run              → { matches: DryRunMatch[], totalCandidates }
GET    /api/rules/templates                → StandingRule[]
POST   /api/rules/templates/:templateId/instantiate → 201 StandingRule
```

From existing Phase 1+ agent surface (frontend already calls this — confirm endpoint exists; if not, Wave 2 adds it):
```
GET /api/agent/tools/schema → Array<{
  name: string,
  description: string,
  sideEffect: "read" | "write",
  editableParams?: string[],
  inputSchema: JSONSchema,
  requiredRole: string[],
}>
```
If `/api/agent/tools/schema` does not yet exist, Wave 2 Task 1 adds it as a simple backend route (5 lines — return toolRegistry.list().map(t => ({ name, description, sideEffect, editableParams, inputSchema, requiredRole }))). Grep `backend/src/routes` for "tools/schema" first.

From backend forecast tools (Wave 1 just registered):
```
forecastDemand(input: { zone?, horizonHours? }) → DemandForecast { tier, forecast: number[], ci90: { lower, upper }, message? }
forecastSupplyGap(input: { zone? }) → SupplyGapResult { tier, gaps: Array<{ hourOfWeek, forecastDemand, scheduledSupply, gap }>, message? }
forecastCashFlow(input: {}) → CashFlowProjection { tier, horizon30, horizon60, horizon90, trend: { slope, intercept, rSq }, message? }
```
These tools are accessed via the existing agent invocation HTTP path. Read backend/src/routes/ai.ts or backend/src/routes/aiChiefOfStaff.ts to confirm the invocation shape — likely `POST /api/agent/tools/:toolName/invoke` or similar. If a direct REST surface is missing, Wave 2 Task 4 adds `GET /api/forecast/demand`, `/api/forecast/supply-gap`, `/api/forecast/cash-flow` thin wrappers that delegate to the tool.

From existing frontend pattern (read first, replicate):
```typescript
// frontend/src/hooks/useDecisions.ts (Phase 2)
export function useDecisions() {
  return useQuery({
    queryKey: ["decisions"],
    queryFn: () => fetch("/api/decisions").then(r => r.json()),
    staleTime: 30_000,
  });
}
```

From existing Shadcn primitives (already installed):
```
@/components/ui/button, card, dialog, form, input, select, switch, table, tabs, badge, separator, textarea
```
The `Form` primitive uses react-hook-form + zodResolver — pattern from `frontend/src/app/(dashboard)/admin/onboarding/page.tsx` (Phase 2).

From Phase 2 /decisions page structure:
```typescript
// frontend/src/app/(dashboard)/decisions/page.tsx
// Renders <ProposalCard /> for each pending action. Wave 2 ADDS a <ProposalCardRuleFooter />
// conditionally rendered when card.source === "rule".
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rule list + builder pages + components + hooks + lib (the /rules surface)</name>
  <files>
    frontend/src/app/(dashboard)/rules/page.tsx,
    frontend/src/app/(dashboard)/rules/[id]/page.tsx,
    frontend/src/app/(dashboard)/rules/new/page.tsx,
    frontend/src/components/rules/RuleBuilder.tsx,
    frontend/src/components/rules/RuleList.tsx,
    frontend/src/components/rules/RuleDryRunPreview.tsx,
    frontend/src/components/rules/RuleJsonEditor.tsx,
    frontend/src/components/rules/RuleExportImport.tsx,
    frontend/src/hooks/useRules.ts,
    frontend/src/hooks/useToolSchema.ts,
    frontend/src/lib/api/rules.ts,
    frontend/src/lib/types/rules.ts,
    frontend/src/components/layout/Sidebar.tsx,
    frontend/src/app/(dashboard)/rules/__tests__/RuleEditor.test.tsx
  </files>
  <action>
    **1. lib/types/rules.ts** — Mirror backend's `services/standingRules/types.ts`. Define `StandingRuleCondition` (discriminated union on event), `StandingRuleAction`, `StandingRuleThrottle`, `StandingRule`, `DryRunMatch`. These are TypeScript types only (no Zod on the FE) — runtime validation happens on backend.

    **2. lib/api/rules.ts** — Thin `fetch()` wrappers per the 8 endpoints in the <interfaces> block. Use the existing API helper if one exists (grep `frontend/src/lib/api/` for `fetchWithAuth` or similar). Each wrapper:
       - Sets `Content-Type: application/json` and `credentials: "include"` (cookies for JWT).
       - Throws on non-2xx with `{ status, message }`.
       - Returns parsed JSON.

    **3. hooks/useRules.ts** — React Query hooks:
       - `useRules()` — list, queryKey `["rules"]`, staleTime 30s.
       - `useRule(id)` — single, queryKey `["rule", id]`.
       - `useCreateRule()` — mutation; on success invalidate `["rules"]`.
       - `useUpdateRule()` / `useDeleteRule()` — mutations.
       - `useDryRunRule()` — mutation; returns matches.
       - `useRuleTemplates()` — staleTime infinite (templates don't change at runtime).
       - `useInstantiateTemplate()` — mutation; on success invalidate `["rules"]`.

    **4. hooks/useToolSchema.ts** — `useToolSchema()` queries `/api/agent/tools/schema`. If grep confirms the endpoint does not exist, add a backend route (single file: `backend/src/routes/agentTools.ts` with `GET /api/agent/tools/schema` returning `toolRegistry.list().map(toPublic)`; mount in server.ts under authMiddleware + tenantScope; RBAC any logged-in user). Make this addition only if grep returns no match.

    **5. components/rules/RuleList.tsx** — Shadcn `<Table>` with columns: Name, Event, Enabled (Shadcn `<Switch />` calls useUpdateRule), Last Fired, Fire Count, Actions (Edit / Delete / Export). Pagination via Phase 2's pagination helper. Empty state: "No rules yet. Start from a template →" with CTA button routing to `/rules/new?from=template`.

    **6. components/rules/RuleBuilder.tsx** — The core visual editor. Two tabs (Shadcn `<Tabs>`): "Visual" + "JSON".
       - Visual tab structure:
         ```
         <Card>
           When... [Select event] [if aggregate: Input "Within X days"] [Input threshold count]
                   [Optional Where filters: platform select, zone input]
           Then... [Select tool from sideEffect=action tools] [Dynamic params per editableParams]
           Throttle [perSubject window+max] [perTenant window+max]
           Enabled [Switch]
           [Test against last 7 days] [Save]
         </Card>
         ```
       - For dynamic params: `tools.find(t => t.name === selectedToolName).editableParams.map(p => <Input label={p} />)`.
       - "Test against last 7 days" button calls `useDryRunRule()` and side-renders `<RuleDryRunPreview />` in a right panel (Shadcn `<Sheet>` from the right).
       - Save → `useCreateRule()` (new) or `useUpdateRule()` (existing).
       - Validation: client-side check `condition.event !== ""`, `action.tool !== ""`, `threshold` values are positive integers. Backend Zod is the authoritative validator; FE only blocks obviously-empty submits.

    **7. components/rules/RuleJsonEditor.tsx** — JSON tab content. Shadcn `<Textarea>` with monospace + `JSON.parse(value)` validation on blur. Shows error highlight if invalid; disables Save while invalid. No new dependency — avoid installing Monaco/codemirror.

    **8. components/rules/RuleDryRunPreview.tsx** — `<Sheet>` showing match list:
       ```
       Last 7 days: N hypothetical matches
       ├ 2026-05-11 14:32  ← Driver M.A. (Hawally)  [sample: {late by 12min}]
       ├ 2026-05-12 06:05  ← Driver K.O. (Salmiya)  [sample: {late by 25min}]
       └ ...
       ```
       PII redaction already applied by backend dryRunService; FE just renders.

    **9. components/rules/RuleExportImport.tsx** — Two buttons:
       - Export → opens a `<Dialog>` with the rule's JSON (tenantId + id stripped — re-instantiable as a template). Has "Copy to clipboard" and "Download .json" actions.
       - Import → opens a `<Dialog>` with a `<Textarea>` accepting pasted JSON. On Submit calls `useCreateRule()` after JSON.parse + minimum-shape check (`condition.event` + `action.tool` exist).

    **10. app/(dashboard)/rules/page.tsx** — Server component shell + client `<RuleListClient />` wrapper. Header: "Standing Rules" + "+ New rule" button + "View templates" link. Renders `<RuleList />` + paginates.

    **11. app/(dashboard)/rules/[id]/page.tsx** — Loads rule via `useRule(params.id)`. Renders `<RuleBuilder rule={rule} />`. Side pane: `<RuleDryRunPreview />` (initially closed; opens on "Test" button). Header: rule name + enabled switch + export button.

    **12. app/(dashboard)/rules/new/page.tsx** — Search params: `?from=template&templateId=...` pre-fills the builder with template data. Otherwise empty builder.

    **13. components/layout/Sidebar.tsx** — Add two nav items:
       ```typescript
       { href: "/rules", label: "Rules", icon: GitBranch, roles: ["ADMIN"] },
       { href: "/forecast", label: "Forecast", icon: TrendingUp, roles: ["ADMIN","OPS_MANAGER","ACCOUNTANT"] },
       ```
       Place after the existing "Decisions" entry. Respect existing role-gating pattern.

    **14. __tests__/RuleEditor.test.tsx** — Vitest + @testing-library/react. Cases:
       - Renders the builder with template-prefilled fields.
       - Clicking "Test against last 7 days" calls dry-run mutation and renders match list.
       - Invalid JSON in the JSON tab disables Save button.
       - Switching event in Visual tab re-renders threshold inputs.

    Test gate: `cd frontend && npm run test:run -- rules` GREEN.
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/Darb/frontend &amp;&amp; npm run test:run -- rules 2&gt;&amp;1 | tail -15 &amp;&amp; npm run build 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <done>
    - /rules + /rules/new + /rules/[id] routes render
    - RuleBuilder reads editableParams from /api/agent/tools/schema and renders dynamic param inputs
    - Dry-run shows hypothetical matches with PII redacted
    - Export/Import JSON works
    - Sidebar shows /rules link (ADMIN only)
    - Vitest test GREEN
    - `npm run build` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: /forecast page + 3 chart components + CashFlowProjection shared with /finance/expenses-pl + hooks/lib</name>
  <files>
    frontend/src/app/(dashboard)/forecast/page.tsx,
    frontend/src/app/(dashboard)/forecast/DemandForecastChart.tsx,
    frontend/src/app/(dashboard)/forecast/SupplyGapPanel.tsx,
    frontend/src/app/(dashboard)/forecast/CashFlowProjection.tsx,
    frontend/src/app/(dashboard)/forecast/__tests__/DemandForecastChart.test.tsx,
    frontend/src/app/(dashboard)/finance/expenses-pl/page.tsx,
    frontend/src/hooks/useForecast.ts,
    frontend/src/lib/api/forecast.ts,
    frontend/src/lib/types/forecast.ts
  </files>
  <action>
    **1. lib/types/forecast.ts** — Mirror backend's `services/forecasting/types.ts`. Types: `ForecastTier`, `DemandForecast`, `SupplyGapResult`, `CashFlowProjection`.

    **2. lib/api/forecast.ts** — Fetch wrappers. If Wave 1 didn't add REST wrappers and forecasts are only callable via `/api/agent/tools/:toolName/invoke`, use that endpoint:
       ```typescript
       export async function fetchForecastDemand(input: { zone?: string, horizonHours?: number }): Promise<DemandForecast> {
         const r = await fetch("/api/agent/tools/forecastDemand/invoke", {
           method: "POST",
           credentials: "include",
           headers: { "Content-Type": "application/json" },
           body: JSON.stringify({ input }),
         });
         if (!r.ok) throw new Error(`forecastDemand failed: ${r.status}`);
         return r.json();
       }
       ```
       Otherwise (if Wave 1 added thin REST wrappers per the <interfaces> note), use those. Pick whichever exists; do not add new backend routes here.

    **3. hooks/useForecast.ts** — React Query hooks with `staleTime: 5 * 60 * 1000` (5 min — forecasts are cached server-side daily but this avoids re-fetch chatter):
       - `useForecastDemand(zone?)`
       - `useForecastSupplyGap(zone?)`
       - `useForecastCashFlow()`

    **4. DemandForecastChart.tsx** — Props: `data: DemandForecast, zone?: string`.
       - tier=insufficient → render fallback `<Card>Forecast available after 14 days of operations</Card>` with Lucide `TrendingUp` icon.
       - tier=averaged or hw → Recharts `<LineChart>` showing forecast[] over `horizonHours` (default 168 hours = 7 days). X axis: day-of-week labels (Mon/Tue/...). Y axis: orders/hour.
       - Shaded `<Area>` between `ci90.lower` and `ci90.upper`.
       - Below the line chart: a 24×7 Tailwind grid heatmap (`grid grid-cols-24 gap-px`) coloring each hour cell by forecast intensity (Tailwind `bg-sand-{200..900}` palette per current design system). Click a cell scrolls the line chart to that hour.
       - Tier badge top-right: "Holt-Winters" or "DOW average" or "Insufficient history".

    **5. SupplyGapPanel.tsx** — Props: `data: SupplyGapResult`.
       - tier=insufficient → fallback copy.
       - else → Shadcn `<Table>` showing top-24 gap hours: hour-of-week, forecast demand, scheduled supply, gap, severity badge (red if gap > 50%, amber if 25-50%, yellow if < 25%).
       - Below: a horizontal stacked bar chart (Recharts `<BarChart layout="vertical">`) of scheduled vs forecast for each hour.

    **6. CashFlowProjection.tsx** — Props: `data: CashFlowProjection, mode: "forecast-tab" | "finance-section"`.
       - tier=insufficient → fallback "Cash flow forecast available after 30 days of operations".
       - else → 3 KPI tiles (30/60/90 day projected net), then Recharts `<LineChart>` showing historical 90-day net + projected next 90 days, with `slope` annotated as a trend line.
       - r² shown below as a small caption ("Confidence r² = 0.82").
       - When `mode="finance-section"`, render in a more compact layout (single row of KPIs + small chart) suitable for embedding below the historical P&L table on `/finance/expenses-pl`.

    **7. app/(dashboard)/forecast/page.tsx** — Shell + Shadcn `<Tabs defaultValue="demand">` with 3 tabs:
       ```
       Demand [DemandForecastChart] (with zone selector dropdown above the chart)
       Supply [SupplyGapPanel] (zone selector shared with Demand tab via local state)
       Cash Flow [CashFlowProjection mode="forecast-tab"]
       ```
       Page header: "Forecast" + "Refresh" button (calls `queryClient.invalidateQueries(['forecast'])`).
       Per-platform breakdown on Demand tab: a row of small per-platform charts below the main chart (one per platform: KEETA, TALABAT, DELIVEROO, AMERICANA). Each is `<DemandForecastChart data={useForecastDemand(zone)} zone={zone}>` — wait, the existing tool doesn't accept a platform filter. Replace with per-platform via separate calls or fold into the zone selector. Simplest: ship per-zone first; per-platform deferred to Phase 12.1 if it is not in the read-tool signature.

    **8. app/(dashboard)/finance/expenses-pl/page.tsx** — Read the existing file. Append `<CashFlowProjection mode="finance-section" data={useForecastCashFlow().data} />` below the existing historical P&L section. Add a section header "Projection". Existing tests + layout untouched.

    **9. __tests__/DemandForecastChart.test.tsx** — Vitest cases:
       - tier="insufficient" → renders fallback copy "Forecast available after 14 days".
       - tier="hw" + forecast.length===168 → renders Recharts container (use `container.querySelector('.recharts-wrapper')` presence check).
       - Heatmap renders 168 cells.

    Test gate: `cd frontend && npm run test:run -- forecast` GREEN.
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/Darb/frontend &amp;&amp; npm run test:run -- forecast 2&gt;&amp;1 | tail -15 &amp;&amp; npm run build 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <done>
    - /forecast page renders all 3 tabs with chart components
    - Cold-start tier shows fallback copy block instead of empty chart
    - /finance/expenses-pl shows CashFlowProjection in the new section
    - Vitest test GREEN
    - `npm run build` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 3: /decisions rule-footer integration + briefing anomaly threshold + sidebar finalisation</name>
  <files>
    frontend/src/components/decisions/ProposalCardRuleFooter.tsx,
    frontend/src/app/(dashboard)/decisions/page.tsx,
    backend/src/agent/prompts/briefing-owner.md,
    backend/src/services/briefings/narrator.ts
  </files>
  <action>
    **1. ProposalCardRuleFooter.tsx** — Small component:
       ```tsx
       export function ProposalCardRuleFooter({ ruleId, ruleName }: { ruleId: string; ruleName: string }) {
         return (
           <div className="text-xs text-sand-500 mt-2 pt-2 border-t border-sand-200">
             via rule:{" "}
             <Link href={`/rules/${ruleId}`} className="underline hover:text-sand-700">
               {ruleName}
             </Link>
           </div>
         );
       }
       ```
       Lucide `Sparkles` icon (or `GitBranch`) optional.

    **2. /decisions/page.tsx integration** — Read the existing page. Find the `<ProposalCard>` render block. The card already receives the proposal data (PendingAgentAction row). Add:
       ```tsx
       {proposal.source === "rule" && proposal.standingRuleId && proposal.standingRuleName && (
         <ProposalCardRuleFooter ruleId={proposal.standingRuleId} ruleName={proposal.standingRuleName} />
       )}
       ```
       **Backend support:** The /api/decisions list endpoint (Phase 2) currently shapes proposals. Verify (read backend/src/routes/decisions.ts) whether it joins StandingRule. If not, add a `LEFT JOIN StandingRule ON PendingAgentAction.standingRuleId = StandingRule.id` to the projection — minimal Prisma change: `include: { standingRule: { select: { id: true, name: true } } }` on the findMany. Update the response shape + types accordingly.

    **3. briefing-owner.md prompt (Phase 11)** — Append to the existing prompt:
       ```markdown

       ## Forecast injection rule

       Only mention demand or supply forecasts when |forecast - 4-week average| > 15% for any zone-hour bucket. If every bucket is within ±15% of the 4-week average, say nothing about forecasts — silence is better than noise. When the threshold is crossed, identify the specific zone + hour-bucket and propose a corrective action ("Hawally Fri 18-20h forecast 35% above last 4 weeks — consider adding 2 drivers to evening shifts").
       ```

    **4. services/briefings/narrator.ts** — The narrator service (Phase 11) is the morning briefing's tool-invocation orchestrator. It currently calls a set of read tools and feeds them to the prompt. Add a guard before invoking forecastDemand:
       ```typescript
       // Before adding forecast to the briefing context, check anomaly threshold.
       const demand = await forecastDemand.execute(ctx, { horizonHours: 168 });
       if (demand.tier === "insufficient") { /* skip — no forecast section */ }
       else {
         const fourWkAvg = await fetchFourWeekHourlyAverage(ctx.tenantId);
         const anomalies = demand.forecast
           .map((f, h) => ({ h, forecast: f, avg: fourWkAvg[h % 168] ?? 0 }))
           .filter(({ forecast, avg }) => avg > 0 && Math.abs(forecast - avg) / avg > 0.15);
         if (anomalies.length > 0) {
           briefingCtx.forecastAnomalies = anomalies.slice(0, 5);  // top 5
         }
         // else: omit forecast section entirely
       }
       ```
       Implement `fetchFourWeekHourlyAverage(tenantId)` as a thin helper in services/briefings/ that runs:
       ```sql
       SELECT EXTRACT(dow FROM timestamp AT TIME ZONE 'Asia/Kuwait') * 24 + EXTRACT(hour FROM timestamp AT TIME ZONE 'Asia/Kuwait') AS bucket,
              AVG(CASE WHEN action='order_delivered' THEN 1 ELSE 0 END) AS avg_orders
       FROM "OrderEvent"
       WHERE "tenantId"=${tenantId} AND timestamp >= NOW() - INTERVAL '4 weeks'
       GROUP BY 1
       ```
       Cache result in AgentMemory with key `forecast:4wk-avg:${todayISO}` + source="cached_forecast".

    **5. Sidebar visual confirmation** — Already added in Task 1; verify it works after both /rules and /forecast are wired.

    **6. End-to-end smoke (manual after deploy)** — log in as ADMIN; click sidebar `Rules`; create a rule from template; click `Test against last 7 days`; verify matches show; visit `/decisions` after the rule fires once (Wave 1 evaluator must have produced a rule-source PendingAgentAction in test data) → see "via rule: …" footer link.
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/Darb/frontend &amp;&amp; npm run test:run 2&gt;&amp;1 | tail -10 &amp;&amp; cd /Users/mac/Documents/Darb/backend &amp;&amp; npm test -- --testPathPatterns='(briefings|decisions)' 2&gt;&amp;1 | tail -10 &amp;&amp; npm run lint:tenant 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <done>
    - ProposalCardRuleFooter shown only when proposal.source === "rule"
    - /api/decisions list endpoint returns standingRuleId + standingRuleName when applicable
    - briefing-owner.md prompt extended with anomaly-only forecast rule
    - narrator.ts gates forecast injection on |forecast - 4wk_avg| > 15%
    - All frontend Vitest tests GREEN; backend tests still GREEN; lint:tenant exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/rules | ADMIN role required for writes; OPS_MANAGER for read+dryRun; tenantScope mid blocks cross-tenant access |
| RuleBuilder.action.params | Owner-supplied params filtered to editableParams allow-list on the backend at fire time (Wave 1 invoker.ts) |
| Rule Import dialog → useCreateRule | Pasted JSON Zod-validated server-side; FE only does minimum-shape check before submit |
| /api/decisions enriched payload | New include path: standingRule.{id,name} — must respect tenantScope (Prisma include is automatically tenant-scoped via FK) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-W2-01 | Tampering | Pasted JSON in Rule Import | mitigate | Backend POST /api/rules Zod-validates; FE minimum-shape check (event + tool fields exist) is a UX guard not a security guard |
| T-12-W2-02 | Information Disclosure | DryRunPreview leaks driver names/phones | mitigate | Backend dryRunService already redacts PII (first-initial+last-name, phone masking); FE renders as-is |
| T-12-W2-03 | Information Disclosure | useForecast hooks cache on the client | accept | React Query cache is per-user-session and cleared on logout; no cross-tenant leakage path because each fetch is JWT-authorised |
| T-12-W2-04 | Elevation of Privilege | OPS_MANAGER user views /rules nav link | mitigate | Sidebar filter: `roles: ["ADMIN"]` on /rules entry → link hidden for non-ADMIN; backend POST /api/rules also rejects with 403 if accessed via direct URL |
| T-12-W2-05 | Tampering | /api/decisions list now reads StandingRule rows | mitigate | Prisma `include: { standingRule: ... }` uses the FK — Postgres enforces referential integrity + cross-tenant FK is impossible because StandingRule.tenantId === PendingAgentAction.tenantId always (Wave 1 invariant) |
| T-12-W2-06 | Repudiation | who created a rule via Import | mitigate | useCreateRule submits with current user as createdBy; backend asserts ctx.userId matches the JWT subject; rule row has createdBy + createdAt for audit |
</threat_model>

<verification>
After all 3 tasks:
- `cd frontend && npm run test:run` — Vitest GREEN across all suites including new rules + forecast component tests.
- `cd frontend && npm run build` — Next.js production build exits 0.
- `cd backend && npm test` — backend GREEN (briefings + decisions tests still pass after include changes).
- `cd backend && npm run lint:tenant` — exits 0.
- Manual smoke (post-deploy): sign in as ADMIN, sidebar shows Rules + Forecast, /rules/new renders builder, /forecast shows 3 tabs, /finance/expenses-pl shows Projection section, /decisions shows "via rule: …" footer on a rule-fired card.
</verification>

<success_criteria>
- /rules + /forecast surfaces live and discoverable from sidebar.
- RuleBuilder renders dynamic action.params per the selected tool's editableParams allow-list (no hard-coded form per tool).
- Dry-run preview shows hypothetical matches with PII redacted.
- Export/Import JSON enables founder to seed design-partner-2 with the design-partner-1 rule set in Wave 3.
- Three forecast charts handle all three tiers (insufficient / averaged / hw) gracefully.
- CashFlowProjection rendered on both /forecast and /finance/expenses-pl from a single component (no duplication).
- /decisions footer link traces rule-fired proposals back to their source rule.
- Morning briefing forecast injection respects the 15% anomaly threshold (no noise when forecasts are nominal).
- All tests green; lint:tenant 0 errors; production build successful.
</success_criteria>

<output>
After completion, create `.planning/phases/12-standing-rules-forecasting/12-02-SUMMARY.md` per template.
</output>
